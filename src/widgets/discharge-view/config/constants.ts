import { MEASUREMENT_ITEMS } from '@/shared/config/measurement';

/**
 * 차트 높이(px).
 *
 * **스켈레톤·빈 상태·실제 차트 셋이 같은 값을 봐야 한다** `[사용자 지적 2026-09-07]`. 세
 * 자리에 숫자를 박아 두면 한 곳만 바뀌어도 값이 도착할 때 카드 높이가 튄다 — 스켈레톤이
 * 없애려던 바로 그 점프를 스켈레톤이 만든다.
 *
 * **한 값이 된 이유** — 누적 차트가 빠지며 유량과 수위가 **위아래로 겹쳐 섰다**
 * `[회의 2026-09-08]`. 둘은 같은 시간축을 보는 한 쌍이라 높이가 다르면 같은 구간의 변화폭이
 * 달라 보인다. 전에는 유량이 전폭(240)·수위가 절반 폭(200)이라 애초에 견줄 수 없었다.
 */
export const CHART_HEIGHT = 240;

/**
 * 타일 셋의 제목. **스켈레톤이 이 순서를 그대로 쓴다** — 순서는 읽는 차례이고
 * (내보내고 있나 → 얼마나 → 수조는) 값이 도착할 때 자리가 바뀌면 안 된다.
 *
 * **넷이었다.** 마지막 칸이 `금일 누적 배출량`이었고 `[회의 2026-09-08]`이 필요 없다고
 * 정리했다. 남은 셋이 `[원문 p.1]`의 **배출 데이터 3종**(유량·수위·방류 여부)과 정확히 같다.
 *
 * 수위는 **항목 사전에서 읽는다.** 화면이 쓰는 것도 `MEASUREMENT_ITEMS.level.label`이라,
 * 여기 글자로 박으면 사전이 바뀔 때 스켈레톤만 옛 이름을 남긴다(**E1**).
 */
export const TILE_LABELS = ['방류 상태', '실시간 배출 유량', MEASUREMENT_ITEMS.level.label];
