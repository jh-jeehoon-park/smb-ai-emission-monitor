/**
 * 로그인 배경 영상 `[사용자 지시 2026-08-25]`.
 *
 * `public/`에 있는 파일이라 경로가 곧 URL이다. **여기 한 곳에서만 적는다** — 파일 이름을
 * 컴포넌트에 박아 두면 영상을 갈아 끼울 때 어디를 고쳐야 하는지 찾아야 한다.
 */
export const LOGIN_VIDEO_SRC = '/login-movie_3.mp4';

/**
 * 영상이 무엇을 담고 있는지. 보조기술에는 숨기지만(`aria-hidden`) 대체 설명은 남긴다.
 *
 * **고른 영상을 실제로 보고 적었다** `[사용자 요청 2026-09-17]`. 포스터를 뽑으면서 프레임을
 * 확인했다 — 하늘에서 내려다본 폐수처리장이고 원형 침전지 여섯 기가 가운데에 있다.
 *
 * 한때 *"방류구에서 물이 흘러나오는 장면"* 이라 적혀 있었는데 그것은 **영상 1을 보고 쓴 것**이라,
 * 고른 영상이 바뀐 뒤로는 보지 않은 것을 말하고 있었다. 잠시 장면을 적지 않는 문구로 두었다가
 * 확인한 지금 다시 적는다.
 */
export const LOGIN_VIDEO_LABEL = '하늘에서 내려다본 폐수처리장의 침전지';

/**
 * **고른 영상의 첫 프레임을 뽑아 둔 정지 이미지 둘** `[사용자 결정 2026-09-17: 모바일은
 * 정지 이미지로 대체]`. 같은 프레임이고 **잘라낸 모양만 다르다.**
 *
 * | 상수 | 크기 | 쓰이는 곳 |
 * |---|---|---|
 * | `LOGIN_POSTER_SRC` | 1920×1080 | `<video poster>` — 넓은 화면에서 65.2MB가 받아지는 동안 |
 * | `LOGIN_POSTER_PORTRAIT_SRC` | 1216×2160 | 좁은 화면의 배경 그 자체 |
 *
 * **왜 좁은 화면은 영상이 아닌가** — 그 폭에서는 유리 기둥이 화면 전체를 덮고 `blur(20px)`이
 * 걸려 **영상이 한 번도 보이지 않는다**(실측: 390·640·768px에서 패널이 화면의 100%).
 * 보이지도 않는 것에 65.2MB를 쓰고 있었다.
 *
 * **왜 세로본이 따로 있는가** `[사용자 지적 2026-09-17: 정지된 이미지는 흐려보이며 화질이
 * 좋지 않아보임]` — 한 장(1080×608)으로 둘을 겸하던 판본이 **세로 화면에서 네 배로 늘어났다.**
 * `object-cover`는 짧은 쪽을 기준으로 채우므로, 390×844 화면(DPR 3 → 1170×2532)을 16:9
 * 그림으로 덮으려면 **가로 4,500px이 필요하다.** 원본이 1080이라 그만큼 확대됐다.
 * 화면과 같은 세로 모양으로 잘라 두면 그 곱이 1.2배로 떨어진다 — 같은 무게로 선명해진다.
 * 잘라내는 자리는 **가로 가운데**이며, 좁은 화면이 지금 실제로 보고 있던 구간 그대로다.
 *
 * 세로본은 `next/image`가 폭에 맞춰 줄여 WebP로 내보내므로 **원본 용량이 전송량이 아니다.**
 * 가로본은 `poster` 속성이라 최적화를 거치지 않고 그대로 나간다 — 그쪽만 화질을 낮춰 잡는다.
 *
 * > **고른 영상을 바꾸면 두 장 다 다시 뽑는다.** 안 뽑으면 좁은 화면과 넓은 화면이 서로
 * > 다른 장면을 보인다. 잘라내는 x좌표는 `(가로 − 세로×9÷16) ÷ 2`이며 아래 값은 4K 기준이다.
 * >
 * > `ffmpeg -y -i public/<고른 영상> -vf scale=1920:1080 -frames:v 1 -q:v 5 public/login-poster.jpg`
 * > `ffmpeg -y -i public/<고른 영상> -vf crop=1216:2160:1312:0 -frames:v 1 -q:v 3 public/login-poster-portrait.jpg`
 */
export const LOGIN_POSTER_SRC = '/login-poster.jpg';
export const LOGIN_POSTER_PORTRAIT_SRC = '/login-poster-portrait.jpg';

/**
 * **이 화면이 «넓은 화면»으로 치는 폭** — 768px부터다 `[사용자 요청 2026-09-17: 768px 기준
 * 아래로 Mobile 반응형을 잡을 것]`.
 *
 * 한때 1024px(`lg`)이었고 **앱 셸의 사이드바와 같은 값을 쓰던 것을 근거로 `shared/config`에
 * 올려 두었다.** 사용자가 이 화면만 768px로 내리면서 그 전제가 깨져 값을 이 화면으로 되돌렸다 —
 * 셸은 1024px 그대로다 `[사용자 결정 2026-09-17: 로그인 화면만]`. 두 화면이 같은 폭을 쓸
 * 이유가 원래 없었다: 로그인에는 사이드바가 없다.
 *
 * **세 곳이 같은 값을 봐야 한다.**
 *   ① 이 상수 — 배경 영상을 **마운트할지**(CSS로는 내려받기를 못 막는다)
 *   ② `login-view.tsx`의 `md:` 클래스 — 배치·타이포
 *   ③ `globals.css`의 `.login-glass` 미디어 쿼리 — 흐린 유리
 *
 * 어긋나면 «영상은 붙었는데 유리 기둥은 아직 카드»처럼 한 화면이 두 판본으로 그려진다.
 * `48rem`이 Tailwind의 `md`이고 `login-media.test.ts`가 셋을 함께 못박는다.
 */
export const LOGIN_WIDE_QUERY = '(min-width: 48rem)';

/** 아이디를 기억해 두는 자리. 값은 사용자가 적은 아이디 문자열뿐이다 */
export const REMEMBERED_ID_KEY = 'smb-remembered-id';

/* ───────────────────────────────────────────────────────────────────────────
 * 여기부터 아래는 **시연용 임시물이다.**
 *
 * TODO(영상 확정 시 제거): `[사용자 요청 2026-09-07]` — 시연에서 수처리 영상 여러 개를
 * 보여 준 뒤 하나로 픽스할 예정이고, **그때 이 블록과 `LoginVideoPicker`를 함께 지운다.**
 *
 * **영상은 3으로 정해졌다** `[사용자 요청 2026-09-17]`. 그런데 **고르는 기능은 남긴다** —
 * 같은 요청이 *"필터 기능은 유지하되"* 라 못박았다. 그래서 이 블록은 «미확정이라 남긴 것»이
 * 아니라 «확정됐는데도 남겨 둔 것»이고, 지우는 시점은 사용자가 따로 정한다.
 *
 * 지울 때 할 일은 셋뿐이다.
 *   ① 이 구분선 아래를 전부 지운다
 *   ② `login-view.tsx`에서 `LoginVideoPicker`와 그 상태를 지운다(`LOGIN_VIDEO_SRC`만 남는다)
 *   ③ 고른 영상 파일만 `public/`에 남기고 나머지 `login-movie_*.mp4`를 지운다
 *
 * **골라 둔 값이 남아 있으면 지운 뒤에도 그 영상이 뜬다.** 저장 키를 함께 지우므로
 * 그럴 일이 없다 — 위 `LOGIN_VIDEO_SRC`가 유일한 원천으로 돌아간다.
 * `login-media.test.ts`가 이 세 자리를 함께 못박는다.
 * ─────────────────────────────────────────────────────────────────────────── */

/**
 * 고를 수 있는 영상.
 *
 * **이름을 «영상 N»으로 둔다.** 내용을 확인하지 않았으므로 장면을 적지 않는다 — 그럴듯한
 * 설명을 붙이면 화면이 보지 않은 것을 말하게 된다. 고르는 사람은 지금 보이는 화면으로 판단한다.
 *
 * **번호는 보이는 순서를 따른다** `[사용자 요청 2026-09-17: 영상 순서는 유지하고 영상 1,
 * 영상 2, 영상 3 으로 변경]`. 그래서 **이름의 번호와 파일 이름의 번호가 다르다** —
 * 고른 영상(`login-movie_3.mp4`)이 «영상 1»이다.
 *
 * 그 전에는 반대로 정해 두었다: 번호를 파일에 붙여 두고 목록을 `3 · 1 · 2`로 두었는데,
 * 근거는 *"시연에서 «영상 3»이라 부르던 것이 다른 것을 가리키게 된다"* 였다. 사용자가
 * 보이는 순서대로 부르기로 정해 그 근거가 걷혔다 — **파일을 말할 때는 번호가 아니라 경로로
 * 말한다**(이 배열이 그 대응을 갖는 유일한 자리다).
 *
 * 첫 항목이 기본값이고 `LOGIN_VIDEO_SRC`와 같아야 한다 — 갈리면 저장값이 없는 첫 방문과
 * 저장값을 지운 뒤가 서로 다른 영상을 띄운다.
 */
export const LOGIN_VIDEOS = [
  { src: LOGIN_VIDEO_SRC, name: '영상 1' },
  { src: '/login-movie_1.mp4', name: '영상 2' },
  { src: '/login-movie_2.mp4', name: '영상 3' },
] as const;

/**
 * 고른 영상을 기억해 두는 자리. 값은 `src` 문자열이다.
 *
 * 번호가 아니라 경로를 담는 이유: 목록이 줄어도(파일을 지워도) 없는 번호를 가리키지 않는다 —
 * 아래 `resolveLoginVideo`가 목록에 없는 값을 기본값으로 떨군다.
 */
export const LOGIN_VIDEO_KEY = 'smb-login-video';

/**
 * 저장값을 실제로 띄울 영상으로 바꾼다.
 *
 * **저장값을 믿지 않는다.** 파일을 지우거나 목록을 줄인 뒤에도 옛 경로가 남아 있으면
 * 배경이 통째로 검게 뜬다(`<video>`가 404를 조용히 먹는다). 목록에 있는 것만 통과시킨다.
 */
export function resolveLoginVideo(stored: string | null): string {
  return LOGIN_VIDEOS.some((video) => video.src === stored) ? stored! : LOGIN_VIDEO_SRC;
}
