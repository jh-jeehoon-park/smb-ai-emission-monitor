/** 3단 스택용 낮은 변형. 세 단 + 표·범례가 한 패널에 들어가야 한다 */
/**
 * 미래 구간을 몇 시간 두는가. `[원문 발표 p.16 그림]`의 축이 `−12h ~ 현재 ~ +6h`다.
 *
 * **그리는 것은 음영뿐이다** — 6시간 예측의 대상이 정해진 것이 전혀 없어(`[TBD-52]`)
 * 곡선은 없다. 자리를 두는 이유는 추정치(왼쪽)와 예측치(오른쪽)의 **시제가 형태로**
 * 드러나게 하려는 것이다 `[사용자 결정 2026-08-25]`.
 */
export const FUTURE_HOURS = 6;

export const COMPACT_HEIGHT = 128;
export const FULL_HEIGHT = 210;
