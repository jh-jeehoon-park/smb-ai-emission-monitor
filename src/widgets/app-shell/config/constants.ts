

/**
 * 헤더 알림에 한 번에 보이는 알람 수.
 *
 * 전부 보이면 팝오버가 화면을 덮는다. 여기는 "무슨 일이 있었나"를 훑는 자리이고
 * 전수 확인은 알람 이력 화면이 맡는다 — 목록 아래 링크가 그리로 보낸다.
 */
export const HEADER_ALARM_LIMIT = 5;

/**
 * 현황판의 **나가기 버튼이 다시 숨는 시간**(ms).
 *
 * 그 화면은 셸을 그리지 않아 사이드바도 헤더도 없다 — 전체화면으로 띄우면 **나갈 길이
 * 하나도 없었다** `[사용자 지적 2026-09-11]`. 그렇다고 버튼을 늘 띄워 두면 주관사 요구와
 * 부딪힌다: *"모니터에 그냥 띄어놓고 보지, 계속 움직이거나 이동하는 것은 지양한다."*
 *
 * **아무도 만지지 않는 동안에는 없는 것과 같게** 둔다 — 포인터가 움직이거나 키를 누르면
 * 나타나고, 손을 떼면 이만큼 뒤에 사라진다. 벽에 걸린 화면에서는 영영 나타나지 않는다.
 *
 * 값은 영상 재생기의 제어 막대와 같은 감각으로 잡았다. 더 짧으면 버튼까지 포인터를 옮기는
 * 사이에 사라지고, 더 길면 조작을 끝낸 뒤에도 벽에 남는다.
 */
export const WALLBOARD_EXIT_HIDE_MS = 4_000;

/**
 * 현황판에서 **마우스 포인터를 감추는** 클래스 `[사용자 요청 2026-09-11]`.
 *
 * 규칙은 `globals.css`가 갖는다(`cursor: none`). 이름을 여기 두는 이유는 붙이는 쪽과 지우는
 * 쪽이 **같은 문자열**을 봐야 하기 때문이다 — 한쪽만 고치면 화면을 떠난 뒤에도 포인터가
 * 돌아오지 않는다.
 */
export const WALLBOARD_CURSOR_IDLE_CLASS = 'wall-cursor-idle';
