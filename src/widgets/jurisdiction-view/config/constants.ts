/**
 * 배관을 우회하면 유량계를 지나지 않아 방류가 관측되지 않는다 `[원문 발표 p.50]`.
 * 감독 기관이 쓰는 화면이라 이 한계가 화면에 있어야 한다 — 없으면 `의심 0건`을
 * *"우회배관도 없다"* 로 읽는다.
 */
export const BYPASS_PIPE_NOTE =
  '계측을 우회한 방류는 여기서 잡히지 않습니다 — 유량계를 지나지 않으면 방류 자체가 관측되지 않습니다. 이 판정이 잡는 것은 처리 없이 내보낸 정황이며, 의심 0건이 우회배관까지 없다는 뜻은 아닙니다.';
