/**
 * 브라우저가 부르는 경로. **상류 주소와 자격증명은 서버(`app/api/tb`)만 안다.**
 *
 * 계측 서버는 **http다**(명세 §2). 브라우저가 직접 부르면 https로 배포한 화면이 mixed
 * content로 전부 막히고, 자격증명도 번들에 실린다. 같은 출처의 이 경로를 거치면 둘 다
 * 생기지 않는다.
 *
 * **공인 IP로 포트포워딩되어 사설망 여부는 더 이상 이유가 아니다** — 닿기는 닿는다.
 * 프록시가 필요한 이유는 http와 자격증명, 이 둘로 남는다.
 */
export const TB_PROXY_PATH = '/api/tb';

/**
 * 한 번에 받을 표본 수 상한.
 *
 * 명세 §4.4 — **넘으면 에러가 아니라 200으로 조용히 잘린다.** 24시간을 1분 주기로 받으면
 * 1,440표본이라 여유가 있고, 상한은 관측되지 않았다(`limit=100000`도 200).
 * 그래도 응답 개수가 이 값과 같으면 잘린 것으로 보고 화면에 알린다.
 */
export const TB_TIMESERIES_LIMIT = 1500;

/** 재시도 간격. 폴링 주기(1분) 안에서 끝나야 요청이 겹치지 않는다(명세 §8) */
export const TB_RETRY_BACKOFF_MS = [1_000, 2_000, 4_000] as const;

/** 서버 도달 불가로 판정하기까지 기다리는 시간 */
export const TB_REQUEST_TIMEOUT_MS = 10_000;

/**
 * **첫 화면이 스켈레톤으로 머무는 상한** `[사용자 요청 2026-09-07]`.
 *
 * 첫 응답이 오기 전까지 화면은 «아직 모른다»(스켈레톤)를 그린다 — 내장 데이터를 먼저
 * 그려 두면 답이 아닐 수 있는 값이 답의 자리에 앉는다. 그래서 **얼마나 기다리는가가
 * 곧 사용자가 보는 시간**이 된다.
 *
 * 재시도 예산을 그대로 두면 그 시간이 **최악 94초**다 — `fetchSiteTelemetry`가 요청을
 * 둘 보내고(장비 등록부 · 계측) 각각 4회 시도 × 10초 타임아웃 + 백오프 7초다.
 *
 * 요청별로 깎지 않고 **첫 로드 전체에 마감을 하나** 둔다: 요청이 몇 개든 한 숫자로 묶이고,
 * 넘기면 그 자리에서 내장 데이터로 내려앉는다(`fallback`). 배경 갱신은 예산을 그대로
 * 쓴다 — 화면에 이미 값이 있어 아무도 기다리지 않는다.
 *
 * 2.5초인 근거: 실측 왕복이 로그인 240ms · 계측 조회 200~800ms다(2026-09-07). 정상 경로는
 * 이 마감에 닿지 않고, 닿는다면 그것은 «서버가 느리다»가 아니라 «닿지 않는다»에 가깝다.
 */
export const TB_FIRST_LOAD_DEADLINE_MS = 2_500;
