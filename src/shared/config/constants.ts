/**
 * 플랫폼 국문 정식명 `[원문 p.37·118]`.
 *
 * 사이드바·로그인 두 곳·브라우저 탭 제목·로고 대체 텍스트가 같은 이름을 써야 한다.
 * 문자열로 흩어 두면 한 곳만 고쳐져 화면마다 이름이 갈린다 — 실제로 5곳에 있었다.
 *
 * **줄여 쓰지 않는다**(A2). 사이드바 폭이 좁아 줄이고 싶어지지만 원문 명칭이다.
 */
export const BRAND_NAME = 'AI 기반 지능형 배출관리 플랫폼';

/**
 * 누적 배출량(m³)의 표시 자릿수.
 *
 * 유량이 `m³/day` 정수라 한 표본의 몫은 소수점 아래에서 쌓인다 — 정수로 반올림하면 이른
 * 아침에 `0 m³`이 한참 이어져 **안 내보낸 것으로 읽힌다.** 한 자리면 첫 표본부터 값이 움직인다.
 *
 * 계측 항목이 아니라 **파생값**이라 `PROVISIONAL_DECIMALS`가 아니라 여기 둔다 — 그쪽은
 * 항목별 센서 정확도에서 온 값이고 이것은 표시 규칙이다.
 *
 * **두 화면이 같은 값을 적는다** — `금일 배출 현황`(`SCR-OP-011`)의 타일·표와 `현황판`
 * (`SCR-AD-006`)의 타일이다. 화면마다 반올림이 다르면 같은 물의 양이 두 숫자로 보인다(**E1**).
 * `discharge-view/config/constants.ts`에 있던 것을 소비처가 둘이 되며 올렸다 —
 * `code-organization.rule.md` §3(*"동일 값이 2개 이상 slice에서 쓰이면 `shared/config`로
 * 올린다"*). 위젯끼리는 서로 import 할 수 없어(FSD §8) 올리지 않으면 복제뿐이다.
 */
export const VOLUME_DECIMALS = 1;

/**
 * 누적 배출량의 단위 — **`m³/day`가 아니다.**
 *
 * 계측 항목 `flow`는 «유량»이라 `m³/day`인데, 그것을 시간으로 적분한 이 값은 **부피**라
 * `m³`다. `MEASUREMENT_ITEMS`에서 끌어 쓰면 화면에 `m³/day`가 찍혀 **하루치를 하루마다
 * 내보내는 양**으로 읽힌다.
 *
 * 한글 병기는 `[회의 피드백 2026-08-24: 단위들도 다 한글까지 병기]`를 따른다.
 */
export const VOLUME_UNIT = 'm³';
export const VOLUME_UNIT_KO = '세제곱미터';

