import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* 기본 위치(bottom-left)가 사이드바 하단의 시연 계정 표기를 가린다.
     심사는 dev 서버로 볼 수도 있으므로 겹치지 않는 쪽으로 옮긴다. */
  devIndicators: { position: 'bottom-right' },

  /**
   * **같은 망의 다른 기기(휴대폰·태블릿)에서 dev 서버를 열 수 있게 한다**
   * `[사용자 지적 2026-09-17: 192.168.50.7 에서는 로그인 영역이 렌더링 되지 않는다]`.
   *
   * Next는 dev 자산(`/_next/*`)에 대한 **교차 오리진 요청을 기본으로 막는다.** 서버를
   * `localhost`로 띄우고 브라우저를 LAN IP로 열면 오리진이 달라 청크 전부가 **403**이 된다
   * (실측: `192.168.50.7:3000`에서 `/_next/static/chunks/*.js` 403 · HMR 웹소켓 핸드셰이크 실패).
   *
   * **증상이 «화면이 안 뜬다»가 아니라 «로그인 영역만 비어 있다»로 나타난다.** 서버 HTML은
   * 그대로 오므로 배경·브랜드는 보이는데, 진입 연출이 `opacity: 0`을 **서버 마크업에 박아 두고**
   * 하이드레이션 뒤에 1로 올리기 때문에 — JS가 403이면 그 1이 영영 오지 않아 **흰 카드만 남는다.**
   * 콘솔을 열지 않으면 원인이 보이지 않는 종류다.
   *
   * **주소를 박지 않고 사설 대역으로 적는다.** 이 저장소는 주소가 바뀌는 데 여러 번 데었고
   * (계측 서버 주소를 `.env.local`로 뺀 이유가 그것이다), LAN IP는 DHCP로 갈린다. 대역 표기는
   * 기기를 다시 붙일 때마다 고칠 일이 없다 — 패턴은 `.`로 끊어 칸마다 맞추므로 `*`가 한 칸이다.
   * **`172.16~31`은 칸 하나로 못 적어** 빠져 있다(그 대역을 쓰면 한 줄 더한다).
   *
   * **dev에서만 듣는다.** `next start`는 이 검사를 하지 않는다(실측: 프로덕션은 같은 LAN IP에서
   * 청크 200). 그래서 이 설정이 배포본의 안전성을 바꾸지 않는다 — 여는 것은 **내 기기의 dev
   * 자산을 같은 망에서 가져갈 수 있는 범위**이고, 시연을 같은 망에서 하는 것이 이 값의 용도다.
   * `*`로 전부 열지 않는 이유도 같다.
   */
  allowedDevOrigins: ['192.168.*.*', '10.*.*.*'],
};

export default nextConfig;
