# 화면 설명 대사

옆에 앉은 사람에게 화면을 보여주며 말한다는 가정으로 쓴 대사다. 순서는 시연 동선을 따른다.

> **2026-08-25 전면 대조.** 디자이너 작업(`49593cf`)과 2026-08-20 회의 판정이 화면을 크게 바꿔 대본이 **없는 화면 둘을 안내하고 있었다** — `수분석 검증 /validation`은 라우트가 없고, `설비 예지보전`은 고장 확률·잔여 수명이 화면에서 내려갔다. 절마다 코드와 대조해 다시 썼다. **확인하지 못한 것은 적지 않았다.**
>
>
> **2026-09-11.** **`현황판 /wallboard` 절을 신설했다** `[사용자 요청 2026-09-10]` — 사업장 사무실 벽 TV에 띄워 두는 화면이라 대사도 «앉아서 보는 화면이 아니다»에서 시작한다. 막대를 읽는 법(pH만 초록인 이유)과 **새 데이터가 하나도 없다는 사실**을 대본이 먼저 말한다 — 청중이 «이건 어디서 온 값이냐»를 묻는 자리다.
>
> **2026-09-10 (6).** **용어 셋을 바꿨다** `[사용자 요청 2026-09-10: 처리 미흡 의심 → 처리 상태 확인 · 들어온 물 → 유입수 · 나간 물 → 유출수]`. 대본의 낱말이 화면과 어긋나면 시연 중에 청중이 다른 말을 듣는다. **`처리 미흡 의심` → `처리 상태 확인`은 알람 조건 라벨이기도 해서** `알람 이력` 절의 조건 이름도 함께 바뀐다. 위 변경 이력 안의 옛 낱말은 **그때 사용자가 실제로 쓴 말이라 고치지 않았다.**
>
> **2026-09-10 (5).** `유입·유출 비교` 절의 카드 대사를 **«위에서 아래»에서 «왼쪽에서 오른쪽»으로** 다시 적었다 `[사용자 요청 2026-09-10: 유입값과 유출값의 비교가 가장 먼저 인식되도록 · 8개 카드를 빠르게 비교할 수 있도록]` — 두 값이 한 기준선에서 마주 서게 되면서, 여덟 장이 **표의 열처럼 훑힌다**는 대사를 넣었다. 반원은 «오른쪽에서 받치는 그림»이 아니라 **«맨 아래, 마지막에 보는 그림»** 이다.
>
> **2026-09-10 (4).** `유입·유출 비교` 절에서 **읽는 순서를 다시 적었다** `[사용자 지적 2026-09-10: 상단 상태와 8개 센서 데이터의 연결이 약하다 · 카드에서 가장 중요한 정보는 게이지가 아니라 «들어온 물»·«나간 물»·«변화»다]`. 대사 둘이 바뀐다 — ① 맨 위가 **«한 줄»이 아니라 «한 상자»** 다(`91 위험`과 `처리 미흡 의심`이 같은 상자에 들어갔고, 왜 그렇게 했는지를 대본이 말한다) ② 카드는 **숫자 셋을 위에서 아래로** 읽고 반원은 곁에서 받친다 — 한때 «반원에 호가 셋 있습니다»로 그림부터 설명했는데 화면의 순서와 거꾸로였다.
>
> **2026-09-10 (3).** `유입·유출 비교` 절의 **처리 판정을 여덟 줄에서 반원 게이지 카드 여덟 장으로** 바꿨다 `[사용자 지적 2026-09-10: 너무 정적이며 촌스러움]`. 대사에서 «막대가 길면 처리가 된 것»을 걷고 **«맨 바깥 호만 보면 된다»** 로 다시 썼다 — 호가 셋이라 무엇을 봐야 하는지를 대본이 먼저 말해 줘야 한다. 정상 사업장과 정체 사업장을 나란히 보여 주는 대사도 넣었다(그 대비가 이 화면의 전부다).
>
> **2026-09-10 (2).** `유입·유출 비교` 절의 **대조축을 유량에서 수질로 바꿨다** `[사용자 지적 2026-09-10]` — 대본이 *"왼쪽이 들어온 물, 오른쪽이 나간 물, 가운데가 그 차이"* 로 유량 대조를 안내하고 있었는데, 그것은 «유입 수질 채널이 없다»는 데이터 사정 위의 화면이었다. 시연 동선의 **사업장 첫 화면도 `사업장 상세`로 되돌렸다**(그 화면이 4번째가 됐다). `시연값` 배지·처리 미흡 알람 대사를 새로 넣었다.
>
> **2026-09-10.** `물의 단면` 절을 **`유입·유출 비교`로 교체했다** — 그 화면이 값 중심 보드로 다시 세워졌다 `[사용자 요청 2026-09-10]`. 스크롤 카메라·단면 그림·「모르는 것」 대사가 전부 사라졌다.
>
> **2026-09-09.** `물의 단면` 절을 신설하고 **시연 동선의 사업장 첫 화면을 바꿨다** `[사용자 결정 2026-09-09]` — 그 자리는 `사업장 상세`였고 대본이 *"사업장이 처음 만나는 화면입니다"*라 적고 있었다. 같은 절의 **네 줄 띠 대사 셋도 걷었다** — 그 띠는 2026-09-08에 «이상 점수 한 장»으로 다시 세워져(`SCR-AD-003` v2.0.0) 가동·방류·수신 줄이 없어졌는데 대본만 남아 있었다. **어떤 검사도 이 파일을 세지 않아 조용히 낡는다.**
>
> **2026-09-08.** `설비 이상 탐지` 절에서 격자의 `방지시설 가동` 줄 대사를 고쳤다 — 그 줄이 화면에서 없어졌다 `[사용자 요청 2026-09-08]`. 없는 것을 안내하지 않으면서 **왜 없는지**는 남긴다(청중이 이상 탐지에서 그 판정을 이미 봤기 때문에 물을 수 있다).

---

## 로그인

> 시연은 여기서 시작합니다. 배경은 방류구 영상이고, 오른쪽 흐린 유리 기둥이 로그인 칸이에요. 유리를 쓰는 건 이 화면뿐입니다 — 뒤에 있는 게 값이 아니라 영상이라 비쳐도 읽을 게 없거든요. 대시보드 모달은 뒤 숫자가 비치면 어느 값이 그 모달 건지 알 수 없어서 전부 불투명입니다.
> 계정은 미리 채워 뒀고 `아이디 저장`도 됩니다. 서버가 없어서 입력값을 검증하지는 않아요. 실패 화면도 일부러 안 만들었습니다. 없는 인증이 있는 것처럼 보이니까요.
> 역할은 여기서 고르지 않습니다. 들어간 다음 오른쪽 위 사람 아이콘에서 바꿔요. 기본은 **시스템 관리자**입니다.

---

# 시스템 관리자 — "어디가 문제인가"

전 지역 사업장을 한꺼번에 보는 사람입니다. 본사나 환경관리 대행업체를 생각하시면 됩니다.

## 통합 관제 `/`

> 처음 만나는 화면입니다. 맨 위가 사업장 열 곳이고, 한 줄로 세워 옆으로 넘깁니다. 두 줄로 쌓았더니 카드 하나하나가 아니라 덩어리로 보여서요.
> 카드마다 이상 점수와 등급이 붙습니다. 카드를 누르면 그 사업장의 미확인 알람이 뜨고요.
> 아래가 **선택한 한 곳**입니다. 탭으로 사업장을 고르고, 스크롤하면 그 탭 줄이 헤더 밑에 붙어요 — 격자가 길어서 지금 어느 사업장을 보는지가 화면 밖으로 나가더라고요.
> 왼쪽 지도에 실증 10개소가 찍혀 있습니다. **면 색은 그 시도에 사업장이 몇 곳인지**고, 상태는 **핀**이 말합니다. 예전에는 시도를 가장 나쁜 등급 색으로 칠했는데, 도형이 크면 색 면적도 커져서 경기도의 주의 한 건이 서울의 위험보다 넓게 칠해졌어요. 면적이 심각도로 읽힌 겁니다.
> 오른쪽 첫 카드가 이상 탐지 결과고, 지금 이 사업장 점수가 88, 위험입니다.
> 이 점수는 AutoEncoder가 냅니다. 정상일 때의 패턴을 미리 학습해 두고, 지금 들어온 값을 복원했을 때 얼마나 어긋나는지 — 그 **복원 오차**를 0에서 100으로 환산한 값이에요. 임계값을 넘으면 이상이라고 하는 방식이 아니라, 정상에서 얼마나 벗어났는지를 보는 겁니다. 사업계획서 64쪽 방식 그대로입니다.
> 옆에 붙은 뱃지 셋이 산출 근거예요 — 모델, 대상 기간, 산출 시각. 그 아래 처리율·가동률은 이 판정이 **믿을 만한 상태에서 나왔는지**를 말합니다.
> 다만 등급을 나누는 경계 — 50, 70, 80 — 이건 원문에 없습니다. 저희가 잠정으로 정한 값이고, 실제 점수 분포가 나오면 오탐지율 기준으로 다시 잡습니다. 지금은 한 파일에만 넣어 둬서 확정되면 그것만 바꾸면 전 화면이 따라옵니다.
> 데이터는 전부 시연용입니다. 사업장마다 성격을 미리 정해 두고 24시간치를 만들어요. 무작위는 아니라서 새로고침해도 같은 값이 나오고, 점수가 치솟는 구간에는 TOC와 탁도도 같이 올라갑니다. 따로 만들면 화면이 서로 다른 말을 하니까요.
> 이 사업장은 통신이 끊겨 있어서 점수 자리가 비어 있습니다. 0으로 채우지 않습니다 — 0으로 그리면 배출이 뚝 떨어진 것처럼 읽히거든요.
> 카드마다 오른쪽 위에 `상세 보기`가 있습니다. 여기는 요약이라 잘려 보이는 값이 있고, 그 전부를 보는 화면이 따로 있어요.
> 그리고 `선택 사업장 현황` 제목 옆에도 하나 있습니다. 이건 주제별로 흩어 보내는 게 아니라 **고른 사업장 하나를 통째로** 보는 화면으로 갑니다 — 뒤에 나올 `사업장 상세`예요.
> 지도 핀이나 탭, 카드를 누르는 건 **고르는 것**이지 넘어가는 게 아닙니다. 누르는 즉시 넘어가면 지도에서 이곳저곳 짚어 보며 훑는 게 안 되거든요. 넘어가는 건 그 링크 하나뿐입니다.

## 금일 배출 현황 `/discharge`

> 원문이 수집 데이터를 네 갈래로 나눠 놨습니다. 수질, 배출, 설비 운영, 운영 패턴. 그중 **배출**이 유량·수위·방류 여부 셋이고, 쓰는 목적을 *"배출량 산정"* 이라고 못박아 뒀어요.
> 그런데 저희 화면에서 그 셋이 흩어져 있었습니다 — 유량은 계측 격자 한 칸, 방류 여부는 일간 리본 한 줄, 수위는 아예 없었고요. 이 화면이 그걸 한 자리에 모은 겁니다.
> 위 세 칸이 정확히 그 묶음입니다. 지금 내보내고 있나, 얼마나, 수조는 얼마나 찼나.
> **한때 네 칸이었습니다** — 넷째가 «금일 누적 배출량»이었는데 회의에서 필요 없다고 정리됐어요. 빼고 나니 남은 셋이 원문이 규정한 세 가지와 딱 겹칩니다.
> 아래 그래프에서 옅은 띠가 방류를 멈춘 구간이고 그때 유량이 0으로 떨어집니다. 진한 띠는 통신이 끊긴 구간이고 선이 아예 끊겨요. **멈춘 것과 못 본 것은 다릅니다.**
> 그 아래 수위 그래프를 보시면 방류를 멈추는 동안 차오릅니다. 멈춰도 폐수는 계속 들어오니까요.
> **두 그래프가 같은 폭, 같은 시간축입니다.** 옅은 띠가 위아래 같은 자리에 서죠 — 누적 그래프가 빠지면서 수위가 반폭에서 전폭으로 올라온 덕입니다. 전에는 시간축이 어긋나 있어서 이게 눈으로 안 이어졌어요.
> 수위는 단위가 원문에 없습니다. 항목은 있는데 미터인지 퍼센트인지, 수조가 몇 미터인지가 없어요. 그래서 시연은 미터에 만수위 3m로 뒀고 칸에 `[PROVISIONAL]`이라 적어 뒀습니다. 정해지면 그 두 값만 바꿉니다.
> 시흥 도금 B라인을 골라 보시면 하루 종일 방류가 없는 날입니다. 유량 선이 **바닥에 붙어 있고** «수신 없음»이 아니에요 — 값을 받았고 그 값이 0인 겁니다. 안동 식품 B동은 통신이 끊겨서 세 칸이 다 «수신 없음»이고 그래프 자리에 글이 뜹니다. 그 둘을 구분하는 게 이 화면의 핵심입니다.

---

## 이상 탐지 `/anomaly`

> 맨 위가 전 사업장 이상 점수 표입니다. 통합 관제는 카드로 훑는 곳이고 여기는 **줄로 비교하는** 곳이에요 — 순위가 열로 서고, 점수가 자릿수를 맞춰 정렬되고, 추세선이 같은 폭 칸에 놓입니다. 카드 격자로는 그 셋이 다 흩어졌습니다.
> 줄을 누르면 아래 상세로 데려갑니다. 표만 바뀌면 뭐가 일어났는지 화면에 안 보이거든요 — 바뀐 곳이 스크롤 아래에 있어서요.
> 아래 왼쪽이 **왜 그 점수인지**입니다. TOC가 34%, DO가 27%… 이렇게 어떤 항목이 점수를 끌어올렸는지 막대로 보여줍니다.
> 오른쪽이 점수를 하루치로 펼친 것이고, 배경의 네 구간이 아까 그 등급, 끊긴 자리가 수신이 안 된 구간입니다.
> 맨 아래는 점수와 **다른 축**의 탐지입니다. 방류는 일어나는데 방지시설 전류가 0인 구간 — 처리 없이 내보낸 정황이죠. 원문이 준 방법 그대로 전류와 유량 발생 시기를 맞춰 본 겁니다.
> 다만 `무단방류`라고는 쓰지 않습니다. 무단인지는 신고된 방류 시간대가 있어야 정해지고, 원문도 "판단 가능성 검토"까지만 말합니다. 그리고 배관을 우회해 버리면 유량계를 지나지 않으니 저희는 못 잡습니다. 못 잡는 것을 화면이 밝히는 게 맞다고 봤습니다.

## 시계열 변화 `/timeseries`

> 계측값을 항목별로 나눠 봅니다. pH는 0에서 14, EC는 2만까지 가니까 한 축에 겹치면 둘 다 못 읽어요.
> 위에서 수질 8종·설비 3종·전체를 고르고, 기간은 6·12·24시간입니다. 5분 주기로 24시간이면 288개 표본이에요.
> 아래 표가 항목별 최소·평균·최대·최신입니다. 단위를 기호와 한글로 같이 적어요 — `NTU`, `Pt-Co`만 봐서는 무엇의 단위인지 알 수가 없어서요.
> 결측은 평균에서 빼되 몇 건인지는 따로 셉니다. 빼기만 하면 얼마나 비었는지 모르고, 0으로 채우면 값 자체가 거짓이 되니까요.
> 최신값은 마지막 표본 그대로입니다. 값이 없으면 앞 값을 끌어오지 않아요. 그러면 몇 시간 전 값이 현재값 행세를 합니다.
> 기준선은 pH에만 있습니다. 5.8에서 8.6. 나머지는 지역구분하고 배출량 규모를 알아야 기준표를 고를 수 있는데 그게 아직 미확인이라 `미확정`으로 둡니다. 법정 기준값은 지어내면 그냥 틀린 값이 됩니다.

## 오염도 추정 `/prediction`

> TOC·TN·TP를 봅니다. **앞으로 6시간을 그리지는 않습니다** — 회의에서 정리된 게 있어서요.
> 핵심은 **TN과 TP에는 센서가 없다**는 겁니다. 값싼 센서로 재는 항목들에서 AI가 추정해 내는 거예요. 고가 분석기를 대체하자는 게 이 과제의 주장이고, 이 화면이 그 자리입니다.
> 그래서 카드는 **농도를 적지 않습니다.** 기준보다 높은지 낮은지만 냅니다 — 소프트 센싱으로는 절대값 정확도를 맞추기 어렵다는 판단이라서요. 숫자를 크게 띄우면 그 값이 계측된 농도로 읽힙니다.
> 기준치는 사업장마다 다릅니다. 지역과 배출량 규모로 갈리고, 허가증에 적힌 값이에요. 그래서 화면은 그걸 비추기만 하고 우리가 채우지 않습니다.
> 오른쪽 R²가 정확도입니다. TN 0.886, TP 0.782 — 원문에 있는 값입니다. TOC는 `원문 미규정`이라고 적혀 있죠. 원문이 TOC의 R²를 안 줘서 비워 둔 겁니다. 유량은 아예 `—`인데, 이쪽은 이유가 달라요. 성능 목표 자체가 없습니다.
> **6시간 예측을 그리지 않는 이유도 화면에 적어 뒀습니다.** 회의에서 TN·TP 예측을 내렸고, 대신 무엇을 예측할지가 아직 정해지지 않았어요. 없는 데이터로 곡선을 그리면 산출된 예측처럼 읽힙니다.

## 설비 이상 탐지 `/equipment`

> **예지보전이 아니라 이상 탐지입니다.** 고장 확률하고 잔여 수명을 내는 건 어렵다는 게 회의 결론이었어요. 현실적으로 가능한 건 진동 센서로 이상을 탐지해 알리는 것과 가동 상태 확인입니다. 원문 성과지표에는 예지보전이 있는데, 화면에서 내렸다는 사실과 이유를 문서에 남겨 뒀습니다.
> 설비마다 카드가 하나입니다 — 이름과 등급, 가동 상태, 이상 신호.
> 아래 격자가 설비 × 24시간입니다. 한 시간 안에 결측이 하나라도 있으면 그 칸은 통째로 모름입니다. 아는 표본으로 등급을 매기면 끊긴 시간이 화면에서 사라져요.
> 격자의 **행은 설비뿐입니다.** 여기에 방지시설 가동 줄을 한 줄 더 두었던 적이 있는데 걷어냈어요 — 방지시설이 멈췄다는 사실은 방류 여부와 나란히 놔야 뜻이 생기고, 그 자리는 아까 보신 **이상 탐지의 방류 의심**입니다. 이 화면에는 함께 볼 축이 없어서 몇 시에 멈췄나까지만 말하고 끝났습니다.
> 설비명을 누르면 상태 등급·가동·이상 신호·누적 운전시간이 뜹니다. **고장 확률하고 잔여 수명은 표시하지 않는다고 모달 안에 적어 뒀어요.**
> 한 가지 짚어 두면, 설비 네 대는 시연값입니다. 원문은 펌프·폭기장치·약품주입펌프라는 종류만 정하고 대수는 어디에도 없습니다.

## 운영 최적화 `/optimization`

> 다른 화면이 전부 "지금 어떤가"라면 이 화면만 "그래서 뭘 할까"를 답합니다.
> 위에 타일이 다섯입니다. 마지막이 **적용 시 에너지 효율**인데, 이 화면에서 유일하게 실제로 계산하는 값이에요 — 평균 전력 곱하기 24 나누기 평균 유량. 계산할 수 있는 걸 만들어 넣으면 그만큼 검증할 수가 없어지니까요.
> 아래 왼쪽이 약품 주입량입니다. 지금 이만큼 넣고 있는데 이만큼으로 줄일 수 있다, 절감률 20~30%. 옆에 왜 그렇게 판단했는지 근거를 답니다.
> 오른쪽이 설비 운전 조건이고, 퍼센트로만 냅니다. 폭기량 몇 m³/min 하는 절대 단위가 원문에 없어서, 지어내는 대신 지금 대비 얼마나 올리고 내리는지만 말합니다.
> 권장값을 실제로 설비에 적용하는 기능은 없습니다. 제어 대상하고 안전 인터록이 원문에 없어서 만들지 않았습니다.

## 알람 이력 `/alarms`

> 발생한 알람을 **날짜로 묶어** 봅니다. 열여섯 건이 한 덩어리로 이어지면 "언제 일어난 일인가"를 줄마다 다시 읽어야 해서요. 이력의 첫 질문은 시점이거든요. 날짜 머리는 스크롤해도 위에 붙어 있습니다.
> 한 줄은 제목이 먼저고, 그 아래 상세, 맨 아래 메타입니다. 예전에는 뱃지 줄이 맨 위였는데 칩 색이 먼저 들어오고 정작 무슨 알람인지는 나중에 읽혔어요.
> 메타는 두 갈래로 갈랐습니다. 등급·조건·우선순위는 뱃지고, 사업장·시각은 맨 글자예요. 색이 뜻을 가지는 쪽이 앞에 섭니다.
> 우선순위는 등급에서 자동으로 나옵니다. 등급이 4단계고 우선순위가 3단계인데 둘을 잇는 규칙이 원문에 없어요. 발표자료 그림에 `경고→높음`, `주의→중간` 두 쌍만 있어서 거기 맞추고, 나머지는 최상위끼리 합쳤습니다. 긴급을 주의로 낮추면 대응이 늦어지니까요.
> 제목을 누르면 그 시각의 계측값이 그대로 뜹니다. 그때 방류 중이었는지, 방지시설이 돌고 있었는지까지요.
> 확인·조치 버튼은 눌러 보실 수 있는데, 저장은 안 됩니다. 서버가 없어서 새로고침하면 돌아가요. 화면에도 그렇게 적어 뒀고 되돌리기 버튼을 뒀습니다.

## 리포트 `/reports`

> 사업장별 집계를 한 표로 모으고 CSV로 내보냅니다.
> 이상 점수 최신·최대·평균, 결측 건수, 방류 시간, 알람 건수, 처리율, 가동률. 전부 앞 화면에 이미 있는 값입니다. 원문이 리포트 항목이나 양식을 정하지 않아서 새 지표를 만들지 않았어요.
> 여기 방류 시간에 상태가 셋입니다. `0시간`, `배출 없음`, `모름`. 통신이 끊긴 곳에 0시간이라고 쓰면 "방류가 없었다"는 사실 주장이 됩니다.
> 그 아래가 구간별 집계고, 시계열 변화 화면과 **같은 표를 씁니다**. 각자 만들면 같은 사업장의 같은 구간이 두 화면에서 다른 값으로 보여요 — 결측 세는 규칙 하나만 갈려도 그렇게 됩니다.
> 다음이 센서 값 기간 통계입니다. 회의에서 요구된 거예요 — 이상 점수만이 아니라 센싱된 데이터의 기간 통계가 필요하다고요.
> 마지막이 오염도 판정이고, 맨 아래에 **이 리포트가 정하지 않은 것**을 따로 적어 뒀습니다.
> 집계는 결측을 빼고 냅니다. 결측을 0으로 세면 평균 점수가 낮아져서 상태가 실제보다 좋아 보여요. 리포트에서 제일 위험한 거짓말입니다.
> 일간·주간·월간은 없습니다. 시연 데이터가 24시간뿐이라 만들면 없는 기간을 지어내는 게 됩니다. 배출량 부피 집계도 없어요 — 데이터셋이 방류 시간만 주고 부피를 안 줍니다.

---

# 사업장 — "얼마 아꼈나"

여기서 역할을 사업장으로 바꾸겠습니다. 사업주예요. 자기 사업장 한 곳만 봅니다.

> 보시면 통합 관제하고 사업장 순위가 사라졌죠. 한 곳뿐이라 비교가 성립하지 않아서 닫았습니다. 주소창을 직접 고쳐 남의 사업장을 열어도 자기 사업장으로 되돌아옵니다.
> 다만 이건 인가가 아니라 시연용 표시입니다. 서버가 없어서 브라우저에서 가리는 것뿐이고, 백엔드가 붙으면 서버가 걸러 보냅니다.

## 사업장 상세 `/overview`

> 자기 사업장 하루를 한 장에 담은 화면입니다. **사업장이 처음 만나는 화면이에요.**
> 맨 위가 **일간 이상 점수** 한 장입니다. 오늘 최악이 언제 얼마였는지를 먼저 크게 적고, 그 자리를 곡선에 점으로 짚습니다.
> 등급 경계는 파선으로만 긋고 **위험 구간만** 음영을 넣었습니다. 네 구간을 다 칠했더니 배경이 그래프보다 세게 읽혔어요.
> 빗금 자리가 통신 두절입니다. 결측은 0으로 채우지 않고 끊어서 둡니다 — 0으로 채우면 배출량 급감으로 오독됩니다.
> **한때 같은 시간축에 네 줄이었습니다** — 이상 점수 밑에 설비 가동·방류·수신 띠가 나란히 있었어요. 세 줄이 뜻을 잃어서 걷었습니다: 수신 줄의 빗금이 위 두 줄의 결측 표기와 정의상 같은 자리가 됐고, 두절은 점수 트랙 배경·범례·캡션이 이미 여러 겹으로 말하고 있었습니다 `[사용자 요청 2026-09-08]`.
> 위 타일 가운데는 **약품비 절감률**입니다. 한때 월 절감액이었는데 회의에서 금액을 전부 내렸어요 — 약품 단가가 정해진 게 없어서 금액을 적으면 저희가 지어낸 수가 됩니다.
> 아래 계측 격자는 통합 관제하고 같은 부품입니다. 고르고 넘어온 사람이 보던 걸 잃지 않게요.

> **이 화면은 세 역할이 같이 씁니다.** 시스템 관리자·기초지자체는 목록에서 하나를 골라 `상세 보기`로 들어옵니다. 그래서 이름이 `자사 현황`이 아니라 `사업장 상세`예요 — 남의 사업장을 보는 사람한테 `자사`는 틀린 말이라서요.
> 그 둘한테는 사이드바에 이 항목이 없습니다. 고르지 않고 열면 무엇의 현황인지가 없거든요. 대신 화면 맨 위에 왔던 곳으로 돌아가는 링크를 뒀습니다 — **사업장한테는 안 보입니다.** 여기가 그 사람의 첫 화면이라 자기 자신을 가리키게 되거든요.

## 유입·유출 비교 `/inout`

> 질문 하나로 요약되는 화면입니다 — «처리가 되고 있느냐».
> **맨 위 한 상자가 답입니다.** 위 칸이 이 사업장 등급 — `91 위험`. 아래 칸이 이 화면의 판정 — `처리 상태 확인`. **한 상자에 넣은 게 일부러입니다** — 따로 떼어 놓으니 두 개가 서로 무슨 관계인지 안 보이더라고요. 지금은 같은 상자에 들어 있으니 «둘 다 지금 상태»라는 게 그냥 보입니다. 옆에 근거가 붙습니다 — 「판정 대상 5개 항목 중 5개가 유입과 거의 같습니다」.
> 왜 그게 문제냐면요. 폐수가 들어와서 공정을 거치면 수질이 좋아져야 하는데, 나갈 때도 들어올 때와 똑같으면 **공정이 일을 안 한 겁니다.** 프로브가 유입·유출 두 지점에 같은 걸로 붙거든요. 회의에서 정해 주신 판정 방법 그대로입니다.
> 왼쪽 도넛이 그걸 셉니다 — `0/5`, 판정 대상 다섯 중 처리된 게 없다는 뜻입니다.
> 아래 카드 여덟 장이 근거입니다. 한 장에 항목 하나고, **왼쪽에서 오른쪽으로 읽으시면 됩니다** — `유입수 10.38` → `유출수 8.30`, 그 아래 `▼20% 감소`. 두 숫자가 **같은 높이에 나란히** 있어서 한눈에 견줘집니다.
> 여덟 장 전부 자리가 같습니다. 유입수는 늘 카드 왼쪽 끝, 유출수는 늘 오른쪽 끝이라 **표의 열처럼** 위아래로 훑을 수 있습니다.
> 맨 아래 작은 반원은 **마지막에 보시면 됩니다.** 호가 셋인데 — **안쪽이 유출수, 그 다음이 유입수, 맨 바깥이 걷어낸 만큼**입니다. 한때 이걸 카드 위쪽에 크게 뒀는데 숫자보다 그림이 먼저 보이니까 순서가 거꾸로였습니다. **읽어야 할 건 숫자고 그림은 거드는 쪽**이라, 상태 문구 옆으로 내리면서 크기도 절반 아래로 줄였습니다.
> **맨 바깥 호를 보시면 한눈에 됩니다.** 길면 많이 걷어낸 거고, 없으면 그대로 나간 겁니다. 지금 이 사업장은 여덟 장 전부 그 호가 거의 없죠 — 그게 «처리 상태 확인»입니다.
> 정상인 사업장을 보시면 확 다릅니다. 탁도 85%, 총유기탄소 70%로 바깥 호가 길게 돌아갑니다.
> 호 길이는 **그 카드 안에서만** 뜻이 있습니다. 항목마다 단위가 달라서요 — pH는 0에서 14, 전기전도도는 2만까지 갑니다. 카드끼리 호 길이를 견주면 안 됩니다. 실제 값은 카드 아래 숫자가 갖고 있습니다.
> 부호는 글로 적습니다. 「70% 감소」·「160% 증가」. 색으로 방향을 가르지 않는 이유는, 용존산소는 폭기를 해서 **오히려 올라가는 게 정상**이라서요. 「줄어드는 게 좋다」고 색으로 말해 버리면 그 줄에서 틀린 말이 됩니다.
> 수온·전기전도도·pH 세 카드에는 「판정 대상 아님」이라 적혀 있죠. 수온은 공정이 바꾸려는 값이 아니고, 전기전도도는 응집제 넣으면 오히려 오를 수도 있고, pH는 들어올 때 이미 중성이면 그대로인 게 맞거든요. **값은 다 보이되 판정만 다섯 항목에 겁니다.**
> **범례에 «유입수 · 시연값»이라 적혀 있죠.** 여기 솔직히 말씀드릴 게 있어요 — 계측 서버에 유입 쪽 수질 채널이 아직 없습니다. 그래서 유출 실측에서 역산해 만든 값이고, 그 사실을 범례와 아래 고지 한 줄이 적고, 값 전체 표에서는 칸마다 적습니다. 채널이 들어오면 그 표기만 걷으면 됩니다.
> **유사한 항목이 있으면 알람이 뜹니다.** 헤더의 미확인 건수가 같이 오르고 알람 이력에서도 보입니다 — `처리 상태 확인`이라는 조건으로요.
> 그 아래가 **물의 양**입니다. 유입·유출 유량과 그 차이. 한때 이게 이 화면의 주인공이었는데 내렸습니다 — 들어온 만큼 나가는 건 정상이라, **양은 처리가 됐는지를 말하지 않거든요.** 지금은 「물이 실제로 흐르고 있나」를 받쳐 주는 자리입니다.
> 숫자 밑에 «10:53 KST 관측»이라고 적혀 있죠. 「지금」이라고 안 씁니다 — 계측 서버가 우리 시간축보다 1분쯤 뒤에 쓰기 때문에, 언제 값인지를 밝혀 두는 겁니다.
> **그 아래 두 줄이 또 하나의 축입니다.** 위가 방지시설 가동, 아래가 방류. 두 줄이 어긋난 자리 — 물은 나가는데 시설이 멈춘 구간 — 이 빨갛게 칠해집니다.
> 이게 원문에 있는 방법 그대로입니다. "전류 발생 시기와 유량 발생 시기를 비교"라고 발표자료 13쪽에 적혀 있어요.
> 다만 **무단이라고 단정하지 않습니다.** 신고 정보가 있어야 무단인지 알 수 있고, 저류된 물을 내보내는 중일 수도 있거든요. 화면은 «의심»까지만 적고 자세한 건 이상 탐지 화면으로 넘깁니다.
> 마지막이 T-N·T-P인데 색이 다릅니다. **직접 재지 않고 AI가 내는 값**이라서요. 이 둘만 카드가 없는데, 유입 짝이 없어서 「얼마나 달라졌나」를 낼 수가 없습니다.

## 현황판 `/wallboard`

> 이건 앉아서 보는 화면이 아닙니다. **사무실 벽 TV에 띄워 두는 화면**이에요.
> 주관사에서 주신 요구가 그대로 설계가 됐습니다 — *"모니터에 그냥 띄어놓고 보지, 계속 움직이거나 이동하는 것은 지양한다."* 그래서 **스크롤이 없고, 누를 게 하나도 없고, 2~3m 밖에서 읽힙니다.**
> 보시면 사이드바하고 헤더가 없죠. 이 화면만 그렇습니다 — 1920을 다 쓰려고요. 대신 셸이 하던 일(사업장 이름·수신 원천·기준 시각)을 맨 윗줄이 직접 맡습니다.
> 맨 위 오른쪽이 이상 점수 91, 위험입니다. 그 아래 작은 글씨가 어떤 모델이 낸 값인지고요. **이 화면에서 점수는 여기 한 번만 나옵니다** — 처음 만들 때는 아래 타일에도 한 번 더 넣었는데, 벽에서 같은 숫자가 두 번 보이면 «저게 다른 값인가» 하고 먼저 확인하게 되더라고요.
> 왼쪽이 이상 점수와 그 근거, 오른쪽이 알람과 설비입니다. 미확인 알람 6건, 지금 방류 중.
> **방류 상태는 세 가지입니다** — 방류 중, 방류 없음, 그리고 «판정 불가». 수신이 없을 때 «방류 없음»이라고 쓰면 안 내보낸 걸로 읽히니까 셋을 갈라 놨습니다.
> 아래가 수질 8종이고 이게 이 화면의 주인공입니다. 항목마다 지금 값이 크게 있고, 그 아래 막대가 **그 값이 어디쯤인지**를 보여줍니다.
> 막대를 읽는 법이 항목마다 다릅니다. **pH만 초록이죠** — 배출허용기준 5.8에서 8.6이 있어서 그 안에 있다는 뜻입니다. 나머지는 회색인데, 그건 «정상»이 아니라 **판정할 기준이 아직 없다**는 뜻이에요. 그 항목들은 트랙이 «오늘 관측 범위»로 바뀌고 밑에 `기준값 미확정`이라 적힙니다.
> 없는 기준으로 초록을 칠하지 않는 게 중요합니다. 그러면 없는 «정상» 판정을 화면이 만들어 내니까요.
> 맨 아래 줄이 **배출 축**입니다. 왼쪽에 유출 유량이 하루를 어떻게 지나왔는지가 넓게 눕고, 곁에 지금 값 셋 — 유량, 수조 수위, 방류 상태. 원문이 «배출 데이터»라고 묶어 둔 셋 그대로예요.
> 추이에는 **축이 있습니다.** 벽에는 마우스가 없어서 툴팁으로 값을 보여 줄 수가 없거든요 — 세로는 0부터 그날 최댓값까지, 가로는 네 시간마다 정시고 맨 오른쪽만 «지금»입니다. **바닥이 0인 게 중요합니다** — 방류를 멈추면 선이 바닥에 닿아요. 처음엔 그 구간의 최솟값이 바닥이어서, 400~600을 오가는 날도 0까지 떨어진 것처럼 보였습니다.
> **한때 여기가 누적 배출량이었습니다.** 회의에서 필요 없다고 정리돼서 `/discharge`와 함께 걷었어요 — 필요 없다고 한 값이 벽에 52px로 남아 있으면 안 되니까요.
> 설비 네 대는 오른쪽 아래입니다. 한 대당 한 줄이에요 — 벽에서 필요한 건 «네 대가 지금 어떤가» 한 눈이라서요.
> **움직이는 게 거의 없습니다.** 값은 1분마다 갱신되는데, 바뀐 칸만 잠깐 밝아졌다 사라져요. 티커나 자동으로 넘어가는 패널은 일부러 안 넣었습니다 — 종일 켜 두는 화면에서 계속 움직이는 건 눈에 남거든요. 시계도 초를 안 씁니다.
> **나가는 건 오른쪽 아래입니다.** 평소엔 안 보여요 — 마우스를 움직이거나 아무 키나 누르면 «현황판 나가기»가 뜨고, 손을 떼면 4초 뒤에 사라집니다. `Esc`로도 나갑니다. 벽에 걸어 두면 아무도 안 만지니까 영영 안 뜨고, 기계 앞에 선 사람한테만 보이는 식이에요.
> **마우스 포인터도 같이 사라집니다.** 버튼만 없애고 화살표가 벽에 남아 있으면 «누를 게 있나» 싶어지니까요. 이 화면에서만 그렇고, 나가면 바로 돌아옵니다.
> 마지막으로, **이 화면에 새로 만든 데이터는 하나도 없습니다.** 전부 다른 화면이 이미 보여주는 값이에요. 달라진 건 크기와 밀도뿐입니다 — 서비스가 확정되기 전이라, 지금은 «우리 톤앤매너로 벽에 걸면 이렇게 된다»를 보시는 화면입니다.

## 비용 절감 현황 `/cost-savings`

> 사업장이 돈을 내고 이걸 쓸 이유를 숫자로 보이는 화면입니다. 시스템 관리자가 "어디가 문제냐"를 묻는다면 사업주는 "얼마 아꼈냐"를 묻거든요.
> 약품비 20~30%, 전력비 10%, 합쳐서 운영비 12% 목표. 전부 원문에 있는 수치입니다.
> 총 절감률은 두 비율을 평균 내지 않았습니다. 약품비가 전력비의 두 배라서 평균을 쓰면 전력 절감이 실제보다 크게 반영돼요. 금액으로 가중합니다.
> 아래가 조기 탐지로 막은 사고입니다. 건수와 금액을 일부러 나눠 적었어요. 곱해서 하나로 내면 확정된 절감액처럼 읽히거든요. 게다가 원문이 건당 1,500만 원과 2,500만 원 두 값을 줘서, 하나를 고르지 않고 범위로 둡니다.
> 전제를 하나 말씀드리면 — 약품 단가하고 계약 전력 단가가 원문에 없습니다. 그래서 여기 금액은 전부 원문 예시값이고, 이 사업장의 실제 비용이 아니라는 문구를 화면에 계속 띄워 둡니다. 단가만 들어오면 실금액으로 바뀝니다.
> 월별 추이는 비워 뒀습니다. 계측이 24시간뿐이라 그리려면 없는 기간을 지어내야 합니다.

## 수처리 공정 `/process`

> 이 화면은 원문 요구사항에 없습니다. 필요해서 만들었어요.
> 이 과제의 핵심 주장이 TMS 대체입니다. 2~3억 들 것을 5,000만 원으로 한다는 건데, 그게 어떻게 가능한지가 어느 화면에도 그림으로 없었습니다.
> 표준 6단계를 놓고 색으로 나눴습니다. 실측하는 곳, AI가 추정하는 곳, 아예 안 재는 곳. 실제로 재는 건 두 점입니다 — 앞에 유입펌프 전류, 끝에 방류구 프로브. 나머지는 AI가 채웁니다.
> 가운데가 비어 보이는 건 저희 한계가 아닙니다. 생물반응조 쪽은 원래 계측 밀도가 높고 전처리는 원래 안 재요. 이 화면이 말하는 건 "우리는 못 본다"가 아니라 "원래 안 재는 구간이 대부분이고, 법이 요구하는 한 점이 관건이다"입니다.
> 메뉴에는 사업장에만 보입니다. 시스템 관리자는 이상 탐지나 알람에서 "이게 어느 단계 문제냐" 할 때 링크로 들어옵니다.

## 사업장 설정 `/settings`

> 마지막이 설정입니다. 여기서 **지역별 방류 기준치**를 넣어요.
> 배출허용기준은 저희가 채우지 않습니다. 법령이 원천이고 우리가 정하면 그냥 틀린 값이 되니까요. 대신 허가증에 적힌 값을 넣는 자리를 뒀고, 넣는 순간 오염도 추정과 시계열 화면의 `미확정`이 판정으로 바뀝니다.

---

# 마무리에 한 번 짚을 것

> 화면에 `미확정`, `원문 미규정`이라고 적힌 자리가 여럿 보이셨을 겁니다. 빠뜨린 게 아니라 일부러 남긴 겁니다.
> 등급 경계, 배출허용기준, 6시간 예측 대상, 단가 — 원문이 정하지 않았거나 회의에서 다시 잡기로 한 것들이에요. 그럴듯한 값으로 채우면 화면이 없는 판정을 만들고, 그 순간 나머지 숫자까지 같이 의심받습니다.
> 대신 그 값들은 전부 한곳에 모아 뒀습니다. 확정되는 대로 파일 하나만 바꾸면 전 화면이 따라옵니다.
