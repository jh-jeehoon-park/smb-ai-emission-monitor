# 화면 설계 — 로그인 (SCR-CO-001)

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 문서명 | 화면 설계 — 로그인 (SCR-CO-001) |
| 버전 | v1.1.0 |
| 작성일 | 2026-08-13 |
| 기반 문서 | /docs/specs/README.md, /docs/specs/screens.md, /docs/specs/requirements.md, /docs/analysis/system-architecture.md, /design-system/smb-ai-emission-monitor/MASTER.md |

### 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.0.0 | 2026-08-13 | Claude | 신규 작성 — 회의로 역할 3종의 성격이 갈리면서 역할을 바꿔 볼 수단이 필요해졌다. 로그인 화면과 역할 전환 장치를 분리해 설계 |
| v1.1.0 | 2026-08-13 | Claude | 화면 구성을 좌우 2열로 개편(§3) — 좌측 브랜드 패널(눈금 격자·장식 파형·근거 있는 지표 4칸) 신설. §3.1에 "색 대신 면적·명도로 대비를 만든다" 추가, §3.2 표시 숫자의 근거표·§3.3 파형이 데이터가 아님을 명시. 본문 폰트가 IBM Plex Mono → Pretendard로 바뀐 것을 §7.6에 기록 |

---

## 2. 화면 개요

| 항목 | 값 |
|------|-----|
| 화면 ID | SCR-CO-001 |
| 메뉴(1Depth) | 로그인 |
| 메뉴(2Depth) | — |
| 화면 구분 | 입력폼 |
| 경로 | `/login` |
| 구현 | [src/app/login/page.tsx](../../../src/app/login/page.tsx) · [login-view.tsx](../../../src/widgets/login-view/ui/login-view.tsx) · [brand-panel.tsx](../../../src/widgets/login-view/ui/brand-panel.tsx) |
| 관련 요구사항 | REQ-CO-012(사용자 인증·권한 관리) |

### 2.1 권한

| 관리자 | 운영자 | 게스트 |
|---|---|---|
| R | R | R |

**역할 이전의 화면이라 역할로 가릴 수 없다** `[설계]`. 다른 화면과 방향이 반대다 — 여기서 막을 대상은 역할이 아니라 **이미 로그인한 사용자**이고, 그것은 §5의 리다이렉트가 맡는다.

이 화면은 애플리케이션 셸(사이드바·헤더) **밖**에 있다. 셸 하단에 역할 전환·로그아웃이 있어, 로그인 화면이 셸 안에 있으면 로그인하지 않은 사용자에게 로그아웃 버튼이 보인다 `[설계]`.

---

## 3. 화면 구성

```
 ┌───────────────────────────────────────┬─────────────────────────────┐
 │  좌: 브랜드 패널 (lg 이상에만)          │  우: 로그인 폼    [테마 토글] │
 │  ─ 배경: 눈금 격자 + 파형(장식)         │                             │
 │                                       │  SIGN IN                    │
 │  [◈] 비TMS 소규모 사업장                │  운영 대시보드 로그인        │
 │                                       │  시연 계정이 입력되어…       │
 │  AI 기반 지능형                         │                             │
 │  배출관리 플랫폼          40px          │  아이디                      │
 │                                       │  [demo@hskorea.co.kr    ]   │
 │  현장 센서와 ECP가 모은 수질·설비        │  비밀번호                    │
 │  시계열을 Cloud AI가 읽고…              │  [••••••••              ]   │
 │                                       │                             │
 │  ┌───────────┬───────────┐            │  [      로그인  →       ]   │
 │  │실증 사업장 │계측 항목   │            │                             │
 │  │10개소     │수질 8·설비 3│            │  ┌─────────────────────┐   │
 │  ├───────────┼───────────┤            │  │실제 인증이 아닙니다   │   │
 │  │AI 모델    │수집 주기   │            │  │원문은 OAuth 2.0 +…  │   │
 │  │4종        │1~10분      │            │  └─────────────────────┘   │
 │  └───────────┴───────────┘            │                             │
 └───────────────────────────────────────┴─────────────────────────────┘
     1fr                                     minmax(400px, 520px)

 lg 미만: 좌측 패널을 숨기고 우측 1열만. 브랜드 마크는 폼 위로 옮긴다
```

| # | 구성요소 | 형태 | 내용 | 근거 |
|---|---|---|---|---|
| 1 | 테마 토글 | 아이콘 버튼 | 라이트·다크 전환 | `[설계: 셸 밖이라 셸의 토글에 닿지 못한다. 다크 우선 팔레트를 첫 화면부터 확인할 수 있어야 한다]` |
| 2 | 브랜드 마크 + 머리글 | 아이콘 + Eyebrow | `비TMS 소규모 사업장` | `[원문: 비TMS 소규모 폐수배출사업장이 사업 대상]`. Eyebrow는 한글에 자간을 주지 않는다(`shared/ui/eyebrow.tsx`) |
| 3 | 대제목 | 40px 2행 | `AI 기반 지능형 배출관리 플랫폼` | `[원문 p.37·118]` 국문 정식명. 줄여 쓰지 않는다(A2) |
| 4 | 시스템 설명 | 본문 1문단 | 센서·ECP → Cloud AI → **이상 탐지·수질 예측·오염도 추정·설비 예지보전** | `[원문]` AI 산출 4종. **하나도 빼지 않는다** — 빼면 범위가 달라 보인다(A2) |
| 5 | 지표 4칸 | 2×2 격자 | 실증 사업장 `10개소` · 계측 항목 `수질 8 · 설비 3` · AI 모델 `4종` · 수집 주기 `1~10분` | `[원문 p.33·37]` `[원문 p.55]` `[원문: ai-model-spec 4종]` `[원문 p.30·48]` — §3.2 |
| 6 | 눈금 격자 | 배경 패턴 | 44px 간격, 가장자리로 갈수록 소멸 | `[설계: 계측 화면의 성격을 배경으로만 암시한다. 끝까지 살아 있으면 표처럼 읽힌다]` |
| 7 | 파형 | 배경 SVG 2선 | **장식이며 데이터가 아니다** | `[설계]` — §3.3 |
| 8 | 아이디 | 텍스트 입력 | `demo@hskorea.co.kr` 프리필 | `[설계: 시연 중에 타이핑할 것이 없어야 한다]` |
| 9 | 비밀번호 | 비밀번호 입력 | `demo1234` 프리필 | 〃 |
| 10 | 로그인 버튼 | 버튼(전체 폭, 화살표) | 누르면 운영자로 진입 | §5 |
| 11 | 진입 안내 | 보조 문구 | 어느 역할로 들어가는지, 역할은 어디서 바꾸는지 | `[설계: 역할 전환 장치가 셸 하단에 있어 로그인 시점에는 보이지 않는다]` |
| 12 | 인증 고지 | 테두리 박스 | 실제 인증이 아님 + 원문 규정(OAuth 2.0 + JWT) | `[원문 p.69]` + `[설계: 없는 것을 있는 척하지 않는다]` |

### 3.1 시각 규칙

**액센트 색을 쓰지 않는다** `[설계: 이 시스템에서 색은 곧 상태다(design-system §2에서 장식용 파랑 #0284C7을 UI 크롬에서 제거했다). 로그인 화면에 장식 색을 두면 그 규칙이 첫 화면부터 깨진다]`.

색을 못 쓰므로 **대비를 면적과 명도로 만든다** — 좌측 패널은 `--surface`, 우측은 `--bg`로 갈라 두 면을 나누고, CTA는 `--fg` 바탕에 `--bg` 글자를 얹어 화면에서 가장 어두운(다크에서는 가장 밝은) 면적을 갖는다 `[설계]`.

브랜드 마크의 `--normal` 계열 색은 예외가 아니라 **사이드바와 동일한 요소를 그대로 옮긴 것**이다. 새로 정한 색이 아니다.

진입 시 stagger 1회(브랜드 → 제목 → 폼 → 고지). `prefers-reduced-motion: reduce`를 존중한다 — 공통 규칙이며 이 화면에서 새로 정하지 않는다.

### 3.2 좌측 패널에 적는 숫자

**첫 화면에서 지어낸 수치를 보이면 뒤 화면의 계측값까지 의심받는다.** 네 값 모두 원문 근거가 있는 것만 골랐다.

| 표시 | 값 | 근거 |
|---|---|---|
| 실증 사업장 | 10개소 | 5개 업종 × 2개소 `[원문 p.33·37]` · 성과지표 "실증 및 상용 적용 사업장 10개소 이상" |
| 계측 항목 | 수질 8 · 설비 3 | 수질 pH·EC·탁도·DO·수온·색도·NO₃-N·TOC + 설비 전류·전력·유량 `[원문 p.55]` |
| AI 모델 | 4종 | AutoEncoder · LSTM · RandomForest · XMARL-PPO `[원문: docs/analysis/ai-model-spec.md]` |
| 수집 주기 | 1~10분 | `[원문 p.30·48]` — 프로토타입은 이 범위 안의 5분을 쓴다 `[PROVISIONAL]` |

### 3.3 배경 파형은 데이터가 아니다

축·눈금·값·범례를 붙이지 않고 테두리 색으로만 그린다. 계측 차트로 읽히면 **E3·E4를 어긴 화면이 된다** `[설계]`.

값은 사인 합성으로 만든다 — 난수를 쓰면 서버와 클라이언트가 다른 그림을 그려 hydration이 깨진다.

---

## 4. 요구 데이터

| 데이터 | 타입 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| 시연 계정 ID | `string` | — | `[설계: shared/config/demo.ts. 서버가 없어 검증하지 않는다 — 입력란을 채우는 용도]` | 아이디 입력 |
| 시연 계정 비밀번호 | `string` | — | 〃 | 비밀번호 입력 |
| 로그인 여부 | `boolean` | — | `[설계: localStorage \`aquasense-signed-in\`]` | 진입 판정(§5) |
| 현재 역할 | `Role` = `admin`\|`operator`\|`guest` | — | `[원문 p.69]` 3역할 · `[설계: localStorage \`aquasense-role\`]` | 로그인 후 셸 전체 |
| 기본 역할 | `Role` | — | `[설계: operator]` — §7 | 로그인 직후 |

**서버로 보내는 값이 없다.** 입력값은 제출 시 버리며 어디에도 저장하지 않는다 `[설계: 검증할 서버가 없는데 계정 문자열을 저장하면 인증이 있는 것처럼 읽힌다]`.

---

## 5. 상호작용

| 조작 | 동작 | 결과(After Action) |
|---|---|---|
| `로그인` 클릭 / 폼 Enter | 입력값을 **검증하지 않고** 로그인 상태로 표시 | `/`(통합 관제)로 이동. 역할은 저장된 값, 없으면 운영자 |
| 미로그인 상태로 `/` 등 접근 | `<head>` 스크립트가 첫 페인트 전에 판정 | `/login`으로 `location.replace` — **깜빡임 없음** |
| 로그인 상태로 `/login` 접근 | 같은 스크립트가 판정 | `/`로 이동 |
| 셸 하단 `로그아웃` | 로그인 표시 제거 | `/login`으로 이동. **역할 선택은 남긴다** `[설계: 다시 들어와 같은 역할을 확인하려는 것이 시연의 정상 흐름이다]` |

| 항목 | 값 |
|---|---|
| SUBMIT | 해당 (로그인) |
| CANCEL | 해당 없음 — 되돌아갈 이전 화면이 없다 |

### 5.1 역할 전환 (로그인과 분리된 장치)

계정이 하나라 **로그인으로는 역할을 가릴 수 없다.** 전환은 셸 하단에 둔다 — 실제 제품에서도 계정·역할은 그 자리에 있다.

```
─────────────────────────
시연 계정            인가 아님
[관리자][운영자][게스트]
사업주 · 최고 책임자    [로그아웃]
자사 1개소
─────────────────────────
```

| 조작 | 동작 | 결과 |
|---|---|---|
| 역할 버튼 클릭 | `data-role` 교체 + localStorage 저장 | 사이드바 메뉴·범위 표시가 즉시 바뀐다. **지금 보는 화면이 새 역할에 닫혀 있으면** 그 역할이 볼 수 있는 첫 화면으로 옮긴다 — 통합 관제에서 관리자로 바꾸면 이상 탐지로 간다 |

메뉴에서 감추기만 하면 관리자로 전환해도 통합 관제 본문이 그대로 남아 회의 결정과 어긋나 보인다. 이동은 [use-role-route-guard.ts](../../../src/widgets/app-shell/lib/use-role-route-guard.ts)가 맡으며, **주소를 직접 쳤을 때도 되돌린다**(운영자가 `/cost-savings`로 들어가면 `/`로 옮긴다 — 브라우저에서 확인).

**그래도 인가가 아니다**(E6 예외). 서버가 막는 것이 아니라 화면이 스스로 옮기는 것이라, 되돌리기 전에 한 프레임이 그려지고 fixture는 여전히 번들 안에 있다. 백엔드가 생기면 서버가 응답 단계에서 걸러야 한다.

**기각한 대안: 로그인 화면에서 역할 선택** — 계정이 하나뿐이라 역할 선택이 곧 인증 절차처럼 읽힌다. 사용자 요청도 "별도의 권한 변경 방법"이었다.

---

## 6. 상태와 예외

| 상태 | 처리 |
|---|---|
| 로딩 | 해당 없음 — 서버 요청이 없다 |
| 빈 상태 | 해당 없음 |
| 통신 두절 | 해당 없음 |
| 결측 | 해당 없음 |
| 인증 실패 | **해당 없음 — 검증하지 않는다.** 실패를 흉내 내면 인증이 있는 것처럼 보인다 `[설계]` |
| 권한 없음 | 해당 없음 — 역할 이전의 화면이다 |
| localStorage 사용 불가(사생활 보호 모드 등) | 스크립트가 `try/catch`로 기본 역할을 세우고 진행한다. 새로고침마다 역할이 초기화되지만 화면은 깨지지 않는다 |
| JS 비활성 | 진입 판정과 역할 전환이 동작하지 않는다 `[설계: 프로토타입 전제. 차트·실시간 갱신이 이미 JS를 요구한다]` |

---

## 7. 설계 근거

### 7.1 왜 로그인 화면이 필요한가

역할 전환만 필요하다면 셸 하단 장치만으로 충분하다. 그럼에도 로그인을 두는 이유는 **시연의 시작점**이 필요해서다 — 심사자가 처음 보는 화면이 계측값 가득한 대시보드면 이 시스템이 누구의 것인지 설명할 자리가 없다. 브랜드 정식명과 "실제 인증이 아님" 고지가 여기서 한 번에 전달된다.

### 7.2 왜 기본 역할이 운영자인가

`[설계]` **구현된 8개가 전부 운영자 화면이다.** 관리자로 시작하면 통합 관제가 닫혀 있고(회의 2026-08-13) 관리자 전용 화면(SCR-AD-001)은 미구현이라 **빈 화면을 만난다.** 운영자로 들어가면 진입 즉시 볼 것이 있다.

### 7.3 왜 하이드레이션이 깨지지 않는가

서버는 `localStorage`를 읽을 수 없다. 역할로 마크업을 분기하면 서버(기본값)와 클라이언트(저장값)가 달라져 하이드레이션이 깨진다 — **이 저장소에서 이미 두 번 터진 문제다.**

그래서 **마크업이 역할을 모르게** 한다. 세 역할을 모두 렌더하고 `<html data-role>`을 CSS가 골라 보인다. `globals.css`의 `theme-when-dark`/`theme-when-light`가 테마에서 쓰는 방법과 같다.

| | 테마 | 역할 |
|---|---|---|
| 초기화 | `THEME_INIT_SCRIPT` → `data-theme` | `SESSION_INIT_SCRIPT` → `data-role` |
| 읽기 | `readAppliedTheme()` — DOM에서 | `readAppliedRole()` — DOM에서 |
| 선택 | `theme-when-*` | `role-hide-*` · `role-only-*` · `role-pick-*` |

DOM에는 숨은 메뉴가 남는다. **이것은 인가가 아니라 시연 표시이므로 문제가 아니다**(`frontend.rule.md` **E6 예외**). 백엔드 착수 시 서버 응답 권한으로 교체하며, 그때는 서버가 걸러 보낸다.

### 7.4 왜 route group인가

로그인은 셸 밖, 나머지 8개는 셸 안이다. Next.js App Router의 route group `(shell)`로 가른다 — URL에 `(shell)`이 드러나지 않아 기존 경로 8개가 그대로 유지된다. **root layout은 하나로 유지한다** — 여러 root layout은 그 사이를 오갈 때 전체 페이지 리로드를 일으킨다.

### 7.6 본문 폰트

전 화면이 **Pretendard 하나**를 쓴다. 이전에는 라틴·숫자를 IBM Plex Mono가 받았으나 한글 라벨의 가독성이 떨어져 교체했다 `[사용자 지시 2026-08-13]`.

**숫자 정렬은 폰트가 아니라 `tnum`이 지킨다.** Pretendard의 기본 숫자는 등폭이 아니다(`1` 898 · `4` 1278 units) — 브라우저에서 `.num`이 붙은 `111`과 `444`가 모두 25.81px, 붙지 않으면 18.42px와 26.22px로 갈리는 것을 확인했다. `globals.css`의 `.num`을 지우면 값이 갱신될 때마다 숫자가 좌우로 밀린다(**E1**).

### 7.5 문서와 코드를 갈리지 않게 하는 장치

역할별 허용 화면은 `screens.md` §5 권한 매트릭스와 `entities/user/config/constants.ts` **두 곳에 존재한다.** 두 곳에 있는 값은 갈린다.

[src/entities/user/user.test.ts](../../../src/entities/user/user.test.ts)가 매트릭스 표를 직접 파싱해 코드와 대조한다. 한쪽만 고치면 테스트가 깨진다 — 실제로 매트릭스의 `✕`를 `R`로 되돌려 실패를 확인했다.

---

## 8. 미정·제약

| 항목 | 표기 | 확정 필요 시점 |
|---|---|---|
| 실제 인증 방식 | `[원문 p.69]` OAuth 2.0 + JWT | 백엔드 착수 시. 현재는 서버가 없어 구현 불가 |
| 계정 정책(ID 형식·비밀번호 규칙·잠금) | `[TBD-09: 계정 관리·로그인 정책]` | 실제 인증 도입 시 |
| 세션 만료·자동 로그인 유지 기간 | `[TBD-09]` — 같은 항목이 만료 정책까지 포함한다 | 실제 인증 도입 시 |
| 비밀번호 찾기·회원가입 | `[설계: 화면에 두지 않는다]` — 원문에 절차가 없고, 없는 흐름을 버튼으로 만들면 눌렀을 때 갈 곳이 없다 | 계정 정책 확정 시 |
| 게스트의 실제 대상 | `[TBD-07]` 원문은 "조회만 가능"까지 · **회의에서도 미정** — 문서 기반 추측(감독기관) | 발주처 확인 |
| 역할별 **범위 필터** | **미구현** — 관리자로 바꿔도 지금은 표시만 바뀐다 | 다음 작업 |
| 게스트 조작 차단 | **미구현** — 알람 확인·리포트 내보내기가 그대로 눌린다 | 다음 작업 |
