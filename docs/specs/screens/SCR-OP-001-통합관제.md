# 화면 설계 — 통합 관제 (SCR-OP-001)

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 문서명 | 화면 설계 — 통합 관제 (SCR-OP-001) |
| 버전 | v1.58.0 |
| 작성일 | 2026-08-13 |
| 기반 문서 | /docs/specs/README.md, /docs/specs/requirements.md, /docs/requirements/functional-requirements.md, /design-system/smb-ai-emission-monitor/pages/dashboard.md |

### 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.58.0 | 2026-09-15 | Claude | **§6 「권한 없음」이 «홈으로 되돌린다»에서 «403 안내»로 바뀌었다** `[사용자 결정 2026-09-15]`. 되돌리는 쪽은 effect가 렌더 뒤에 돌아 **권한 없는 본문이 1.61~11.75초 그대로 보였다**(실측) — 가드가 막으려던 것을 가드가 보여 주고 있었다. 낡은 서술을 지우지 않고 뒤집힌 이유를 같은 칸에 적었다. 화면은 [`SCR-CO-002`](SCR-CO-002-안내오류.md) |
| v1.57.0 | 2026-09-15 | Claude | **월보드 카드를 누르면 그 사업장 상세로 간다** `[사용자 요청 2026-09-15: 카드를 클릭했을 때 알림에 대한 모달이 아닌 상세로 넘어가게]`. 2026-08-24부터 카드 클릭은 **미확인 알람 모달**이었다 — 카드는 훑는 개요이고 화면을 옮기는 것은 구역 머리의 링크뿐이라는 짜임이다(§7). **그 모달은 함께 걷혔다**: 부를 자리가 이 클릭 하나뿐이라 트리거를 잃었고, 알람으로 가는 길은 요약 머리의 `전체 보기`(알람 이력) 하나로 남는다. 카드 아래 줄의 **건수는 그대로**라 «어디에 몇 건»은 여전히 카드가 말한다. **바로 앞 v1.56.0과 갈리는 지점** — 그 판본은 `b884b1b`를 되돌려 카드 안의 «상세 칸»을 없앴고, 이번에는 칸을 되살리지 않고 **카드 전체를 한 목적지**로 만들었다: 같은 카드에 목적지가 둘이 되지 않으므로 v1.49.0이 hover를 세 단으로 가른 장치도 필요 없다. **`<button>`이 아니라 `<a>`다** — `onClick`으로 옮기면 새 탭·가운데 클릭·주소 복사가 사라지는데 화면으로는 똑같아 보인다(검사로 잠갔다). **사업장 축의 입구가 둘이 됐다**(구역 머리 `상세 보기` + 카드 10장) — 대상이 «고른 사업장»과 «그 카드의 사업장»으로 달라 겹치지 않는다. §3 도해 · §3.1 항목 3행 · §4 · §5 상호작용 + `After Action` · §7. **관내 감독(`SCR-GU-001`)은 그대로다** — 요청의 영역이 통합 관제였고, 그쪽 카드는 선택이다 |
| v1.56.0 | 2026-09-15 | Claude | **월보드 카드를 `b884b1b` 이전 짜임으로 되돌렸다** `[사용자 요청 2026-09-15: b884b1b 로 되돌리고, 내가 확인]`. 그 커밋(v1.44.0~v1.50.0)이 카드 안에 **상세 칸**을 내고 구역 머리의 `상세 보기`를 걷었는데, 되돌리면서 **둘이 함께 제자리로** 왔다 — 카드는 다시 **두 줄**(이름·점수 / 알람·등급 뱃지)에 한 줄 5장이고 카드 전체가 알람 모달이며, 사업장 상세로 가는 길은 다시 **구역 머리의 `상세 보기` 하나**다. §3 도해 · §3.1 항목(카드 행 되돌림 · `상세 링크` 행 제거 · `상세 이동 링크` 행 복원) · §5 상호작용(카드 클릭 행 되돌림 · `상세` 칸 행 제거 · 구역 머리 행 복원) · `After Action`. **낡은 서술을 지우지 않았으므로 아래 v1.44.0~v1.50.0 행은 그대로 둔다** — 무엇을 왜 넣었다 왜 걷었는지가 다음 사람이 같은 근거로 되돌리는 것을 막는다. **함께 오지 않은 것 셋**: ① 지도 확대 수정(v1.43.0 `기본 사업장 재선택`)은 같은 커밋이지만 카드와 무관해 남는다 ② `49593cf` 이후의 등급 틴트·넘김 화살표는 그대로다(디자인 기준선) ③ `사업장 현황 요약` 머리의 `전체 보기`(v1.51.0 `[사용자 확인 2026-08-31]`)도 남는다 — 그 근거의 본줄기는 «축이 다르다»이고 카드 `상세` 칸과의 충돌은 곁가지였다 |
| v1.55.0 | 2026-09-08 | Claude | **오염도 패널이 계측 계열을 본다** `[사용자 요청 2026-09-08]`. 이 화면의 예측 카드도 `getForecast`의 생성값을 그리고 있었다 — 같은 사업장의 TOC를 아래 계측 격자는 서버 값으로, 이 카드는 생성값으로 말했다(**E1**). 오염도 추정 화면과 **같은 계열**을 넘긴다 |
| v1.54.0 | 2026-09-07 | Claude | **첫 응답 전에는 계측 격자가 스켈레톤이다** `[사용자 지적 2026-09-07]`. 한때 `pending`이 내장 데이터를 들고 있어 답이 아닐 수 있는 값이 먼저 그려졌고, 응답이 오면 카드 여덟 장이 눈에 보이게 다시 그려졌다 — 그 구조가 이상하다는 지적이었다. §6에 대기 상태를 한 행으로 넣었다. 규칙과 상한은 `screens.md` §8 `로딩`이 갖는다 |
| v1.53.0 | 2026-08-27 | Claude | **수집 주기 5분 → 1분** `[INC-111]` — 범위 안의 값이라 원문 텍스트와 어긋나지 않으며, 어긋나는 것은 발표 그림의 5분이다 `[INC-111]`. 파생 표본 수를 함께 고쳤다 |
| v1.0.0 | 2026-08-13 | Claude | 신규 작성 — `docs/page-data-spec/dashboard.md`의 패널별 데이터를 근거 표기와 함께 옮기고, 화면 구성·상호작용·설계 근거를 추가 |
| v1.0.1 | 2026-08-13 | Claude | §4.4에 경향 카드가 표시하는 값·R²를 명시(구현은 있으나 문서에 없었다). TOC의 R² `원문 미규정` 표기를 §6 예외에 추가 |
| v1.0.2 | 2026-08-13 | Claude | 화면 ID 재배치 — `SCR-AD` → `SCR-OP`. 현재 화면은 전 사업장 통합 관제 주체(운영자)의 것이다 `[회의 2026-08-13]` |
| v1.1.0 | 2026-08-13 | Claude | §2.1 권한 관리자 열 `R` → `**✕**` — 회의 결정(통합 관제는 운영자 전용)이 screens.md §5 매트릭스에만 반영되고 이 문서에 남아 있던 것을 맞춤. `verify:docs` 검사 8이 잡았다 |
| v1.2.0 | 2026-08-19 | Claude | 수집 주기 근거를 `[PROVISIONAL]` → `[원문 발표 p.7 그림]`으로 교체 |
| v1.4.0 | 2026-08-21 | Claude | **경향 3열이 농도를 싣지 않는다.** `[회의 2026-08-20]`이 "소프트 센싱으로는 절대값 정확도를 맞추기 어려워 기준 대비 높낮이만 낸다"고 정리했는데 **이 화면만 계속 `50.0 mg/L`를 찍고** 있었다 — 같은 결정이 오염도 추정·리포트에서는 지켜졌다. 세 항목 전부에 `· AI 추정`을 박아 둔 것도 정정한다(TOC는 직접 계측이라 **E3** 위반이었다). 판정 문구·색·칩을 `entities/prediction`의 순수 함수 하나로 모아 세 화면이 같은 답을 낸다. 예측 파선·신뢰구간은 `[INC-109]`로 이미 화면에서 내려갔는데 §3 도해·§3.1에 남아 있어 함께 정정 |
| v1.3.0 | 2026-08-20 | Claude | **§3.1 `항목 목록` 신설** — 지도 13(핀 10 + 3) · 월보드 10 · 지표 타일 4 · 계측 8 · 타임라인 6 · XAI 7 · 예측 5 · 설비 4 · 알람 1 = 58행으로 13개 화면 중 가장 크다. §4.1·4.3·4.4 헤더를 템플릿 5열로 통일하고 전 행의 `UI 위치`를 채웠다. §4.1·4.6 제목의 `× 10`·`× 4`를 뗐다 — 사업장 10은 원문 근거가 있으나(`[원문 p.29]`) 설비 4는 시연값이다 `[TBD-48]` |
| v1.5.0 | 2026-08-24 | Claude | **선택과 개요를 분리했다** `[사용자 지시 2026-08-24]` — 사업장 월보드를 화면 최상단 전폭으로 올리고(10개소를 한 줄에), 카드 클릭을 사업장 선택에서 **미확인 알람 모달**로 바꿨다. 선택은 지도 머리에 신설한 **사업장 탭**이 전담한다(지도 핀 선택은 유지). §3 도해·§3.1 월보드/지도 행·§5 상호작용·§7 설계 근거 갱신 |
| v1.6.0 | 2026-08-24 | Claude | **요약 패널 이름·알람 합계·탭 위치 정리** `[사용자 지시 2026-08-24]` — `사업장 현황` → `사업장 현황 요약`, 지표 타일의 `미확인 알람 (전체)`을 요약 패널 머리로 옮겨(타일 4→3장) 선택 사업장 값과 전 사업장 합계가 같은 줄에서 섞이지 않게 했다. 사업장 탭을 지도 머리에서 **상세 격자 위 전폭**으로 옮겼다 — 460px 안에서는 세 줄로 접혔다. §3 도해·§3.1(탭 절 신설·요약 머리 행 추가)·§5·§7 갱신 |
| v1.7.0 | 2026-08-24 | Claude | **구역 제목 신설, 아래 2단 폭을 타일 3열에 정렬** `[사용자 지시 2026-08-24]` — 탭 줄 위에 `선택 사업장 현황` 제목을 두어 탭이 화면 이동이 아님을 밝혔고(`상세 대상` 라벨은 제목이 대신한다), 아래 2단의 오른쪽 열을 `calc((100% - 3rem)/3)`으로 잡아 타일 한 칸과 폭이 정확히 같아지게 했다. §3 도해·§3.1 탭 절·§7 갱신 |
| v1.8.0 | 2026-08-24 | Claude | **구역 묶기, 추세선 한 곳으로** `[사용자 지시 2026-08-24]` — 제목·탭·상세 격자를 한 `<section>`으로 묶고 안쪽 간격을 바깥보다 좁게 두었다. 요약 카드 10장의 스파크라인을 걷고 선택 사업장의 추세선 하나만 이상 점수 타일 안에 녹였다. 지도 레일 500→480px(사이드바 312px에 맞춰 오른쪽 열을 416px로 지킨다) |
| v1.9.0 | 2026-08-24 | Claude | **이상 탐지 결과를 상세의 첫 카드로, 타임라인을 그 안에 녹였다** `[사용자 지시 2026-08-24]` — 타일 격자 위 전폭에 두고 카드 안에서 타임라인 2 : 판정 1로 나눈다(아래 타일 3열과 같은 비율). 구역을 테두리로 두르고(면은 깔지 않는다) 그 여백을 지도 레일 480→440px로 지불했다. 로고 마크 24→28px |
| v1.10.0 | 2026-08-24 | Claude | **이상 탐지 결과 카드 재배치, 월보드 카드 두 줄로** `[사용자 지시 2026-08-24]` — 판정을 가로 3칸으로 눕히고 타임라인을 그 아래 전폭에 두었다(시간축은 넓을수록 읽힌다). 머리에 상세 이동 화살표(→ `/anomaly`)를 넣고, 이상 점수 타일의 추세선은 걷었다(같은 계열을 위 카드가 전폭으로 그린다). 구역 설명을 인포 툴팁으로, 월보드 카드는 업종·지역을 지우고 미확인 알람 + 등급 뱃지 두 줄로 |
| v1.11.0 | 2026-08-24 | Claude | **판정을 가로 3칸 → 2칸으로** `[사용자 지시 2026-08-24]` — 근거 3항목을 표에서 뱃지로 바꿔 점수 덩어리 안에 붙였다. 점수 덩어리는 옅은 면 위에 올리고 수치 46→56px `font-bold`, 등급을 라벨에서 뱃지로 |
| v1.12.0 | 2026-08-24 | Claude | **XAI 막대 3→8px·방향 아이콘, 범례를 그래프 아래로, 상세 링크에 글자** `[사용자 지시 2026-08-24]` — 카드 제목에 대상 사업장을 붙이고, 요약 패널 머리에 알람 이력 링크를 넣었다. 월보드 카드는 등급 뱃지를 점수 옆으로 올리고 줄 간격을 좁혀 높이를 줄였다 |
| v1.13.0 | 2026-08-24 | Claude | **막대 그라데이션·트랙 홈, 점수 면을 등급색 그라데이션으로, 구역 면 신설** `[사용자 지시 2026-08-24]` — 상세 링크 문구를 `상세 보기`로 통일하고 월보드 카드의 알람 줄에 화살표를 붙였다. 지도 레일을 두 단으로(xl 420 / 2xl 470px) — 1920px에서 오른쪽 열이 50px 줄고 지도가 380→430px로 커진다. xl에서 470px을 쓰면 오른쪽 열이 354px이 되어 타일이 2열 하한 밑으로 떨어진다 |
| v1.14.0 | 2026-08-24 | Claude | **알람 카드 제거, 구역 이름을 `선택 사업장 현황`으로** `[사용자 지시 2026-08-24]` — 요약 카드 클릭이 그 사업장 알람을 모달로 열고 요약 머리에 이력 링크가 있어 화면의 목록은 세 번째 경로였다. 오른쪽 칸이 비어 2단 분할도 함께 걷었다. 점수 덩어리 면의 끝을 회색에서 카드 면으로 바꾸고 점수를 그라데이션 글자로 |
| v1.15.0 | 2026-08-24 | Claude | **범례를 판정 줄 오른쪽 아래로, 요약 안내를 제목 옆 툴팁으로, 점수 면 6→14%** `[사용자 지시 2026-08-24]`. 차트가 `[그래프로 보기] [표로 보기]` 탭을 갖게 되어 대체표도 다른 표와 같은 형태가 됐다 |
| v1.16.0 | 2026-08-24 | Claude | **지도 아래 두 줄 제거** `[사용자 지시 2026-08-24]` — 선택 사업장 카드·핀 위치 주석을 걷고 지도 높이 계산의 상수를 318→250px로 내렸다(지도 68px 확대). 월보드 카드 점수에 `CountUp`을 붙였다 |
| v1.24.0 | 2026-08-24 | Claude | **요약 카드 3곳에 상세 이동, 고른 핀은 크기·그림자만, 점수 면 아래를 투명으로** `[사용자 지시 2026-08-24]` — 계측 → `/timeseries` · 예측 → `/prediction` · 설비 → `/equipment`. 고른 핀의 포인트색 고리를 걷고 1.28배 + 좁고 짙은 그림자로 바꿨다. 점수 면 그라데이션의 끝을 `transparent`로 |
| v1.23.0 | 2026-08-24 | Claude | **핀 테·상태 효과 정리, 검은 네모 제거, 점수 면 17/14%** `[사용자 지시 2026-08-24]` — 핀 흰 테 1.6→1px, hover는 뜸·누름은 눌림·고름만 고리. 지도와 핀의 `feDropShadow`를 걷어 검은 네모(무효 `flood-color`)를 없앴다. 점수 면을 7/4 → 17/14%로 올리고 점수 글자 그라데이션을 40→30%로 좁혔다 |
| v1.22.0 | 2026-08-24 | Claude | **점수 면 그라데이션을 아주 옅게** `[사용자 지시 2026-08-24]` — 위 7% → 아래 4%. 직전 판본(14→20%)은 면이 상태색으로 읽혀 값과 경쟁했다. 함께 올렸던 작은 글자 색도 `--fg-subtle`로 되돌렸다 |
| v1.21.0 | 2026-08-24 | Claude | **지도 핀을 물방울 마커로, 설비 상태를 카드로, 점수 면을 진하게** `[사용자 지시 2026-08-24]` — 핀은 바닥 그림자·등급색 그라데이션·흰 테·안쪽 그림자·흰 눈 다섯 층이고 끝이 좌표를 짚는다. 설비 상태는 구분선 네 칸에서 **사업장 요약과 같은 카드**로(이름 + 뱃지 / 가동 / 이상 신호). 점수 면 그라데이션을 아래쪽 20%까지 진하게 하고 그 위의 작은 글자를 `--fg-muted`로 올렸다(대비) |
| v1.20.0 | 2026-08-24 | Claude | **지도 선 색·크기 정리** `[사용자 지시 2026-08-24]` — 겉 테두리와 시도 구분선을 아주 연한 그레이로. svg의 최소 높이 400px를 걷고 감싼 상자가 남는 높이를 주게 해 카드 밖으로 밀려 나오지 않는다(`xl` 미만에서만 320px 바닥값) |
| v1.19.0 | 2026-08-24 | Claude | **지도 비주얼 개편** `[사용자 지시 2026-08-24: 첨부 이미지 래퍼런스]` — 기본 축척 남한 전체, 시도 면을 브랜드 블루 농도(사업장 수)로, 측면 층 + 그림자로 살짝 입체, 축척 전환을 560ms 보간(라벨·핀이 함께 움직인다), 핀의 고름·가리킴을 다른 축으로 분리, 범례에 면 농도 축 추가. 지도 레일 상단에 24px 여유(90→88svh) |
| v1.18.0 | 2026-08-24 | Claude | **구역 이름을 `선택 사업장 현황`으로, 지도를 탭에 연동** `[사용자 지시 2026-08-24]` — 탭으로 사업장을 바꾸면 지도가 그 시도로 확대된다(직접 누른 확대는 그 사업장을 보는 동안만 유지). 지도 확대 줄을 다른 탭과 같은 껍데기로, 핀 툴팁을 유리면으로 바꾸고 좌측 상태색 띠를 걷었다. 판정 카드의 두 칸이 같은 높이에서 끝난다 |
| v1.17.0 | 2026-08-24 | Claude | **KPI 타일 3장을 걷고 탭 줄을 고정했다** `[사용자 지시 2026-08-24]` — `선택 사업장 이상 점수` 타일은 같은 카드의 56px 점수와 중복이라 지웠고, 데이터 처리율·시스템 가동률은 그 카드 안 게이지 아래 **뱃지**로 내려왔다(목표치는 뱃지 안에 남긴다). 탭 줄은 스크롤 중 헤더 아래에 붙고, 지도 레일의 고정 위치·높이에서 그 줄 높이(`--sticky-bar-h`)를 뺀다 — 빼지 않으면 지도 머리가 탭 뒤로 들어간다. §3 도해·구성요소표·§3.1 갱신 |
| v1.25.0 | 2026-08-25 | Claude | **알람 모달을 네 단 위계로, 경향을 뱃지로** `[사용자 지시 2026-08-25]` — 모달에 요약 면(큰 건수 + 우선순위 뱃지 + 등급)과 이력 링크 푸터를 두고, TOC 카드의 경향 표기를 맨 글자에서 뱃지로 바꿨다 |
| v1.26.0 | 2026-08-25 | Claude | **요약 카드에 등급색 그라데이션, 모달 이력 링크를 요약 면 안으로** `[사용자 지시 2026-08-25]` — 카드 면에 위 9%→투명 등급색을 깔되 통신 두절은 깔지 않는다(E4). 알람 모달의 `이력 전체 보기`를 푸터에서 요약 면 오른쪽으로 옮겨 미확인 건수와 한 줄에서 읽히게 했다 |
| v1.27.0 | 2026-08-25 | Claude | **요약 카드 면을 흰색으로 되돌리고 구역 툴팁을 줄였다** `[사용자 지시 2026-08-25]` — 등급색 9% 그라데이션은 카드 10장이 늘어서면 격자를 얼룩지게 하고 점수·뱃지의 색과 경쟁했다. `선택 사업장 현황` 툴팁에서 전 사업장 기준 문장을 뺐다(위 요약 카드가 스스로 말한다) |
| v1.28.0 | 2026-08-25 | Claude | **요약 카드를 누르는 카드로** `[사용자 지시 2026-08-25]` — 오른쪽 끝에 화살표(hover에서 진해지며 오른쪽으로), 내용은 왼쪽에 모으고 여백을 줄였다(`px-4 py-3`). 아래 줄의 화살표는 걷었다 — 카드 오른쪽 끝이 그 일을 맡는다 |
| v1.29.0 | 2026-08-25 | Claude | **요약 카드의 값을 가운데로** `[사용자 지시 2026-08-25]` — 이름(왼쪽) · 점수+등급(가운데) · 화살표(오른쪽). 값이 오른쪽 끝에서 화살표와 붙어 한 덩어리로 보였다 |
| v1.30.0 | 2026-08-25 | Claude | **요약 카드 hover와 묶음 정리** `[사용자 지시 2026-08-25]` — hover는 테두리 한 단 + 얕은 그림자(면 색 변경은 열 장이 한 번에 반응하는 것처럼 티가 났다), 화살표는 색만 바뀌고 움직이지 않는다. 안쪽을 ①이름+미확인 알람 ②점수+등급 두 묶음으로 갈라 값이 카드 가운데에 선다 |
| v1.31.0 | 2026-08-25 | Claude | **요약 카드에 왼쪽에서 번지는 등급색** `[사용자 지시 2026-08-25: 첨부 이미지 래퍼런스]` — 12%→4%→투명(68%). 위에서 아래로 깔면 카드 열 장이 격자를 얼룩지게 했는데, 왼쪽에서만 번지면 값과 화살표가 있는 쪽은 흰 면 그대로다. 통신 두절은 깔지 않는다 |
| v1.32.0 | 2026-08-25 | Claude | **요약 카드 hover 강화, 그라데이션 방향 반전** `[사용자 지시 2026-08-25]` — hover에서 테두리(포인트색)·면(옅은 회색)·그림자 셋이 함께 움직인다(테두리만 바뀌던 판본은 알아채기 어려웠다). 등급색은 **오른쪽에서 번져 왼쪽으로** 사라진다 — 점수·뱃지가 오른쪽이라 색이 그 값 뒤에서 번진다 |
| v1.33.0 | 2026-08-25 | Claude | **타임라인·점수·알람 모달 마감** `[사용자 지시 2026-08-25]` — 이상 점수 타임라인의 선을 다른 차트와 같은 무게로(AI 계열 #1c7ed6, 면 34%, 밴드 55%), 56px 점수를 165° 세 단 그라데이션으로, 알람 모달을 유리면(반투명 + 뒤 흐림)으로 바꿨다 |
| v1.34.0 | 2026-08-25 | Claude | **사업장 카드를 한 줄 넘김으로** `[사용자 지시 2026-08-25]` — 줄바꿈 격자(2·3·5열)를 **다섯 장이 보이는 한 줄 트랙 + 화살표·인디케이터**로 바꿨다. 카드 폭·내용·누를 때 하는 일(그 사업장 알람 모달)은 그대로다 — 폭 계산이 격자와 같은 식이라 카드 안의 190px 분기도 움직이지 않는다. 근거는 screens.md §8 `카드 넘김` |
| v1.35.0 | 2026-08-25 | Claude | **넘김 화살표를 카드 줄 양 끝에 걸치게** `[사용자 지시 2026-08-25]` — 아래 한 줄에 모아 두었던 것을 트랙 좌우 가장자리에 절반씩 걸터앉게 옮겼다. 인디케이터는 아래 줄에 남는다. 근거는 screens.md §8 `카드 넘김` |
| v1.36.0 | 2026-08-25 | Claude | §2.1 권한 — **기초지자체에 이 화면을 닫았다**(`R` → `✕`) `[설계 2026-08-25]`. 전국 10개소가 `관할 시·군·구`라는 범위 정의와 모순이라 사업장에 닫은 것과 같은 논리다. 그 자리는 `SCR-GU-001 관내 감독 현황`이 받는다(미구현). 역할 이름도 함께 개명 |
| v1.37.0 | 2026-08-25 | Claude | §7 설계 근거의 시각 결정 출처를 **`screens.md` §8**으로 바꿨다 `[사용자 지시 2026-08-25]`. `design-system/` 링크는 착수 전 판단으로 강등 — 지도 면이 등급색에서 사업장 수 농도로 바뀐 것이 그 문서와 지금이 다르다는 예다 |
| v1.52.0 | 2026-08-31 | Claude | **XAI 기여 막대의 하강 기여가 파랗게 뜨던 것을 고쳤다** `[사용자 지적 2026-08-31: 범례에는 초록·노랑·주황·빨강인데 프로그래스 바에 파랑이 나온다]`. 막대와 화살표를 `up ? 등급색 : var(--actual)`로 갈라 칠하고 있었다 — 안동 염색 1공장은 `pH 수소이온농도`가 하강이라 초록 막대 넷 사이에 **파란 막대 하나**가 섰고, 그 파랑은 바로 아래 4구간 범례에 없는 다섯 번째 색이었다. 게다가 `--actual`은 **그래프 실측 계열색**이라(§8 `그래프 색`) 다른 모든 차트에서 «실측»을 뜻하는 색을 여기서만 «하강»으로 쓰고 있었다. **어디에도 근거가 없었다** — 이 문서 §3.1이 *"방향은 아이콘"* 이라 규정하고 `data-definition.md`도 필드 존재만 적는다. 다섯 막대를 한 색(그 판정의 등급색)으로 되돌렸다: 길이가 «얼마나 기여했는가»를 말하는데 색이 둘이면 길이 비교가 두 무리로 갈린다. 방향은 화살표 모양과 `sr-only` 문구가 그대로 나른다. **`AnomalyPanel`을 쓰는 네 화면이 함께 고쳐진다**(통합 관제 · 이상 탐지 · 사업장 상세 · 관내 감독). §8 `막대·게이지`에도 규칙을 올렸다(v2.66.0) |
| v1.51.0 | 2026-08-31 | Claude | **`사업장 현황 요약` 머리의 링크를 `상세 보기` → `전체 보기`로** `[사용자 확인 2026-08-31]`. 축이 다르다 — 나머지 넷은 «고른 사업장을 그 주제로 더 자세히»이고 이것만 «전 사업장 알람을 전부»다. **v1.44.0이 만든 충돌이기도 했다**: 같은 패널의 카드마다 `상세` 칸이 생겨 `상세`가 한 패널에 열한 번 나오면서 뜻이 둘로 갈렸다(카드=그 사업장 · 머리=알람 이력). `DetailLink`에 `text` 프롭을 두어 기본은 `상세 보기`로 두고 이 한 자리만 바꾼다. §8도 «동작이 다르면 문구도 다르다»로 다듬었다(v2.65.0) |
| v1.50.0 | 2026-08-31 | Claude | **구역 머리의 `상세 보기`를 걷었다** `[사용자 요청 2026-08-31]`. v1.39.0이 «사업장 축의 입구»로 넣었던 것이다 — 다른 `상세 보기` 다섯이 **주제별로** 흩어 보내는데(계측→시계열…) 고른 사업장 하나를 통째로 볼 곳이 없어서였다. **그 자리를 월보드 카드의 상세 칸이 물려받았고**(v1.44.0~v1.49.0) 카드에서는 고르지 않고 한 번에 간다 — 두 입구를 남기면 **같은 목적지가 화면에 둘**이 되고, 하나는 «고른 것»을 다른 하나는 «그 카드»를 뜻해 어느 사업장으로 가는지가 자리마다 달라진다. §3 도해 · §3.1 항목 1행 · §5 상호작용 1행 제거, `After Action`의 «링크 6개»를 «주제별 5개 + 카드 10칸»으로 정정 |
| v1.49.0 | 2026-08-31 | Claude | **카드 안의 두 목적지를 hover로 갈랐다** `[사용자 지적 2026-08-31: 알람 모달 영역과 상세 버튼 영역이 명확히 구분이 안 된다]`. 원인은 **CSS `:hover`의 전파**였다 — 테두리 hover가 카드 `<div>`에 걸려 있어 상세 칸 위에서도 켜졌고, 두 신호가 동시에 떠 무엇을 누르게 되는지가 그림으로 갈리지 않았다. 테두리만 `[&:has(>button:hover)]`로 **본체 버튼에 매달고** 그림자는 카드에 남긴다 — 상세 칸은 그 버튼의 형제이자 `z-[2]`라 포인터가 칸에 있으면 `button:hover`가 거짓이 된다. 결과는 **세 단**이다: 그림자=이 카드 · 테두리=이 카드 본체 · 칸 면·글자=이 칸. `[사용자 결정 2026-08-31]` 상세 칸에서는 **그림자만** 뜬다(카드를 완전히 세우는 안은 «칸 밖으로 나가면 문득 떠오르는» 그림이 되어 기각). 쉬는 경계(40px 세로선 + 반투명 면)는 그대로. **이 저장소의 첫 `:has()` 사용이다** — 자식이 조상을 꾸미는 CSS 수단이 그것뿐이고, JS 상태로 가르면 카드 열 장이 hover마다 다시 그려진다. 브라우저 실측: 본체 위 `--accent/50` · 상세 칸 위 `--border`(쉬는 값) · 두 곳 모두 그림자 있음 |
| v1.48.0 | 2026-08-31 | Claude | **카드 테두리를 기존으로 되돌렸다** `[사용자 요청 2026-08-31: border-color는 기존처럼 되돌려줘]`. v1.45.0이 첨부 이미지를 따라 등급색을 `--card-border`에 35% 섞어 얹었던 것을 걷고, 테두리는 다시 **클래스가 정하는 세 단**(기본 `--border` · hover `--accent/50` · 선택 `--accent/40`)이 된다. 등급색은 **면 그라데이션 하나**로만 말한다. 되돌리면서 둘이 함께 풀렸다 — ① v1.45.0이 따로 막아야 했던 **«인라인 테두리가 고른 카드의 선택 표시를 덮는»** 문제가 원인째 없어졌고(관내 감독에서 무엇을 골랐는지 사라졌던 것) ② 등급색을 덮지 않으려고 걷어 두었던 **hover 테두리가 살아났다**. 다만 예전의 `hover:bg-surface-2`(면)는 되살리지 않는다 — 등급 틴트 위에 회색을 얹는 것이라 `상세` 칸에서 지적된 «색이 안 어울린다»를 카드 전체에서 되풀이한다. 검사도 «어느 카드도 테두리 색을 인라인으로 정하지 않는다»로 바꿔 일부러 되살려 실패를 확인했다 |
| v1.47.0 | 2026-08-31 | Claude | **`상세` 칸의 글자색과 hover를 색상 축에서 뺐다** `[사용자 지적 2026-08-31: text 색상과 hover 색상이 적절하지 않다]`. 두 판본을 거쳤다 — ① 쉴 때부터 포인트색 + `hover:bg-accent-weak`: hover마다 등급 틴트가 **파란 틴트로 갈아 끼워졌다**(초록 카드를 가리키면 그 칸만 파래진다) ② 중립으로 쉬고 `hover:text-accent`: 앱의 다른 `상세`들과 어휘는 같지만(관내 감독 표의 `ACTION_BUTTON_QUIET`), 카드가 저마다 등급으로 물든 이 자리에서는 **색상 축을 하나 더 얹는 셈**이다. 지금은 **명도만 움직인다** — 면 55%→85%, 글자 `--fg-muted`→`--fg`. 카드의 그림자와 겹쳐 «카드가 떴고 그 안에서 이 칸이 한 단 더 밝다»는 위계가 된다. **framer-motion으로 대신하는 길도 검토했다**(`whileHover` 웨이브 · `SegPill`식 `layoutId` 하이라이트): `MotionPreferences`가 `reducedMotion="user"`라 감속 설정에서는 transform·layout이 꺼지고 끝 상태로 앉으므로 **끝 상태가 달라야 하는 것은 마찬가지**이고, §8도 모션을 «진입 시 1회»로 정한다 — 모션은 대안이 아니라 장식이라 미뤘다 |
| v1.46.0 | 2026-08-31 | Claude | **`상세` 칸의 면을 반투명으로 바꿨다** `[사용자 지적 2026-08-31: 카드 색상과 어울리지 않는다]`. 고정 회청색(`--surface-2`)을 깔았는데 등급색 그라데이션이 `to left`라 **오른쪽 끝이 가장 짙고**, 그 자리를 이 칸이 통째로 덮고 있었다 — 초록·붉은 카드 위에 회색 조각이 붙어 보인 이유다. `--surface`를 55%로 얹어 첨부 이미지가 흰 면에 #F8FAFC로 한 단 낮춘 것과 같은 단차를 만들되 **카드의 색조가 통과**한다. 글자색은 그대로 포인트색이며 대비는 라이트 8.6:1 · 다크 8.5:1로 잰다 |
| v1.45.0 | 2026-08-31 | Claude | **사업장 카드를 세 칸으로 다시 짰다** `[사용자 요청 2026-08-31: 첨부 이미지]`. 판정 열(등급 뱃지 · 점수 · **점수 막대** 신설) → 정보(이름 · 알람) → 세로선 뒤 **상세 칸**(64px, `--surface-2`). 바로 아래 v1.44.0이 넣은 칩이 이름 줄에서 자리를 다퉈 칸으로 넓혔다. 등급색은 **면 그라데이션에 테두리가 더해졌다** — 테두리는 첨부 이미지의 것이고 그라데이션은 원래 있던 것이다(한 번 걷었다가 화면을 보고 되살렸다 `[사용자 수정 요청 2026-08-31]`). **한 줄 5장 → 4장** `[사용자 결정 2026-08-31]`: 세 칸이 163px을 먼저 가져가 222px 카드에서는 이름이 전부 잘렸다. 분기값을 사업장 이름 실측 최장폭(103px)에서 다시 냈고 **뷰포트 390~1920px 전 구간에서 잘림 0**을 확인했다. 함께 고친 결함 — **고른 카드의 선택 테두리를 등급색이 덮었다**(인라인 스타일이 클래스를 이긴다). 관내 감독은 카드가 선택이라 거기서 무엇을 골랐는지 사라졌다. 초록 포인트는 우리 `정상` 등급색이라 `상세` 글자는 `--accent`로, 9~11px 글자는 12px 이상으로 옮겼다(§8) |
| v1.44.0 | 2026-08-31 | Claude | **월보드 카드마다 `상세` 칩을 넣어 사업장 상세를 한 번에 연다** `[사용자 요청 2026-08-31]`. 지금까지는 두 걸음이었다 — 핀·탭으로 고르고, 구역 머리의 `상세 보기`를 다시 누른다. 그 링크는 **이 화면에 여섯 개인 같은 모양의 칩** 중 하나라 눈에도 띄지 않았다. 카드의 오른쪽 끝 화살표(«이 카드를 누른다»는 표시였다) 자리를 테두리 있는 칩이 물려받았고, **카드 본체는 그대로 알람 모달이다**(A2). 카드가 한 덩어리 `<button>`에서 **본체 버튼 + 상세 링크 두 형제**로 바뀌었다 — 버튼 안의 링크는 유효하지 않은 HTML이라 칩을 눌러도 본체가 먹는다. 실측: 카드 222×91px 그대로이고 이름 10개 모두 잘리지 않는다(왼쪽 블록 162→134px). 좁은 카드(<160px)에서는 §8대로 글자를 감추고 화살표만 남긴다. §3.1 항목 1행 신설 + 카드 행 갱신 · §5 상호작용 1행. **관내 감독은 그대로다** — 그쪽은 표의 `상세` 버튼이 이미 그 일을 하고 카드는 선택이다 |
| v1.43.0 | 2026-08-31 | Claude | **기본 사업장으로 돌아오면 확대가 풀리던 것을 고쳤다** `[사용자 지적 2026-08-31]`. 바로 아래 v1.42.0 수정이 «처음»을 **마운트 시점의 사업장**으로 붙잡아 두었는데, 첫 방문의 그 값이 기본 사업장(`S-02` 구미)이라 **다른 곳을 거쳐 구미를 다시 누르면** 첫 방문과 같아져 전국으로 돌아갔다 — 열 곳 중 한 곳만 확대되지 않았다. 지금은 «주소가 지금 사업장을 지목하고 있는가»만 매 렌더에 본다(붙잡아 두는 상태가 없어졌다). §5에 `기본 사업장 재선택` 1행. 곁들여 지도 축척 보간의 하한 클램프 누락도 고쳤다 — 프레임 시각이 출발보다 앞서면 배율이 **음수**가 되어 지도가 뒤집힌다 |
| v1.42.0 | 2026-08-31 | Claude | **새로고침이 지도 확대를 풀던 것을 고쳤다** `[사용자 지적 2026-08-31]`. 사업장을 고르면 지도가 그 시도로 확대되는데 새로고침하면 탭·주소는 그대로이고 **지도만 전국**이었다 — §8이 말하는 «처음»은 아무것도 고르지 않은 첫 방문인데 코드는 마운트 시점의 사업장으로 판단했다. §5에 `새로고침` 1행 |
| v1.41.0 | 2026-08-28 | Claude | **`Δ 유입 − 유출` 칸에 단위 한글 병기를 채웠다** `[사용자 지적 2026-08-28]`. `[회의 피드백 2026-08-24]`가 정한 규칙(카드는 기호 툴팁으로 병기)을 같은 격자의 다른 두 칸은 지키는데 이 칸만 빠져 있었다 — 2026-08-25에 새로 만들면서 규칙이 딸려오지 않은 것이다. **이 자리가 두 번째다**: 처음에는 툴팁을 단위 span에 달아 `pH`·`진동`에서 조용히 사라졌었다. 세 번째가 없게 **칸의 종류를 세지 않고 기호 span 전체를 훑는 검사**를 넣고 두 함정을 일부러 되돌려 확인했다(`unit-annotation.test.tsx`) |
| v1.40.0 | 2026-08-28 | Claude | **`데이터 처리율`·`시스템 가동률` 뱃지를 걷었다** `[사용자 요청 2026-08-28]`. 두 값은 원문 성과지표(p.3·p.119)이고 v1.17.0이 KPI 타일에서 뱃지로 내려 두었던 것이다 — 그때의 근거는 *"그 판정이 믿을 만한 상태에서 나왔는가를 말하는 보조값"* 이었고, 사용자가 두 화면 모두에서 내리기로 정했다. **값은 사라지지 않았다**(`Site.dataThroughput`·`Site.uptime`) — 다시 세울 자리가 생기면 뱃지 둘을 되돌리면 된다. 아래 [모델][기간][산출] 세 뱃지는 남긴다 — AI 산출 근거라 E3가 값과 함께 요구한다. **추이 카드 툴팁에 6시간 예측 미제공 사유를 더했다** — 차트 아래 문단이던 것을 §8대로 제목 옆으로 옮겼다. §3 도해·항목 목록 2행·§4 갱신 |
| v1.39.0 | 2026-08-28 | Claude | **사업장 축의 입구를 냈다** `[사용자 요청 2026-08-28]` — 이 화면의 `상세 보기` 다섯은 주제별로 흩어 보내는데(계측→시계열…) **고른 사업장 하나를 통째로 볼 곳이 없었다.** 구역 머리에 `사업장 상세`(`SCR-AD-003`) 링크를 넣고 §3 도해·항목 목록 1행·§5 상호작용 1행을 더했다. `After Action`의 `화면 이동 없음`을 정정 — **주제별 링크가 이미 있었으므로 그 서술은 처음부터 틀렸다.** 핀·카드·탭·헤더 넷은 그대로 선택이다 `[사용자 결정 2026-08-28]` |
| v1.38.0 | 2026-08-25 | Claude | **계측 격자를 소절 둘로 나눴다** `[회의 피드백 2026-08-24: 유량·유입·유출은 나와야 한다]` — 수질 8칸 + **유량 3칸**(유입·유량·유출). 한 격자에 섞지 않는 이유는 축이 다르기 때문이다(농도 vs 부피/시간). 유입·유출은 채널이 없어 `계측 없음 [TBD-52]`으로 적는다. §3 구성요소와 항목 목록 3행 추가 |

---

## 2. 화면 개요

| 항목 | 값 |
|------|-----|
| 화면 ID | SCR-OP-001 |
| 메뉴(1Depth) | 통합 관제 |
| 메뉴(2Depth) | — |
| 화면 구분 | 대시보드 |
| 경로 | `/` |
| 구현 | [`src/widgets/dashboard/ui/dashboard-view.tsx`](../../../src/widgets/dashboard/ui/dashboard-view.tsx) |
| 관련 요구사항 | REQ-AD-004(통합 대시보드) · REQ-AD-026·027·028(다사업장) · REQ-AD-001·003(계측) · REQ-AD-006·007(이상 점수) |

### 2.1 권한

| 사업장 | 기초지자체 | 시스템 관리자 |
|---|---|---|
| **✕** | **✕** | R |

`[회의 2026-08-13: 통합 관제 화면은 운영자만 확인할 수 있는 페이지]` — 지도·10개소 월보드는 **자사 1개소뿐인 관리자에게 의미가 없다.** 원문 p.69의 "관리자=모든 기능"과 어긋나지만 회의 결정이 우선한다(README §3 근거 표기 규약).

**기초지자체에도 닫는다** `[설계]` — 전국 10개소가 `관할 시·군·구`라는 범위 정의와 모순이다. 그 자리는 `SCR-GU-001 관내 감독 현황`이 채운다(미구현).

시스템 관리자는 조작 요소가 없어 R만 존재한다 `[원문 p.69]`.

---

## 3. 화면 구성

```
┌────────────────────────────────────────────────────────────────────────────┐
│ 사업장 현황 요약        전체 미확인 알람 N건 · 긴급/주의/정보 · 카드→상세   │  ← 최상단 전폭
│ ┌────┬────┬────┬────┬────┐                                                │
│ │카드│카드│카드│카드│카드│  ← 10개소, 컨테이너 860px 이상에서 5열          │
│ ├────┼────┼────┼────┼────┤     클릭 → 그 카드의 사업장 상세로 간다         │
│ │카드│카드│카드│카드│카드│                                                │
│ └────┴────┴────┴────┴────┘                                                │
└────────────────────────────────────────────────────────────────────────────┘
  ┌ 선택 사업장 현황 [상세 보기]  탭으로 고른 한 개소를 자세히 본다 ← 구역 제목 ┐
  │ [탭][탭][탭] … 10개소   ← 스크롤하면 헤더 아래(61+8px)에 붙는다            │
  │ (제목·탭·격자를 한 구역으로 묶는다 — 면 + 테두리, 안쪽 12px < 바깥 24px)   │
┌──────────────────────────────┬────────────────────────────────────────────┐
│ 사업장 위치            [범례] │ ┌ 이상 탐지 결과   [구간 범례] [상세 보기]┐ │
│                               │ │ 점수 56px · 게이지  │ XAI 기여 5        │ │
│ ┌──────────────────────────┐ │ │                                         │ │
│ │                          │ │ │ [모델][기간][산출]                      │ │
│ │ [전국][경기][경북]  hover │ │ │ 이상 점수 타임라인 — width 100%         │ │
│ │      대한민국 SVG        │ │ └─────────────────────────────────────────┘ │
│ │      시도 17 + 핀 10     │ │ 수질·설비 실시간 계측                       │
│ │  (xl 이상 sticky 레일 —  │ │ 예측 차트                                   │
│ │   탭 줄 높이만큼 내려간다)│ │ 설비 상태                                   │
│ └──────────────────────────┘ │                                             │
└──────────────────────────────┴────────────────────────────────────────────┘ ┘

선택(= 아래 상세의 대상)은 **사업장 탭**과 **지도 핀**만 바꾼다.
월보드 카드는 선택을 건드리지 않고 **그 카드의 사업장 상세(`/overview`)로 옮긴다** `[사용자 요청 2026-09-15]`.
한때는 그 사업장의 미확인 알람 모달이 열렸다.
```

| 구성요소 | 형태 | 구현 |
|---|---|---|
| 사업장 지도 | SVG 지도, 시도 17 도형, 사업장 핀 10, **확대 탭**(다른 탭과 같은 껍데기), 유리면 툴팁, 범례 | `site-map/` |
| 사업장 월보드 | 카드 격자, 상태 띠, 이상 점수, 스파크라인, 알람 배지 | `site-wallboard/` |
| 수질·설비 계측 격자 | **소절 둘** — 수질 8칸 + 유량 3칸. 소형 다중 차트 | `water-quality-grid/` |
| 이상 점수 타임라인 | 면적 차트, 4구간 밴드, 결측 음영, 구간 범례 | `anomaly-timeline/` |
| 이상 탐지 결과 | 게이지, XAI 기여 변수 막대 | `anomaly-panel/` |
| 계열 차트 | 계열 선(출처별 색), 현재 기준선, **판정 3열** | `forecast-chart/` |
| 알람 목록 | 행 목록, 우선순위 칩 | `alarm-list/` |
| 설비 상태 | 카드 4개 — 이름 + 등급 뱃지 / 가동 상태 / 이상 신호 (사업장 요약 카드와 같은 구성) | `equipment-panel/` |

---

### 3.1 항목 목록

**영역이 아니라 영역 안의 항목이 한 행이다**(`deliverable-xlsx.rule.md` X4). 근거는 [`items.md`](../items.md)가 갖고 여기서는 **그 화면에서의 형태**만 적는다.

이 화면이 가장 크다 — **운영자의 첫 화면**이라 전 사업장을 한 장에 담는다. 관리자에게는 닫혀 있다 `[회의 2026-08-13]`.

#### 사업장 지도 — 실증 10개소

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| `SITE-01`~`SITE-10` | 지도 | 사업장 1개소당 마커 하나 | **물방울 마커** — 바닥 타원 그림자 · 등급색 그라데이션 몸통 · 흰 테 1px · 안쪽 그림자 · 머리 가운데 흰 눈 + 등급색 점. 끝이 좌표를 짚는다. 상태는 **크기와 그림자**로 가른다 — 가리킴 1.08배 + 뜸(그림자 넓고 옅게), 누름 0.92배 + 눌림, **고름 1.28배 + 좁고 짙은 그림자**(테두리 없음) · hover 유리면 툴팁 | `[사용자 지시 2026-08-24]` 원형 핀은 "여기"를 가리키지 못했다. 흰 테가 굵으면 등급색 면이 줄어 1px로 내렸다. 고름에 포인트색 고리를 두던 판본은 테두리가 마커의 등급색과 경쟁했다 — 크기와 그림자만으로도 하나만 도드라진다 |
| — | 지도 | 아래 붙는 줄 | **없다** — 선택 사업장 카드와 핀 위치 주석을 걷었다 | `[사용자 지시 2026-08-24]` 같은 값은 아래 `이상 탐지 결과`가 더 크게 보이고, 핀 좌표의 근거(`시·군까지만 지정`)는 이 문서가 갖는다. 지도가 68px 커지고 카드 높이가 선택에 따라 흔들리지 않는다 |
| — | 지도 | 시도 도형 | Natural Earth 1:50m · **브랜드 블루 그라데이션**, 농도는 그 시도의 사업장 수(0·1·2·3+ 네 단) · 같은 도형을 3만큼 내려 깐 **측면 층**과 옅은 포인트색 그림자로 살짝 입체 · 겉 테두리와 구분선은 아주 연한 그레이(`--border`) | `[설계]` Natural Earth 1:50m 퍼블릭 도메인 · `[사용자 지시 2026-08-24: 브랜드 블루 그라데이션 · 약간의 3D]` 등급색으로 칠했던 판본은 한 시도에 여럿일 때 나머지가 지워지고 **면적이 심각도로 읽혔다**(경기도의 주의 1건 > 서울의 위험). 기울이는 3D 투영은 쓰지 않는다 — 위쪽 시도의 면적·거리가 왜곡된다 |
| — | 지도 | 확대 탭 | 전국 · 시도별 · 다른 화면의 탭과 같은 껍데기 | `[설계]` 전국 배치에서 개별 사업장을 고르려면 확대가 필요하다 · 대상 시도는 실증 사업장이 있는 곳만 · **기본값은 남한 전체** `[사용자 지시 2026-08-24]` — 들어온 순간 한 시도로 확대돼 있으면 나머지 9개소가 화면 밖이다. 탭으로 사업장을 바꾸면 그 시도로 **560ms에 걸쳐 이동**한다 |
| — | 지도 | 범례 | 등급 4단계 + 통신 두절(핀) · **사업장 수 3단 계단**(면) | 상태 등급 4단계 `[INC-01·03·04]` · 색과 함께 형태 부호를 쓴다 `[설계: 색각 이상·흑백 인쇄에서 등급이 사라진다]` · `[사용자 지시 2026-08-24]` 면이 개수를 말하게 되어 그 축을 범례에 적지 않으면 농도가 심각도로 읽힌다 |

#### 사업장 현황 요약 — 사업장 1개소당 카드 한 장 (화면 최상단 전폭)

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 요약 제목 옆 | 인포 툴팁 | `카드를 누르면 그 사업장의 상세 화면으로 이동합니다…` `[사용자 요청 2026-09-15]` | `[사용자 지시 2026-08-24]` 머리 오른쪽 끝에 문구로 두었을 때는 제목과 멀어 무엇을 설명하는지 알 수 없었다 |
| — | 요약 머리 | 이동 링크 | **`전체 보기`** + `ChevronRight` → `/alarms` (`aria-label`은 `알람 이력으로 이동`) | `[사용자 지시 2026-08-24]` 카드가 사업장별 알람을 열어 주므로 전체 이력으로 가는 길도 같은 머리에 둔다. **글자가 `상세 보기`가 아닌 이유** `[사용자 확인 2026-08-31]` — 이 화면의 다른 넷은 «고른 사업장을 그 주제로 더 자세히»인데 이것만 **«전 사업장 알람을 전부»** 라 축이 다르다. 패널 이름이 `요약`이라 그 반대는 `전체`이고, 바로 옆이 `전체 미확인 알람 N건`이라 한 줄로 이어 읽힌다 |
| — | 요약 머리 | 전체 미확인 알람 | 건수(위험색 강조) + 우선순위 분해 `긴급 N · 주의 N · 정보 N` | `[사용자 지시 2026-08-24]` 지표 타일 한 장이던 것을 여기로 옮겼다 — 타일들은 **선택 사업장**의 값이고 이것만 전 사업장 합계라 같은 줄에서 축이 갈렸다. 카드마다 사업장별 건수가 있어 그 합계가 놓일 자리는 이 머리다 |


| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| `SITE-01`~`SITE-10` | 월보드 | 사업장 1개소당 카드 | **두 줄** — 위: 사업장명 왼쪽 · 이상 점수 오른쪽, 아래: 미확인 알람 왼쪽 · 등급 뱃지 오른쪽. **클릭 시 그 카드의 사업장 상세(`/overview`)로 간다** `[사용자 요청 2026-09-15]` — 오른쪽 끝의 화살표가 그 자리임을 말하고, 이제 **카드 전체가 하나의 링크**라 화살표는 표시가 아니라 링크의 일부다. 주소는 `useSiteHref('/overview', siteId)`가 만든다 — 손으로 `?site=`를 이어 붙이면 `scope`·`municipality`가 함께 날아간다(§8 `상세 이동` ②). **`<button>`이 아니라 `<a>`다** — `onClick`으로 옮기면 새 탭·가운데 클릭·주소 복사가 사라지고 화면으로는 드러나지 않는다. **한때 «클릭 시 알람 모달»이었다** `[사용자 지시 2026-08-24]` — 카드는 훑는 개요이고 화면을 옮기는 것은 구역 머리의 링크뿐이라는 짜임이었다. 그 사이 `b884b1b`가 카드 안에 **상세 칸을 따로 내는** 방식을 세웠다 걷혔고(2026-09-15 되돌림), 지금은 칸이 아니라 **카드 전체가 한 목적지**라 같은 카드에 목적지가 둘이 되지 않는다 — v1.49.0이 hover를 세 단으로 가른 것도 그래서 필요가 없다. 업종·지역은 적지 않는다 `[사용자 지시 2026-08-24]` — 카드를 훑는 목적은 어디가 위험한가이고 그 답은 점수·등급·알람이다(업종·지역은 지도 핀 툴팁과 알람 모달이 갖는다). **스파크라인은 없다** `[사용자 지시 2026-08-24]` — 10장에 각자 두면 선 열 개가 서로 경쟁해 훑기가 어려워진다. 선택 사업장의 추세는 아래 이상 점수 타일이 더 넓은 폭으로 보여 준다 | `[사용자 지시 2026-08-24]` 카드는 훑는 개요이고 고르는 일은 지도 머리의 탭이 한다. 좌측 상태 띠와 선택 강조는 걷었다 — 누를 때 무엇이 일어날지 알 수 없어진다 |

#### 사업장 탭 — 실증 10개소 (상세 격자 위 전폭)

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 구역 머리 | 제목 + 인포 툴팁 | `선택 사업장 현황` (16px bold) + 인포 아이콘(호버 시 범위 설명) | `[사용자 지시 2026-08-24]` 탭만 있으면 화면 이동으로 읽힌다. 화면명(20px)보다 작고 카드 제목(15px)보다 커야 아래 카드가 이 구역에 속한 것으로 읽힌다 |
| — | 구역 머리 | 상세 이동 링크 | `상세 보기` + `ChevronRight` → `/overview` (`aria-label`은 `<사업장명> 사업장 상세로 이동`) | `[사용자 요청 2026-08-28]` 이 화면의 다른 `상세 보기` 다섯은 **주제별로** 흩어 보내는데(계측→시계열, 예측→오염도…), **고른 사업장 하나를 통째로 볼 곳이 없었다** — 대표님이 "사업장을 클릭했을 때 상세하게 볼 화면"을 요청한 자리가 여기다. 선택은 탭·핀·헤더 선택기가 그대로 맡고 **이 링크만 화면을 옮긴다**(§5). **«이 링크만»은 2026-09-15에 깨졌다** `[사용자 요청 2026-09-15]` — 월보드 카드도 화면을 옮긴다. 둘을 함께 두는 이유는 **대상이 다르기 때문**이다: 이 링크는 «탭에서 고른 사업장», 카드는 «그 카드의 사업장»이다. `b884b1b`가 이 링크를 걷었던 근거(*"같은 목적지가 화면에 둘"*)는 그때 **카드 하나 안에** 목적지가 둘이던 것과 겹쳐 있었고, 지금은 카드에 목적지가 하나뿐이라 겹치지 않는다 |
| `SITE-01`~`SITE-10` | 탭 줄 | 사업장 1개소당 탭 | 등급 점 + 사업장명 · 선택 시 **흰 알약이 미끄러져 온다**(그림자 + hairline) · 넘치면 줄바꿈 · **스크롤하면 헤더 아래에 붙고 그림자·반투명 면이 생긴다** | `[사용자 지시 2026-08-24]` **아래 상세의 대상을 정하는 유일한 조작**이다. 지도 머리(460px)에 두었을 때는 세 줄로 접혀 본문 전폭으로 옮겼다. 회색 본문 위라 선택 강조에 흰 면을 쓴다 — 카드 안에서 쓰던 회색 강조는 배경과 붙는다 |

#### 운영 지표 뱃지 — 2종 (이상 탐지 결과 카드 안, 게이지 아래)

**타일 3장을 걷었다** `[사용자 지시 2026-08-24]`. `선택 사업장 이상 점수` 타일은 같은 카드의
56px 점수와 같은 값이라 지웠고(중복), 나머지 둘은 같은 카드 안 뱃지로 내려왔다 — 지금 봐야 하는
것은 이상 점수이고 두 비율은 그 판정이 **믿을 만한 상태에서 나왔는가**를 말하는 보조값이다.
목표치는 뱃지 안에 남긴다: 98%·95%가 원문 성과지표라 값만 두면 근거가 사라진다.

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|

#### 수질·설비 실시간 계측 — 수질 8종

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 계측 머리 | 상세 이동 링크 | `상세 보기` + `ChevronRight` → `/timeseries` (`aria-label`은 `시계열 변화로 이동`) | `[사용자 지시 2026-08-24]` 이 카드는 8항목을 작은 차트로 압축해 보여 준다 — 값을 자세히 읽으려면 시계열 화면이 필요하고, 그 길이 카드 머리에 있어야 읽다 막힌 자리에서 이어진다 |
| `MEAS-pH` | 계측 격자 | pH | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-DO` | 계측 격자 | DO | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-EC` | 계측 격자 | EC | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-turbidity` | 계측 격자 | Turb | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-TOC` | 계측 격자 | TOC | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-NO3N` | 계측 격자 | NO₃-N | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-temperature` | 계측 격자 | Temp | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-chromaticity` | 계측 격자 | Color | 기호 · 현재값 · 단위 · 소형 면적 차트 · 결측 시 `수신 없음` 배지 | — |
| `MEAS-inflow` | 계측 격자(유량 소절) | Qin | 기호 · 현재값 · 단위 · 소형 면적 차트 | `[회의 피드백 2026-08-24]` `[PM 의견 2026-08-25]` — 들어온 양과 나간 양의 **차이**를 보려면 둘이 다 있어야 한다 |
| `MEAS-flow` | 계측 격자(유량 소절) | Qout | 기호 · 현재값 · 단위 · 소형 면적 차트 | **유출(방류)로 확정** `[사용자 결정 2026-08-25]` — 원문 계측 사양의 유량이 이것이다 `[원문 p.55]` |
| — | 계측 격자(유량 소절) | Δ 유입 − 유출 | 파생 칸 · 부호 · `정상 범위`/`나간 양이 더 많다` · **기호 툴팁에 단위 한글 병기** | `[파생: 유입 마지막 표본 − 유출 마지막 표본]`. 음수면 증발·슬러지로 설명되지 않아 **방류 의심**의 단서다 `[TBD-46]`. 한쪽이라도 결측이면 판정하지 않는다(**E4**). 병기는 `[회의 피드백 2026-08-24]` — 이 칸만 빠져 있던 것을 2026-08-28에 채웠다 `[사용자 지적 2026-08-28]` |

**이 화면은 수질 8종만 낸다** — 설비 3종은 [SCR-OP-003](SCR-OP-003-시계열변화.md)의 필터로 본다 `[설계: 첫 화면에 11칸을 넣으면 좁은 열에서 단위가 값에 가려진다]`.

#### 이상 탐지 결과 — 상세의 첫 카드 (전폭)

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 카드 제목 | 대상 사업장 | `이상 탐지 결과 · <사업장명>` | `[사용자 지시 2026-08-24]` 탭이 위에 있어도 카드만 보면 어느 사업장의 판정인지 알 수 없다 — 설비 상태 카드와 같은 표기다 |
| — | XAI 기여 | 기여 막대 | 높이 **8px**(3px→) · 트랙 안쪽 그림자 + 채움 그라데이션 · **채움은 그 판정의 등급색 하나** · 방향은 `TrendingUp`/`TrendingDown` 아이콘 | `[사용자 지시 2026-08-24]` 3px는 선이라 길이 차이가 읽히지 않았다. 도형 문자(▲▼)는 폰트마다 크기·기준선이 달라 글자와 어긋난다 — 방향은 `sr-only` 문구가 함께 나른다 |
| — | 판정 줄 | 구간 범례 | 기여 변수 칸의 **오른쪽 아래**(`mt-auto`) | `[사용자 지시 2026-08-24]` 4구간은 타임라인 배경만이 아니라 위 게이지에도 깔려 있어 판정 줄의 끝이 그 자리다. 타임라인 밑에 전폭으로 두면 카드가 한 줄 더 길어진다 |

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 점수 덩어리 | 면 | 등급색 그라데이션 — **위 17% → 아래 투명** | `[사용자 지시 2026-08-24]` 끝을 `transparent`로 둬 카드 색과 섞여 사라진다. 14→20%(아래가 진함)는 면이 상태색으로 읽혀 값과 경쟁했고 7→4%는 면이 보이지 않았다 |
| — | 점수 덩어리 | 점수 | 56px `font-bold` **그라데이션 글자** — 위는 잉크, 아래는 마크 쪽으로 40% 민 값 | `[사용자 지시 2026-08-24]` 밝은 끝도 흰 면에서 3.3:1 이상이라 큰 글자 기준(3:1)을 지킨다 — 100%까지 밀면 `주의`가 1.97:1로 떨어진다. 구현은 `.text-gradient`(`screens.md` §8) |
| — | 이상 탐지 결과 | 배치 | 판정을 **가로 2칸**(점수 덩어리 260px / XAI 기여 5)으로 두고 그 아래 **타임라인 전폭**. 근거 3항목은 점수 덩어리 안에 뱃지로 붙는다 `[사용자 지시 2026-08-24]` — 점수·등급·게이지·근거는 "이 판정이 무엇에서 나왔는가"라는 한 이야기다. 카드 본문 512px 미만에서는 2칸을 쌓는다 | `[사용자 지시 2026-08-24]` 시간축은 넓을수록 읽히므로 타임라인에 카드 폭을 다 준다. 판정을 세로로 쌓으면 카드가 길어져 그 타임라인이 화면 밖으로 밀린다. 512px 미만에서 쌓는 이유: 1280px에서 카드 본문이 376px이라 3칸이 110px씩이 되어 XAI 막대의 라벨과 퍼센트가 한 줄에 들어가지 않는다 |
| — | 이상 탐지 결과 머리 | 상세 이동 링크 | `상세 보기` + `ChevronRight` → `/anomaly` (`aria-label`은 `이상 탐지 상세로 이동`). `sm` 미만에서는 화살표만 | `[사용자 지시 2026-08-24]` 이 카드가 요약이라는 것과 어디서 더 볼 수 있는지를 한 자리에서 말한다 |

#### 이상 점수 타임라인

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| `BAND-normal`~`BAND-critical` | 타임라인 | 구간 밴드 4 | 배경 색 + 구간 범례 | — |
| — | 타임라인 | 이상 점수 계열 | 면적 차트 · AI 전용 색(**E3**) · 결측 끊김+음영(**E4**) | — |
| — | 타임라인 | 커서 툴팁 | `시각 KST` · 점수 · 등급 | `[설계]` 시각과 값을 함께 읽어야 이상 구간을 짚을 수 있다 · 시각은 KST를 명시한다(**E5**) |

#### 이상 탐지 결과 — XAI 기여 변수 5개

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 탐지 결과 | 이상 점수·등급 | 옅은 면 위 한 덩어리 — 56px 수치 + 등급 뱃지 + 게이지 + 근거 뱃지 3개 `[사용자 지시 2026-08-24]` | `[원문 p.64]` 0~100 · 산출 시각·대상 기간·모델을 값과 함께 노출한다(**E3**) |
| `XAI-TOC` | 탐지 결과 | TOC 총유기탄소 | 기여도 막대 + `%` + 방향 | — |
| `XAI-DO` | 탐지 결과 | DO 용존산소 | 기여도 막대 + `%` + 방향 | — |
| `XAI-turbidity` | 탐지 결과 | 탁도 | 기여도 막대 + `%` + 방향 | — |
| `XAI-EC` | 탐지 결과 | EC 전기전도도 | 기여도 막대 + `%` + 방향 | — |
| `XAI-current` | 탐지 결과 | 전류(폭기 블로워) | 기여도 막대 + `%` + 방향 | — |
| — | 탐지 결과 | 산출 메타 3 | 모델 `AutoEncoder` · 대상 기간 · 산출 시각(**E3·E5**) | — |

#### 예측 차트 — 대상 1항목 + 경향 3열

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| `FCST-TOC` | 계열 차트 | 기본 대상 TOC | 계열 선(출처별 색) · 현재 기준선. **예측 파선·신뢰구간 없음** `[INC-109]` | — |
| `FCST-TOC` | 판정 3열 | TOC | **판정 문구**(14px) + 경향 칩 + `R² · 값의 출처`. **농도를 적지 않는다** `[회의 2026-08-20]` | — |
| `FCST-TN` | 판정 3열 | TN | 같음 | — |
| `FCST-TP` | 판정 3열 | TP | 같음 | — |
| — | 계열 차트 | 산출 메타 | 모델 `LSTM + Attention` · 산출 시각 · 입력 대상 기간(**E3**) | — |

#### 설비 상태 — 설비 1대당 카드 한 장

> **설비 개수는 명세가 아니다** `[TBD-48]`. 규칙은 **"설비 1대당 한 행"** 이고 아래 4대는 시연값이다.

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| `EQ-01` | 설비 상태 | 폭기 블로워 #1 | 카드 · 등급 배지 · 고장 확률 %+막대 · 잔여 수명 · MPI | — |
| `EQ-02` | 설비 상태 | 유입 펌프 #2 | 카드 · 등급 배지 · 고장 확률 %+막대 · 잔여 수명 · MPI | — |
| `EQ-03` | 설비 상태 | 약품주입 펌프 A | 카드 · 등급 배지 · 고장 확률 %+막대 · 잔여 수명 · MPI | — |
| `EQ-04` | 설비 상태 | 방류 펌프 #1 | 카드 · 등급 배지 · 고장 확률 %+막대 · 잔여 수명 · MPI | — |

#### 알람 — **이 화면에 목록을 두지 않는다**

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 요약 카드 | 사업장별 미확인 알람 | 건수 + 화살표. **누르면 그 사업장 상세로 간다** `[사용자 요청 2026-09-15]` — 이 화면에서 알람은 **건수만** 말한다 | `[사용자 지시 2026-08-24]` 화면에 목록을 또 두면 **같은 알람에 이르는 길이 셋**이 된다 — 요약 카드 클릭(모달) · 요약 머리의 링크(알람 이력) · 화면 안 목록. 목록의 정본은 [SCR-OP-007](SCR-OP-007-알람이력.md)이다. **2026-09-15에 길이 하나로 줄었다** `[사용자 요청 2026-09-15]` — 카드가 사업장 상세로 가는 입구가 되면서 그 모달은 부를 자리를 잃어 함께 걷혔고, 알람으로 가는 길은 요약 머리의 `전체 보기` 하나다. 우선순위·조건·상태 `[원문 p.32]` |

---

## 4. 요구 데이터

### 4.1 사업장 (`Site`)

| 데이터 | 타입 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| `id` `name` | string | — | `[설계]` 식별자·표시명 | 지도 핀 · 월보드 카드 |
| `industry` | 5종 열거 | — | 섬유·염색/식품/화학/도금/전자부품 `[원문 p.37]` | 월보드 카드 보조 |
| `region` `address` `province` | string | — | `[PROVISIONAL]` 실증지 주소가 원문에 없어 시·군까지만 | 지도 툴팁 · 월보드 |
| `coordinates` | `[lat, lng]` | 도(°) | `[PROVISIONAL]` 주소 기준 시·군 중심 근사값. **주소와 함께 갱신해야 한다** | 지도 핀 좌표 |
| `anomalyScore` | number \| null | 점 | 0~100 정규화 `[원문 p.64]`. 통신 두절 시 `null` | 지도 툴팁 · 월보드 · 지표 타일 |
| `status` | 4등급 \| null | — | `[INC-01·03·04]` 잠정 4단계 | 핀 색 · 상태 띠 · 등급 배지 |
| `online` | boolean | — | `[설계]` ECP 통신 상태 | 수신 표시 · 빈 상태 |
| `lastSyncIso` | ISO8601 | KST | `[설계]` 마지막 수신 시각 | 두절 사업장 안내 |
| `spark` | (number\|null)[] | 점 | `[파생: 이상 점수 계열 다운샘플]` | 월보드 스파크라인 |
| `dataThroughput` | number | % | 목표 ≥98% `[원문 p.3·121]` | **화면에 내지 않는다** `[사용자 요청 2026-08-28]` |
| `uptime` | number | % | 목표 ≥95% `[원문 p.38·119]` | 같음 |

### 4.2 계측 (`MeasurementPoint` × 288)

수질 8항목(pH·EC·탁도·DO·수온·색도·NO₃-N·TOC) + 설비 3항목(전류·전력·유량). 단위·측정범위·정확도는 `[원문 p.55]`, 표시 자릿수는 `[PROVISIONAL]`.

- 1분 주기 × 24시간 = 1,440 표본 `[사용자 확인 2026-08-27]` `[INC-111]` — 실증 장비가 1분으로 바뀌었다. **원문 값을 지우지 않는다**: `[원문 발표 p.7 그림]`은 `5분 단위 + AI 추론`이고 텍스트(p.30·48)는 `1~10분` 범위만 적는다
- **결측은 `null`.** 0으로 채우면 배출량 급감으로 오독된다(**E4**)

### 4.3 이상 점수 (`AnomalySummary` · `AnomalyPoint[]`)

| 데이터 | 타입 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| `score` | number \| null | 점 | 0~100 정규화 `[원문 p.64]` | 게이지 · 타임라인 · 지표 타일 |
| `level` | 4등급 \| null | — | `[INC-01·03·04]` · 구간 경계 `[TBD-02]` | 등급 라벨 · 밴드 |
| `computedAtIso` `windowLabel` `modelLabel` | string | KST · — · — | **E3 — 산출 시각·대상 기간·모델을 값과 함께** | 탐지 결과 산출 메타 3행 |
| `contributions[]` | `{label, weight, direction}` | — · 0~1 · — | Feature Attribution `[원문 p.64]` · 값은 `[PROVISIONAL]` `[TBD-32]` | XAI 기여 변수 5행 |

### 4.4 계열과 판정 (`ForecastSummary`)

TOC 최근 6시간 관측 구간. 필드 상세는 [SCR-OP-004](SCR-OP-004-오염도추정.md) §4.2~§4.4·§4.7.

**판정 3열(TOC·TN·TP)에 농도를 싣지 않는다** `[회의 2026-08-20]`.

| 데이터 | 표시 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| 판정 문구 | `기준보다 높음·낮음` / `수신 없음` / `기준 미설정` | — | `[회의 2026-08-20]` 소프트 센싱은 절대값 정확도를 맞추기 어려워 기준 대비만 낸다 · **축은 기준치 하나다** `[사용자 지적 2026-08-21]` — 시간·분포 대비로 대체하지 않는다 · 판정 순서는 SCR-OP-004 §6.1 | 판정 3열 헤드라인 |
| 경향 칩 | 상승 `↑` · 유지 `→` · 하락 `↓`. 표 안이라 **배경 없는 변형** | — | `[사용자 요청 2026-08-21]` 색은 한 방향에만 — 근거는 SCR-OP-004 §7.1 | 판정 3열 우측 |
| 기준 미설정 사유 | 무엇을 설정해야 하는지 — 사업장 분류 / 시트 / 항목값 셋을 구분 | — | `[설계]` 할 일이 다르므로 한 문구로 뭉치지 않는다. 기준치 표는 오염도 추정 화면이 갖는다(SCR-OP-004 §4.7) — 관제 화면에 표를 또 두면 같은 것을 두 번 적는다 | 판정 3열 하단 |
| 값의 출처 | `직접 계측`(TOC) · `소프트 센싱 추정`(TN·TP) | — | `[원문 p.55]` · `[원문 발표 p.17]` · **예전에는 세 항목 전부에 `AI 추정`을 박아 뒀다** — 실측을 추정으로 적는 것이라 **E3** 위반이었다 | 판정 3열 하단 |
| R² | TN `0.886` · TP `0.782` · **TOC `—`** | 0~1 | `[원문 p.27]` · TOC는 원문에 없다 `[INC-20·21]` | 판정 3열 하단 |
| 경향 값 | **표시하지 않는다** | 항목 단위 | `[회의 2026-08-20]` 기준 판정의 입력으로만 쓴다 | — |

**세 화면이 같은 함수를 쓴다** `[설계: 위젯마다 판정 분기를 들고 있던 동안 이 화면만 농도를 찍고 TOC를 `AI 추정`이라 적었다 — 같은 값이 화면마다 다른 판정을 갖는 것은 E1이 막으려는 것과 같은 부류다]` — [`entities/prediction/lib/verdict.ts`](../../../src/entities/prediction/lib/verdict.ts) · [`ui/trend-chip.tsx`](../../../src/entities/prediction/ui/trend-chip.tsx) · [`lib/format-r2.ts`](../../../src/entities/prediction/lib/format-r2.ts).

### 4.5 알람 (`Alarm[]`)

우선순위 3종 `[원문 p.32]` · 조건 4종 · 상태 3종. 상세는 [SCR-OP-007](SCR-OP-007-알람이력.md) §4.

**이 화면은 전 사업장 미확인 건수와 사업장별 건수만 쓴다** `[사용자 지시 2026-08-24]`. 개별 알람은 이 화면에 없다 — 목록의 정본은 `SCR-OP-007 알람 이력`이고 요약 머리의 `전체 보기`가 그리로 보낸다. 한때 요약 카드를 누르면 미확인 알람 모달이 그것을 읽었는데, 카드가 **사업장 상세로 가는 입구**가 되면서 걷혔다 `[사용자 요청 2026-09-15]`.

### 4.6 설비 (`Equipment[]`)

고장 확률 0~100% `[원문 p.66]` · RUL · MPI `[TBD-22]`. 상세는 [SCR-OP-005](SCR-OP-005-설비예지보전.md) §4.

---

## 5. 상호작용

| 조작 | 동작 | 결과 |
|---|---|---|
| 지도 핀 클릭 | 사업장 선택 | `?site=` 갱신 → 하단 전 패널이 그 사업장으로 바뀜 |
| **새로고침** | 고른 사업장과 **지도 확대가 함께 남는다** | `?site=`가 주소에 있으므로 탭·상세·지도 축척이 모두 되살아난다. 한때 지도만 전국으로 돌아갔다 `[사용자 지적 2026-08-31]` — «처음 보이는 것은 남한 전체다»(§8 `지도 연동`)를 «마운트 시점의 사업장»으로 판단해, 주소에 남은 선택까지 첫 방문으로 봤다 |
| **기본 사업장 재선택** | 다른 곳을 거쳐 `구미 염색 2공장`으로 돌아와도 **그 시도로 확대한다** | 열 곳 중 이 한 곳만 확대가 풀렸다 `[사용자 지적 2026-08-31]` — 위 새로고침 수정이 «처음»을 마운트 시점의 사업장으로 붙잡았는데 그 값이 기본 사업장(`S-02`)이라, 돌아오면 첫 방문과 구분되지 않았다. 판단 근거는 **주소가 지금 사업장을 지목하는가** 하나이며 무엇을 누르든 `?site=`가 박히므로 되돌아와도 참으로 남는다 |
| 지도 핀 hover | 강조 + 맨 앞으로 | 지도 하단 요약 카드 갱신 · 해당 시도 강조 |
| 시도 도형 클릭 | 그 시도로 확대 (최대 3배) | 같은 시도 재클릭 시 전국 복귀 |
| 시도 hover | 채움 강조 + 테두리 | 상단에 `시도명 · 사업장 N개소` 표시 |
| 확대 버튼 (전국/경기/경북) | 시도 확대 | 키보드 접근 경로 |
| 월보드 카드 클릭 | **화면 이동** — `사업장 상세`(`SCR-AD-003`, `/overview`)로, **그 카드의 사업장으로** | `[사용자 요청 2026-09-15]` 고르지 않고 바로 간다 — **선택은 여전히 바뀌지 않는다** `[사용자 지시 2026-08-24]`: 탭·핀이 가리키는 사업장은 그대로이고 이 클릭은 화면을 떠난다. 카드가 `<a>`라 새 탭·가운데 클릭·주소 복사가 브라우저의 일로 남는다. **한때 미확인 알람 모달이었다** — 그 모달은 트리거가 이 클릭 하나뿐이라 함께 걷혔고, 알람 목록은 요약 머리의 `전체 보기`(알람 이력)가 맡는다 |
| 사업장 탭 클릭 | 사업장 선택 | 지도 핀과 같은 결과. 탭은 상세 격자 **위**에 있어 아래 전체가 그 결과임이 위치로 드러난다 |
| 헤더 사업장 선택 | 사업장 선택 | 위와 같음 |
| **구역 머리 `상세 보기`** | **화면 이동** — `사업장 상세`(`SCR-AD-003`, `/overview`) | 고른 사업장 그대로 넘어간다. **이 화면에서 유일하게 화면을 옮기는 사업장 축 조작**이고 나머지 넷(핀·카드·탭·헤더)은 선택이다 `[사용자 결정 2026-08-28]` — 클릭이 곧 이동이면 지도·월보드에서 **고르며 훑는** 동작이 사라진다. 주소는 `useSiteHref('/overview')`가 만들어 `scope` 등 다른 축을 함께 나른다 |
| 차트 hover | 툴팁 | 해당 시각의 값·단위·시간대 |

| xlsx 열 | 값 |
|---|---|
| SUBMIT | N (제출 요소 없음) |
| CANCEL | N |
| After Action | 사업장 선택 시 URL 쿼리 갱신 및 하위 패널 재구성. **월보드 카드는 선택을 바꾸지 않고 화면을 옮긴다** `[사용자 요청 2026-09-15]` — 한때는 모달만 열었다. **화면을 옮기는 것은 머리의 이동 링크 6개와 월보드 카드 10장이다** — 주제별 5개(알람 이력·이상 탐지·시계열 변화·오염도 추정·설비 이상 탐지)와 사업장 축 1개(사업장 상세) `[사용자 요청 2026-08-28]`. 문구는 다섯이 `상세 보기`이고 알람 이력만 `전체 보기`다 `[사용자 확인 2026-08-31]` — 그 하나만 «전 사업장 알람을 전부»라 축이 다르다. 앞선 판본이 `화면 이동 없음`이라 적었으나 주제별 링크가 이미 있었다 |

---

## 6. 상태와 예외

| 상황 | 표시 |
|---|---|
| 통신 두절 사업장 선택 | 계측·이상 점수·예측·설비가 **모두** 빈 상태. 지도 핀은 채우지 않고 테두리만 |
| **첫 응답 대기** | 계측 격자가 **스켈레톤**이다 — 값을 미리 그리지 않는다 `[사용자 지적 2026-09-07]`. `수신 없음`과 다르다(그쪽은 확인된 부재다). 2.5초를 넘기면 내장 데이터로 내려앉고 원천 배지가 그 사실을 적는다 (**E4**·`screens.md` §8 `로딩`) |
| 구간 결측 | 차트가 끊긴 구간으로 표현 + 두절 구간 시각을 문장으로 안내(**E4**) |
| 이상 점수 없음 | `—` + "산출값 없음" + 마지막 수신 시각(**E3·R19**) |
| TOC의 R² | `원문 미규정`. 원문이 TN·TP만 값을 주므로 지어내지 않는다(**E3**) `[INC-20·21]` |
| 사업장이 없는 시도 확대 | "이 지역에는 실증 사업장이 없습니다" |
| **권한 없음** | **본문 대신 403 안내가 선다** `[사용자 결정 2026-09-15]` — 무엇이 왜 막혔는지 적고 그 역할의 첫 화면으로 가는 길을 준다. 셸은 남아 다른 메뉴로 갈 수 있다. **한때 라우트 가드가 홈으로 되돌렸다** — effect가 렌더 뒤에 돌아 권한 없는 본문이 1.61~11.75초 그대로 보였다(실측). 인가가 아니라 시연 표시다(**E6** 예외) · [`SCR-CO-002`](SCR-CO-002-안내오류.md) |

---

## 7. 설계 근거

**고르는 일과 훑는 일을 나눴다** `[사용자 지시 2026-08-24]`. 예전에는 월보드 카드가 선택도 하고 개요도 보였다 — 카드를 누르면 아래 전 패널이 바뀌는데, 그 카드가 화면에서 가장 훑기 좋은 요소라 "보려고 눌렀다가 화면이 바뀌는" 일이 생겼다. 이제 **월보드는 최상단 전폭 개요**(누르면 그 사업장 **상세** `[사용자 요청 2026-09-15]` — 2026-08-24부터 그날까지는 그 사업장 **알람 모달**이었다)이고 **선택은 지도 머리의 탭**이 전담한다. **목적지가 바뀌어도 이 분리는 남는다** — 이 문단이 막으려던 것은 «보려고 눌렀다가 **아래 화면이 바뀌는**» 일이고, 카드가 옮기는 것은 그 일이 아니다(화면을 떠나므로 돌아오면 고른 것이 그대로다). 지도 핀 선택은 그대로 남는다 — 탭과 같은 자리(지도 패널)에 있어 둘이 같은 일을 한다는 것이 보인다.

**구역을 테두리로 두르고 면은 깔지 않았다** `[사용자 지시 2026-08-24]`. 안이 전부 흰 카드라 면까지 깔면 카드를 담은 카드가 되고, 본문 배경(`#f3f4f5`)과 `--surface-2`(`#f0f3f6`)는 차이가 1.01:1이라 깔아도 보이지 않는다. 테 안쪽은 본문 배경이 그대로 비쳐 카드와 이질감이 생기지 않는다. **테의 여백(좌우 20px)은 폭을 먹는다** — 1280px에서 타일 격자 컨테이너가 2열 하한(384px) 밑으로 떨어져 지도 레일을 480→440px로 줄여 되돌렸다.

**제목·탭·상세 격자를 한 구역으로 묶었다** `[사용자 지시 2026-08-24]`. 안쪽 간격(12px)을 바깥(24px)보다 좁게 두는 것이 묶는 방법이다 — 탭이 무엇을 바꾸는지를 거리로 말한다. 테두리나 면으로 감싸는 방법은 쓰지 않았다: 그 안이 전부 카드라 카드를 담은 카드가 된다.

**아래 2단의 폭을 위 타일 3열에 맞췄다** `[사용자 지시 2026-08-24]`. 오른쪽 열을 `calc((100% - 3rem)/3)`으로 고정하면 그것이 타일 한 칸과 정확히 같아지고(`(W - 2G)/3`), 왼쪽이 자동으로 `2칸 + gap`이 된다. **`2fr_1fr`로는 맞지 않는다** — 그 경우 오른쪽이 `(W - G)/3`이라 한 칸보다 `G/3`(8px)만큼 넓다. 단이 갈리는 지점은 타일이 `@2xl`(672px), 아래 2단이 `@3xl`(768px)로 다르다 — 672px에서 나누면 오른쪽 열이 208px이라 XAI 기여 막대와 알람 목록이 함께 들어가지 않는다.

**전체 미확인 알람을 요약 머리에 녹였다** `[사용자 지시 2026-08-24]`. 지표 타일 넷 중 셋은 `선택 사업장`의 값이고 하나만 전 사업장 합계라, 같은 줄에 두면 어느 것이 무엇의 값인지 라벨을 읽어야 알 수 있었다. 요약 패널은 전 사업장을 다루는 영역이라 합계가 거기 있는 것이 축에 맞고, 카드마다 사업장별 건수가 이미 있어 머리의 합계와 카드의 개별 값이 한 패널에서 이어진다.

**월보드를 전폭으로 올린 이유**는 10개소를 한 줄에 담기 위해서다. 상세 레일 안(1280px에서 428px)에 두면 2열까지 접혀 "한눈에"가 성립하지 않는다.

**지도를 좌측 sticky 레일에 둔다** `[설계: 상세를 스크롤하는 동안에도 전체 위치가 보여야 관제 화면이다]`. 1280px 미만에서는 레일을 만들지 않는다 — 1024px에서 나누면 오른쪽에 210px만 남아 지표 타일이 44px로 뭉개진다(실측).

**지도 높이를 뷰포트에서 뺀다** `[설계: 폭 기준 분기만 쓰면 화면이 낮은 모니터에서 패널이 잘린다 — 1366×768에서 219px 잘림 실측]`. sticky 레일은 화면을 넘으면 아래쪽에 영영 닿을 수 없다.

**시도 확대는 자유 배율이 아니다** `[설계: 10개소 중 7개소가 서로의 클릭 영역을 침범. 안동 두 곳은 6.1px 거리에 핀 지름이 11px]`. 배율은 시도 bbox에서 계산해 1.4~3배이며, 상한 3배는 단순화한 지형이 각져 보이기 시작하는 지점이다.

**사업장이 있는 시도만 색칠한다** `[설계: 17개를 균일하게 칠하면 데이터가 없는 지역까지 무언가 있는 것처럼 읽힌다]`. 한 시도에 여럿이면 **가장 나쁜 등급**으로 칠한다 — 평균을 내면 위험이 묻힌다.

**지표 타일은 사업장별 값이다** `[설계: 하드코딩하면 사업장을 바꿔도 그대로라 조용히 거짓말을 한다]`.

기각한 대안: **타일 지도(Leaflet + CARTO)** — 출처 표기가 법적 의무라 화면에서 지울 수 없고, 한국어 지명 변형이 없어 `SEOUL`·`DAEGU`로 나온다. 외부 네트워크에도 의존한다. 상세는 [design-system §2.1](../../../design-system/smb-ai-emission-monitor/pages/dashboard.md).

색·타이포·지도 시각 규칙의 **정본은 [`screens.md`](../screens.md) §8이다** `[사용자 지시 2026-08-25]`. 아래 [design-system 문서](../../../design-system/smb-ai-emission-monitor/pages/dashboard.md)는 **착수 전** 판단이라 지금 값과 다르다(지도 면이 등급색 → 사업장 수 농도로 바뀐 것이 그 예다) — 검증기 결과와 함께 그때의 근거로 남겨 둔다. 여기서 복제하지 않는다.

---

## 8. 미정·제약

| 항목 | 표기 | 확정 필요 시점 |
|---|---|---|
| 실증지 주소·좌표 | `[PROVISIONAL]` | 실증 사업장 확정 시 — 주소와 좌표를 **함께** 갱신 |
| 상태 등급 체계 | `[INC-01·03·04]` | 등급 확정 시 `provisional.ts` 하나만 교체 |
| 이상 점수 구간 경계 | `[TBD-02]` | 실제 점수 분포 확보 후 오탐지율 <10% 기준 재조정 |
| 사업장–그룹–테넌트 계층 | `[TBD-08·13]` | 단지·지자체 단위 관제 착수 시 |
| 수집 주기 1분 고정 `[INC-111]` | 실증 장비값 `[사용자 확인 2026-08-27]` | 설정 저장소(서버) 생길 때 |
