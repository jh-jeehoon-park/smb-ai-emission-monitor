# 화면 설계 — 이상 탐지 (SCR-OP-002)

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 문서명 | 화면 설계 — 이상 탐지 (SCR-OP-002) |
| 버전 | v1.3.0 |
| 작성일 | 2026-08-13 |
| 기반 문서 | /docs/specs/README.md, /docs/specs/requirements.md, /docs/analysis/ai-model-spec.md, /docs/requirements/source-inconsistencies.md |

### 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.0.0 | 2026-08-13 | Claude | 신규 작성 — `docs/page-data-spec/routes.md` §4.1을 근거 표기와 함께 옮기고 화면 구성·상호작용·설계 근거를 추가 |
| v1.0.1 | 2026-08-13 | Claude | §8 등급↔우선순위 대응 태그를 `[TBD-21]`(TN·TP 계측 방식) → `[INC-02]`(알람 우선순위 체계)로 정정 |
| v1.0.2 | 2026-08-13 | Claude | 화면 ID 재배치 — `SCR-AD` → `SCR-OP`. 현재 화면은 전 사업장 통합 관제 주체(운영자)의 것이다 `[회의 2026-08-13]` |
| v1.1.0 | 2026-08-14 | Claude | §2.1에 관리자 자사 1개소 범위 구현과 사업장별 순위 블록 비노출 근거 추가 |
| v1.2.0 | 2026-08-19 | Claude | **방지시설 미가동 중 방류 의심 패널 신설**(§4.5) — 발표자료 p.11 그림의 `무단방류 의심 탐지`를 이 화면에 둔다(같은 `이상배출 조기탐지` 그룹). 판정 방법은 원문에 있다 `[원문 발표 p.13]`, 요구는 `[원문 p.2]`, 비율 근거는 `[데이터셋 …/04_…]` 2.8%. **`무단`이라 적지 않는 이유**와 체류시간 보정을 적용하지 않은 이유를 §7·§8에 기재 |
| v1.3.0 | 2026-08-19 | Claude | §8에 **우회배관 한계** 추가 — `발표 p.50` 판독으로 무단방류의 실제 형태가 우회배관임을 확인했다. 유량계를 지나지 않으므로 우리 판정으로는 잡히지 않는다. 무엇을 잡고 무엇을 못 잡는지 화면 문서가 밝혀야 한다 |

---

## 2. 화면 개요

| 항목 | 값 |
|------|-----|
| 화면 ID | SCR-OP-002 |
| 메뉴(1Depth) | 이상 탐지 |
| 메뉴(2Depth) | — |
| 화면 구분 | 대시보드 |
| 경로 | `/anomaly` |
| 구현 | [`src/widgets/anomaly-view/ui/anomaly-view.tsx`](../../../src/widgets/anomaly-view/ui/anomaly-view.tsx) |
| 관련 요구사항 | REQ-AD-005(AI 결과 시각화) · REQ-AD-006(이상 점수) · REQ-AD-007(상태 분류) · REQ-AD-008·013(기여 변수·XAI) · **REQ-AD-032**(방지시설 미가동 방류 의심) |

### 2.1 권한

| 관리자 | 운영자 | 게스트 |
|---|---|---|
| R | R | R |

**관리자는 자사 1개소만 본다** `[회의 2026-08-13]` — **구현됨**. 라우트 가드가 URL에 `?site=자사&scope=site`를 박고, 주소를 직접 고쳐도 되돌린다. 범위를 URL에 두는 이유는 서버가 역할을 모르기 때문이다([README §4.4](../README.md)).

**사업장별 이상 점수 순위 블록은 관리자에게 보이지 않는다** `[설계: 이 블록의 존재 이유가 비교다 — "한 사업장만 보면 그 점수가 높은 건지 알 수 없다". 자사 1개소뿐인 관리자에게는 비교 대상이 없어 1행짜리 목록이 된다. 그 자리에 무엇을 둘지는 관리자 전용 화면 설계에서 정한다]`.

---

## 3. 화면 구성

```
┌────────────────────────────────────────┬──────────────────────────┐
│ 이상 점수 타임라인      [0-49 50-69 …] │ 이상 탐지 결과           │
│ ┌────────────────────────────────────┐ │  88  위험                │
│ │  면적 차트 + 4구간 밴드            │ │  ├──────────────┤       │
│ │  결측 구간 음영                    │ │  0  50  70  80  100     │
│ └────────────────────────────────────┘ │  산출 모델 / 대상 기간   │
├────────────────────────────────────────┤  산출 시각               │
│ 사업장별 이상 점수    점수 높은 순      │                          │
│ ┌────────────────────────────────────┐ │  주요 기여 변수 · XAI    │
│ │ 1 구미 염색 2공장  ∿∿∿   88 위험   │ │  TOC ▲ ────────  34%    │
│ │ 2 평택 도금 A라인  ∿∿∿   84 위험   │ │  DO  ▼ ─────     27%    │
│ │ …                                  │ ├──────────────────────────┤
│ │ 10 안동 식품 B동   ——  — 수신없음  │ │ 관련 알람                │
│ └────────────────────────────────────┘ │ (조건 = 이상 탐지)       │
└────────────────────────────────────────┴──────────────────────────┘
```

| 구성요소 | 형태 | 구현 |
|---|---|---|
| 이상 점수 타임라인 | 면적 차트, 4구간 밴드, 결측 음영, 구간 범례 | `anomaly-timeline/` |
| 사업장별 순위 | 행 목록, 상태 띠, 스파크라인, 점수·등급 | `anomaly-view.tsx` `SiteScoreRanking` |
| 이상 탐지 결과 | 게이지, 산출 메타, 기여 변수 막대 | `anomaly-panel/` |
| 관련 알람 | 행 목록 (조건 필터) | `alarm-list/` |

---

## 4. 요구 데이터

### 4.1 이상 점수 계열 (`AnomalyPoint[]`)

| 데이터 | 타입 | 단위 | 근거 |
|---|---|---|---|
| `t` | ISO8601 | KST | `[설계]` 계측과 같은 시간축 |
| `score` | number \| null | 점 | 0~100 정규화 `[원문 p.64]`. 결측·두절 시 `null` |

288 표본(5분 × 24시간). 계측과 **같은 결측 판정**을 쓴다 `[설계: 한 화면에서 계측은 끊기고 점수는 이어지면 모순이 된다]`.

### 4.2 이상 탐지 결과 (`AnomalySummary`)

| 데이터 | 타입 | 단위 | 근거 |
|---|---|---|---|
| `score` | number \| null | 점 | 0~100 `[원문 p.64]` |
| `level` | `normal`\|`caution`\|`warning`\|`critical` \| null | — | `[INC-01·03·04]` 잠정 4단계 · 경계 `[TBD-02]` |
| `online` | boolean | — | `[설계]` 통신 상태 |
| `computedAtIso` | ISO8601 | KST | **E3** |
| `windowLabel` | string | — | 대상 기간 (예: 최근 1시간 다변량 패턴) **E3** |
| `modelLabel` | string | — | AutoEncoder 복원오차 기반 `[원문 p.64]` **E3** |
| `contributions[].label` | string | — | 기여 변수명 `[원문 p.64]` |
| `contributions[].weight` | number | 0~1 | Feature Attribution 기여도 `[원문 p.64]` |
| `contributions[].direction` | `up`\|`down` | — | `[설계]` 상승/하강 기여 구분 |

### 4.3 사업장별 순위

| 데이터 | 출처 |
|---|---|
| 사업장 최신 점수·등급 | `Site.anomalyScore` · `Site.status` |
| 추이 스파크라인 | `[파생: buildAnomalyScores(siteId) 다운샘플 40점]` |
| 정렬 | `[설계: 점수 내림차순, 통신 두절은 맨 끝]` |

### 4.4 관련 알람

`condition === 'anomaly'`인 알람만 `[설계: 이 화면의 관심은 이상 탐지 조건이다]`.

### 4.5 방지시설 미가동 중 방류 의심 (`IdleDischargeRun[]`)

**이상 점수와 다른 축의 탐지다.** 점수가 낮아도 여기서 잡힌다 — 그래서 같은 화면에 나란히 둔다.

| 근거 | 내용 |
|---|---|
| 요구 | "방지시설 가동상태 및 설비 이상 판단 어려움"(Before) → **"방지시설 가동상태 자동 판단"**(After) `[원문 p.2]` |
| 방법 | "**전류 발생 시기와 유량 발생 시기를 비교** → 공정 체류시간을 고려한 데이터 매칭", "수질 변화와 유량·전류·전력 간 관계 분석하여 **방류 유무 판단 가능성 검토**" `[원문 발표 p.13]` |
| 기능명 | "무단방류 의심 탐지 — 패턴 분석을 통한 무단방류 탐지" `[원문 발표 p.11 그림]` |
| 데이터 | 진유원 6,528시간 중 **184시간(2.8%)이 유량=1·전류=0** `[데이터셋 Non_TMS_sites/04]` (전류계 위치=유입펌프) |

| 데이터 | 타입 | 근거 |
|---|---|---|
| 구간 시작·종료 | ISO8601 | `[파생: 방류=true ∧ 방지시설 미가동=true 인 연속 표본]` |
| 지속 표본 수 | number | 화면이 시간으로 환산한다(수집 주기 5분) |
| 그 시각 유량·전류·전력 | number \| null | 계측값 그대로. 전류·전력이 **0**인 것이 판정의 근거다 |
| 최소 지속 시간 | 12표본(1시간) | `[PROVISIONAL]` **원문에 없다** `[TBD-46]`. 실증 데이터가 시간 단위 집계라 그보다 짧은 판정은 원본으로 검증할 수 없다 |

**결측이면 구간이 끊긴다.** 방류 여부나 가동 여부 중 하나라도 수신되지 않았으면 그 표본에서 구간을 닫는다 — 모르는 시간을 이어 붙이면 없는 의심을 만든다(**E4**).

---

## 5. 상호작용

| 조작 | 동작 | 결과 |
|---|---|---|
| 순위 행 클릭 | 사업장 선택 | `?site=` 갱신 → 타임라인·결과·알람이 그 사업장으로 |
| 차트 hover | 툴팁 | 시각·점수·등급 |
| 헤더 사업장 선택 | 사업장 전환 | 위와 같음 |

| xlsx 열 | 값 |
|---|---|
| SUBMIT | N |
| CANCEL | N |
| After Action | 사업장 선택 시 URL 갱신 및 패널 재구성. 화면 이동 없음 |

---

## 6. 상태와 예외

| 상황 | 표시 |
|---|---|
| 통신 두절 사업장 | 결과 패널이 `—` + "산출값 없음" + 마지막 수신 시각(**E3**) |
| 구간 결측 | 타임라인이 끊기고 해당 구간에 음영(**E4**) |
| 순위의 두절 사업장 | **점수를 0으로 내리지 않고** 목록 끝에 `수신 없음`(**E4**) |
| 관련 알람 0건 | 빈 목록. 개수를 머리글에 표기 |

---

## 7. 설계 근거

**"무단방류"라고 쓰지 않는다** `[설계: 무단 여부는 신고된 방류 시간대·유량 한도가 있어야 정해진다. 원문도 "방류 유무 판단 **가능성 검토**"까지만 말한다(발표 p.13). 저류된 물을 내보내는 중일 수도 있어, 판정이 아니라 의심으로 남기는 것이 옳다 — `README` §3.1은 법정 판정값을 지어내지 말라고 못박는다]`.

**판정과 그림이 한 원천에서 나온다** `[설계: 미가동 구간을 시나리오에 두고 계측 fixture가 그 구간의 전류·전력을 0으로 만든다. 두 곳에서 각각 정하면 이 화면은 "미가동"이라 적는데 시계열 차트에는 전류가 도는 모순이 생긴다]`.

**사업장별 순위를 넣은 이유** `[설계: 한 사업장만 보면 그 점수가 높은 건지 알 수 없다. 통합 관제의 판단은 늘 비교다]`. 대시보드에는 없던 요소다.

**구간 범례를 차트 머리글에 둔다** `[설계: 4구간 경계가 잠정값(TBD-02)이라 값을 읽는 사람이 경계를 함께 봐야 한다]`. 같은 범례를 통합 관제에서도 쓰며 구현은 한 곳이다(`anomaly-band-legend.tsx`) — 두 곳에 두면 구간이 갈린다.

**기여 변수에 방향(▲▼)을 함께 낸다** `[설계: 기여도만으로는 그 변수가 점수를 올렸는지 내렸는지 알 수 없다]`.

**색만으로 등급을 전달하지 않는다** — 색 + 글리프(●◆▲■) + 라벨을 함께 쓴다(REQ-NF-008).

---

## 8. 미정·제약

| 항목 | 표기 | 확정 필요 시점 |
|---|---|---|
| 상태 등급 체계 | `[INC-01·03·04]` | 등급 확정 시 |
| 방류 의심의 최소 지속 시간 | `[PROVISIONAL]` `[TBD-46]` | 현업 기준 확인 시 — `provisional.ts` 한 값만 교체 |
| 공정 체류시간 보정 | **미적용** `[TBD-46]` | 원문이 "고려한다"고만 하고 값을 주지 않았다. 보정 없이는 유입이 멈춘 직후 잔류수 방류까지 잡힐 수 있다 |
| 무단 여부 판정 | **하지 않는다** | 신고된 방류 시간대·유량 한도가 있어야 한다. 원문도 "판단 **가능성 검토**"까지만 말한다 |
| **우회배관은 잡지 못한다** | 판정의 구조적 한계 | `[원문 발표 p.50]`이 사고 사례로 `무단배출·우회배관`을 든다. **배관을 우회하면 유량계를 지나지 않으므로 방류가 관측되지 않는다** — 우리 판정(`방류 중 ∧ 미가동`)은 방류가 관측될 때만 성립한다. 잡을 수 있는 것은 *처리 없이 내보낸 정황*이지 *계측을 우회한 방류*가 아니다 |
| 점수→등급 경계 | `[TBD-02]` | 실제 점수 분포 확보 후 |
| 등급 ↔ 알람 우선순위 대응 | `[INC-02]` | 알람 연동 설계 시 |
| XAI 산출 알고리즘 | 서버 몫 | 백엔드 착수 시 — 화면은 `contributions`를 받아 그린다 |
