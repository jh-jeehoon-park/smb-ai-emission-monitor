# 화면 설계 — 현황판 (SCR-AD-006)

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 문서명 | 화면 설계 — 현황판 (SCR-AD-006) |
| 버전 | v1.1.0 |
| 작성일 | 2026-09-11 |
| 기반 문서 | /docs/specs/README.md, /docs/specs/screens.md, /docs/specs/requirements.md, /docs/requirements/source-inconsistencies.md |

### 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.1.0 | 2026-09-11 | Claude | **부품을 하나도 가져오지 않고 다시 지었다** `[사용자 요청 2026-09-11: 기존의 컴포넌트를 활용하지 않고 새로 구축하며, 톤앤매너는 유지하되 최대한 레퍼런스와 유사한 UI로 구성]`. `Panel`·`StatTile`·`MeterBar`·`Sparkline`·`CountUp`·`StaggerGroup`을 전부 걷고 `WallPanel`(강조 틱 머리 띠)·`WallBar`·`WallSpark`·`ScoreArc`·`useCountUp`을 새로 만들었다 — **토큰은 그대로라 톤앤매너가 유지되고 달라진 것은 치수와 짜임**이다(**§7.8 신설**). 레퍼런스를 따라 짜임이 4칸 줄에서 **세 열 + 아래 띠**로 바뀌었다: 이상 점수 **반원 계기**(**§7.9 신설** — `AnomalyGauge`의 «원형 게이지 기각» 사유를 네 구간·경계 눈금으로 갚는다) · 기여 변수 순위 막대(**§7.10**) · 수질 8칸에 **24시간 추이선** 추가 · 알람 집계 + 맨 위 3건(**§7.11** — 티커는 새 무한 반복이라 만들지 않았다) · 설비 · 누적 추이 + 값 셋. **캡처가 결함 셋을 잡았다**(**§7.10**·**§7.12**): 기여도가 `0.34%`로 적히던 것 · 맨 끝 표본이 늘 비어 수위·유량이 종일 `수신 없음`이던 것 · 계측 칸이 위아래로 흩어지던 것. §3 도해·구성요소 표·§3.1 항목 목록 전면 교체 |
| v1.0.0 | 2026-09-11 | Claude | 신규 작성 — 사업장 사무실 벽 TV에 띄워 두는 화면 `[사용자 요청 2026-09-10]`. **새 데이터를 만들지 않는다** — 값은 전부 다른 화면이 이미 보여 주는 것이고 달라지는 것은 크기와 밀도다 `[사용자 확인 2026-09-10: 아직 어떠한 서비스를 구축할 것인지에 대한 확정이 안된 상태임으로 현재 시스템의 톤앤매너로 어떻게 보여줄 수 있을 것에 대한 것이 중심이 됨]`. 셸을 그리지 않는 유일한 화면이며 `SCR-AD-004`를 되쓰지 않았다(§7.1) |

---

## 2. 화면 개요

| 항목 | 값 |
|------|-----|
| 화면 ID | SCR-AD-006 |
| 메뉴(1Depth) | 현황판 |
| 메뉴(2Depth) | — |
| 화면 구분 | 대시보드 |
| 경로 | `/wallboard` |
| 구현 | `src/app/(shell)/wallboard/page.tsx` · `src/widgets/wallboard/` |
| 관련 요구사항 | **REQ-AD-036**(사업장 현황판) — 원문 FR 없음. §7.3 |

### 2.1 권한

| 사업장 | 기초지자체 | 시스템 관리자 |
|---|---|---|
| R | ✕ | ✕ |

**사업장 전용이다** `[사용자 요청 2026-09-10]`. 자사 1개소를 사무실 벽에 띄우는 화면이라 `SCR-AD-005`와 같은 논리로 다른 역할이 들어갈 길이 없다 — 시스템 관리자·기초지자체는 «어디가 이상한가»를 다사업장 축에서 묻고, 한 곳을 고른 뒤에는 `SCR-AD-003 사업장 상세`가 그 자리를 이미 갖고 있다.

**`SCREEN_ROLES`에 반드시 등재된다.** 빠지면 `canRoleSee`가 미등재를 전 역할 차단으로 읽어 가드가 접근을 막고 메뉴에서도 사라진다.

**첫 화면이 아니다** `[사용자 결정 2026-09-10: 사업장 상세는 그대로]`. 메뉴에서 관제 묶음 **맨 끝**이라 `homeHrefFor('site')`가 여전히 `/overview`를 집는다 — 앞으로 옮기면 로그인 직후 벽 화면이 열린다.

---

## 3. 화면 구성

```
1920 × 1080 · 스크롤 없음 · 사이드바·헤더 없음

┌ 머리줄 ─────────────────────────────────────────────────────────────────┐
│▐ 구미 염색 2공장          [원천 …] [기준 …] [주기 1분]  [9월 11일 10:44]│
│  경북 구미 · 섬유·염색                                                  │
├──────────────┬──────────────────────────────────────┬───────────────────┤
│▐ 이상 점수   │▐ 수질 8종            지금 값과 기준  │▐ 알람    확인 필요│
│   ╭──────╮   │ ┌────────┐┌────────┐┌────────┐┌─────┐│ 미확인  ● 긴급  6│
│  ╱ 50 70 80╲ │ │pH      ││DO      ││EC      ││탁도 ││   6     ● 주의  0│
│ │  ╲  91   │ │ │8.23    ││0.90    ││1296    ││24.7 ││   건    ● 정보  0│
│  ╲  위험  ╱  │ │∿∿∿∿∿∿  ││∿∿∿∿∿∿  ││∿∿∿∿∿∿  ││∿∿∿∿ ││                  │
│              │ │▬▬▬▬░░  ││▬▬░░░░  ││▬░░░░░  ││▬▬░░ ││ ● 탁도 외 4종 …  │
├──────────────┤ │기준 5.8││오늘 …  ││오늘 …  ││오늘 ││ ● 복합 이상 패턴 │
│▐ 기여 변수   │ └────────┘└────────┘└────────┘└─────┘│ ● TOC 급변 …     │
│ ▲TOC     34% │ ┌────────┐┌────────┐┌────────┐┌─────┐├───────────────────┤
│ ▬▬▬▬▬▬▬▬▬▬   │ │TOC     ││NO₃-N   ││수온    ││색도 ││▐ 설비         4대│
│ ▼DO      27% │ │2.7     ││0.05    ││26.8    ││40   ││ ●폭기블로워#1 위험│
│ ▬▬▬▬▬▬▬▬     │ │∿∿∿∿∿∿  ││∿∿∿∿∿∿  ││∿∿∿∿∿∿  ││∿∿∿∿ ││ ●유입 펌프#2  위험│
│ ▲탁도    16% │ └────────┘└────────┘└────────┘└─────┘│ ●약품주입펌프 정상│
│ … 5줄        │                                      │ ●방류 펌프#1  경고│
├──────────────┴──────────────────────┬───────┬───────┴───┬───────────────┤
│▐ 금일 누적 배출량 추이     자정부터 │▐ 금일 │▐ 방류 수조│▐ 방류 상태    │
│                              ╱╱╱╱╱╱ │  누적 │   수위    │               │
│                    ╱╱╱╱╱╱╱╱╱        │288.4  │  3.36     │  방류 중      │
│          ╱╱╱╱╱╱╱╱╱                  │  m³   │    m      │               │
│ 결측 없음              663 m³/day   │자정부터│  지금     │  지금         │
└─────────────────────────────────────┴───────┴───────────┴───────────────┘
```

| # | 구성요소 | 형태 | 근거 |
|---|---|---|---|
| 1 | 머리줄 (`WallHeader`) | 강조 틱 + 사업장명 **30px** · 지역·업종 · 오른쪽에 `원천`·`기준`·`주기` 칩 셋 + 벽시계 알약 | **E3**·**E5** · `[설계: 셸을 그리지 않으므로 헤더가 갖던 이름·원천·시각을 이 줄이 직접 맡는다]` — §7.2 |
| 1.1 | 벽시계 (`WallClock`) | `9월 11일 (금) 오전 10:44` — **초를 쓰지 않는다**. 30초마다 갱신 | `[설계: 종일 켜 두는 화면에서 초침은 «살아 있다»가 아니라 눈에 남는 움직임이다]` — §7.4 |
| 1.2 | 패널 껍데기 (`WallPanel`) | **제목 왼쪽 강조 틱** + 굵은 제목 + 오른쪽 보조 한 줄, hairline으로 몸통과 갈린다 | `[사용자 요청 2026-09-11: 최대한 레퍼런스와 유사한 UI]` — 레퍼런스 셋이 공통으로 쓰는 표식이다. §7.8 |
| 2 | 이상 점수 계기 (`ScoreArc`) | 반원 180°를 **네 등급 구간으로 끊어** 각각 등급 색으로 칠하고 **경계마다 눈금과 숫자**(50·70·80) · 점수 자리에 **바늘** · 아래에 점수 52px + 등급 라벨 | `[원문 발표 p.15]` 등급 4단 · `[TBD-02]` 경계 · **E2**·**E4** · §7.9 — 원형 게이지 기각 사유를 갚는다 |
| 3 | 기여 변수 (`ContributionBars`) | 방향 화살표 + 이름 + 퍼센트, 그 아래 순위 막대 5줄. **가장 큰 기여가 꽉 찬 막대** | `[원문 p.60]` XAI 기여도 · §8 `막대·게이지`(채움색은 범례에 있는 색만) · §7.10 |
| 4 | 수질 8종 격자 (`QualityCell`) | 4열 × 2행. 항목명 + 기호 · 값 **34px** + 단위 · **24시간 추이선** · **기준 트랙 막대** · 기준 또는 오늘 관측 범위 | `[원문 p.55]` · §7.5 |
| 4.1 | 기준 트랙 막대 (`WallBar`) | 기준이 있는 항목은 **배출허용기준**이 트랙이고 색이 초과 여부, 없는 항목은 **오늘 관측 범위**가 트랙이고 **중립색** | `[공정자료 p.11]`(pH만 확정) · `[TBD-45]` · §7.5 — **기준을 지어내지 않는다** |
| 4.2 | 추이선 (`WallSpark`) | 손 SVG · 가로 격자 3줄 · 바닥 그라데이션 · **결측에서 끊김** · 6분 버킷으로 솎음 | **E4** · §8 `솎기` · §7.6 |
| 5 | 알람 (`AlarmTally`) | 미확인 **52px** + `건`, 곁에 우선순위 3줄(긴급·주의·정보, 색 옆에 늘 이름) · 아래에 **미확인 맨 위 3건** 제목과 조건 | `[원문 p.32]` 우선순위 3종 · `[INC-02]` · **E2** · §7.11 |
| 6 | 설비 (`EquipmentRows`) | 대당 한 줄 — 가동 점 · 이름 15px · 신호 또는 가동 문구 · 등급. 두절이면 등급을 그리지 않는다 | `[TBD-48]`(대수는 시연값) · **E4** · §7.7 |
| 7 | 금일 누적 배출량 추이 | 넓은 추이선 + 결측 건수 + **마지막으로 받은 유량** | **E4** · §7.6 · §7.12 |
| 8 | 값 한 칸 (`BigCell`) | 제목 · 값 **52px** + 단위 · 곁의 사실. 금일 누적 · 방류 수조 수위 두 자리가 쓴다 | `[원문 p.1]` 배출 데이터 · `[TBD-57]` 수위 사양 · **E4** |
| 9 | 방류 상태 | `방류 중` / `방류 없음` / `판정 불가` 34px | `[TBD-46]` · **E4** — 셋을 가른다 |

### 3.1 항목 목록

**영역이 아니라 영역 안의 항목이 한 행이다**(`deliverable-xlsx.rule.md` X4). 도메인 항목의 근거는 [`items.md`](../items.md)가 갖는다.

> **이 화면은 도메인 항목을 새로 만들지 않는다.** 아래 계측 8종은 전부 사전에 이미 있고, 나머지는 이 화면에서만 쓰는 표시 요소라 `항목ID`가 `—`다.

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 머리줄 | 사업장명 | 30px 굵은 글자 + 강조 틱 | `[설계: 벽에서 «어느 사업장인가»가 가장 먼저 읽혀야 한다 — 셸이 없어 다른 곳에 적히지 않는다]` |
| — | 머리줄 | 지역 · 업종 | 13px 보조 | 같음 |
| — | 머리줄 | 수신 원천 칩 | `계측 서버 수신 중` / `내장 데이터 · 서버 미연결` | **E3** · `integration/README.md` §5 |
| — | 머리줄 | 기준 시각 칩 | `KST` 병기 | **E5** |
| — | 머리줄 | 수집 주기 칩 | `1분` | **E5** |
| — | 머리줄 | 벽시계 | `M월 D일 (요일) 오전/오후 HH:MM` · 30초 갱신 · **초 없음** | **E5** · `[설계]` §7.4 |
| — | 이상 점수 | 반원 계기 | 네 등급 구간 + 경계 눈금·숫자 + 바늘 | `[원문 발표 p.15]` · `[TBD-02]` · §7.9 |
| — | 이상 점수 | 점수 · 등급 라벨 | 52px + 22px · 등급 색 | **E2**·**E3**·**E4** |
| — | 이상 점수 | 모델명 | 머리 띠 오른쪽 | **E3** — AI 산출은 모델을 병기한다 |
| `XAI-TOC` `XAI-DO` `XAI-turbidity` `XAI-EC` `XAI-current` | 기여 변수 | 순위 막대 5줄 | 방향 화살표 + 이름 + 퍼센트 + 막대 | — |
| `MEAS-pH` `MEAS-DO` `MEAS-EC` `MEAS-turbidity` `MEAS-TOC` `MEAS-NO3N` `MEAS-temperature` `MEAS-chromaticity` | 수질 8종 | 계측 칸 | 값 34px + 단위 + 24시간 추이선 + 기준 트랙 막대 + 기준/오늘 범위 | — |
| — | 수질 8종 | 기준 트랙 막대 | 트랙 = 기준 또는 오늘 관측 범위 · 초과는 위험색, 판정 불가는 중립색 | `[TBD-45]` · §7.5 — **없는 기준으로 초록을 칠하지 않는다** |
| — | 알람 | 미확인 건수 | 52px + `건` | `[원문 p.32]` |
| `ALP-urgent` `ALP-caution` `ALP-info` | 알람 | 우선순위 3줄 | 색 점 + 이름 + 건수 | — |
| — | 알람 | 미확인 맨 위 3건 | 제목 + 발생 조건 | `[설계: 목록 정본은 `/alarms`이고 여기는 «지금 무엇이 밀려 있나»까지다]` |
| `EQ-01` `EQ-02` `EQ-03` `EQ-04` | 설비 | 설비 한 줄 | 가동 점 + 이름 + 신호/가동 + 등급 | — |
| — | 배출 | 누적 추이 선 | 손 SVG · 격자 3줄 · 결측에서 끊김 | **E4** · §7.6 |
| — | 배출 | 마지막 유량 | `m³/day` · **맨 끝이 아니라 마지막으로 받은 값** | **E4** · §7.12 |
| — | 배출 | 금일 누적 값 | 52px + `m³` | `[파생: Σ(유량 × 수집주기 ÷ 1440)]` |
| `MEAS-level` | 배출 | 방류 수조 수위 | 52px + `m` · 채널이 없으면 그 사실을 적는다 | — |
| — | 배출 | 방류 상태 | `방류 중` / `방류 없음` / `판정 불가` | `[원문 p.1]` 방류 여부 · **E4** |

---

## 4. 요구 데이터

| 데이터 | 타입 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| 사업장명 · 지역 · 업종 | `string` | — | `[원문 p.29]` 실증 10개소 · `getSite(siteId)` | 머리줄 |
| 이상 점수 · 등급 | `number \| null` · `StatusLevel \| null` | 점 | `[원문 p.64]` AutoEncoder 복원오차 · `getAnomalySummary` | 머리줄 · 지금 상태 |
| 미확인 알람 건수 | `number` | 건 | `[원문 p.32]` · `countOpen(useAlarmStates(...))` | 지금 상태 |
| 금일 누적 배출량 | `number \| null` | m³ | `[파생: Σ(유량 × 수집주기 ÷ 1440), 자정부터]` `[원문 p.1]` | 지금 상태 · 배출 |
| 방류 여부 | `boolean \| null` | — | `[원문 p.1]` 배출 데이터 3종 · `[TBD-46]` | 지금 상태 |
| 수질 8종 현재값 | `number \| null` | 항목별 | `[원문 p.55]` 계측 사양 | 수질 8종 |
| 방류 기준치 | `DischargeLimitTable` | 항목별 | `[공정자료 p.11]`(pH) · `[TBD-45]` · 사용자 설정값 | 수질 8종 |
| 누적 추이 | `CumulativePoint[]` | m³ | 같음 | 배출 |
| 설비 상태 | `Equipment[]` | — | `[원문 p.30·62·63]` 종류만 · `[TBD-48]` 대수 | 설비 |
| 수신 상태 · 기준 시각 | `TelemetryStatus` · ISO | — | **E3**·**E5** · `useSiteSeries` | 머리줄 |

---

## 5. 상호작용

**조작이 없다.** 벽에 걸어 두고 아무도 누르지 않는 화면이라 버튼·탭·필터·툴팁을 두지 않는다.

| 조작 | 동작 | 결과(After Action) |
|---|---|---|
| (없음) | — | — |
| 사업장 전환(다른 화면에서) | `?site=`가 바뀌면 이 화면도 그 사업장을 그린다 | 카드 `key`가 바뀌어 진입 모션을 다시 밟는다 |
| 계측 수신(1분 주기) | 값이 제자리에서 갱신된다 | **바뀐 칸만 한 번 밝아진다**(§7.4) |

| xlsx 열 | 값 |
|---|---|
| SUBMIT | N |
| CANCEL | N |
| After Action | 조작이 없다. 값은 수집 주기(1분)마다 갱신되고 바뀐 칸이 한 번 밝아진다 |

---

## 6. 상태와 예외

| 상태 | 화면 |
|---|---|
| **첫 응답 대기** | 값 자리에 `수신 없음`이 아니라 **대기 문구**가 온다 — §8 `로딩`이 *"대기 문구에 확인된 부재의 어휘를 쓰지 않는다"* 로 못박았다 |
| **이상 점수 두절** | 숫자를 그리지 않고 `이상 점수 수신 없음`. **0점이 아니다**(**E4**) |
| **누적 배출량 표본 없음** | `수신 없음` + `센 표본이 없습니다 — 0이 아니라 모릅니다`. `0 m³`으로 적으면 «안 내보냈다»는 사실 주장이 된다 |
| **방류 여부 모름** | `판정 불가`. `방류 없음`과 다른 사실이다 |
| **수질 항목 결측** | 그 타일이 `수신 없음`이고 막대를 그리지 않는다 |
| **기준이 없는 항목** | 트랙이 **오늘 관측 범위**로 바뀌고 색이 중립이다 · `기준값 미확정 [TBD-45]`. **초록을 칠하지 않는다** — 없는 «정상» 판정을 만든다 |
| **통신 두절(설비)** | 등급을 그리지 않고 `통신이 두절되어 설비 상태를 확인할 수 없습니다` |
| **권한 없음** | 라우트 가드가 그 역할의 첫 화면으로 되돌린다. 메뉴에도 뜨지 않는다 |
| **감속 설정** | `CountUp`이 첫 프레임에 앉고 값 변동 하이라이트가 즉시 끝난다. **무한 반복이 이 화면에 하나도 없다** |
| **좁은 화면** | 이 화면은 **1920×1080 벽면 전용이다.** 좁은 화면에서는 칸이 눌리지만 스크롤을 열지 않는다 — 넘치면 잘려서 눈에 띄는 편이 «한 눈에»라는 요구가 조용히 깨지는 것보다 낫다 |

---

## 7. 설계 근거

### 7.1 `SCR-AD-004`를 되쓰지 않았다

그 번호는 **세 번 쓰였다 세 번 비었다** — `시스템 설정`(→ `SCR-OP-010`으로 이관, `screens.md` v2.2.0) · `처리 흐름`(2026-09-09 두 판본 폐기) · **`현황판`(2026-09-01 폐기)**.

세 번째가 이 화면과 같은 이름·같은 경로다. 2026-09-01에 완성됐고 커밋 없이 통째로 버려졌다 — **왜 버렸는지는 복구되지 않는다.** 그래서 그 판본을 되살리지 않고 다시 판단했다. 다만 그 판본이 주석에 남긴 **주관사 요구**는 그대로 유효하다:

> *"모니터에 그냥 띄어놓고 보지, 계속 움직이거나 이동하는 것은 지양한다."*

번호를 되쓰지 않는 이유는 `README` §4.1이 적어 둔 *"글자는 역사이고 뜻은 표다"* 다 — 되쓰면 세 개의 역사와 충돌한다.

### 7.2 셸 안에 두되 셸을 그리지 않는다

폐기된 판본은 `(wall)` route group으로 **셸 밖**에 나갔고, 그 대가로 전용 가드를 따로 만들어야 했다(`useRoleRouteGuard`는 `NAV_ITEMS`에 있는 경로만 본다).

이번에는 `(shell)` 안에 두고 `AppShell`이 **이 경로에서만** 사이드바·헤더를 그리지 않는다. 얻는 것 셋:

| | |
|---|---|
| 메뉴 항목이 성립한다 | `[사용자 결정 2026-09-10: 메뉴 새 항목]` |
| 전용 가드가 필요 없다 | `NAV_ITEMS`에 있으므로 기존 가드가 그대로 덮고 `?site=…&scope=site`도 그대로 찍힌다 |
| `verify:docs` 검사 3이 맞는다 | 그 검사는 «셸 라우트 수 = 사이드바 메뉴 수»를 본다 — 셸 밖으로 나가면 이 등식이 깨진다 |

**훅보다 아래에서 분기한다** — 훅은 조건부로 부를 수 없다. 가드와 헤더 높이 관측은 그대로 돌고 그리는 것만 달라진다.

### 7.3 원문 FR에 근거가 없다 — `REQ-AD-035` 선례를 따른다

원문은 «벽에 띄우는 화면»을 규정하지 않는다. `REQ-AD-033`·`REQ-AD-034`·`REQ-AD-035`가 같은 처지라 추적표의 FR 칸을 `—`로 적는 선례를 이미 만들어 두었고, 이 화면도 `REQ-AD-036`으로 그것을 따른다.

**보여 주는 값은 전부 원문 근거가 있다** — 없는 것은 «한 화면에 모아 벽에 띄운다»는 **짜임**뿐이고 그것이 `[설계]`다.

### 7.4 벽 치수와 모션 — §8에서 벗어나는 곳

2~3m 밖에서 읽히려면 `type-scale.ts`의 두 단(22·18px)으로 모자란다. **위젯 안에 그 화면만의 단을 둔다**(`WALL_VALUE_XL` 56 · `WALL_VALUE_LG` 40 · `WALL_LABEL` 20 · `WALL_META` 15) — `inout-compare`의 `HERO_VALUE`가 *"소비처가 이 화면 하나뿐이라 `shared`에 올리지 않는다"* 로 같은 선례를 만들었다. **§8 `글자 최소`(12px)는 그대로다 — 여기서는 위로만 벗어난다.**

| §8 규칙 | 이 화면에서 | 왜 |
|---|---|---|
| `보조 설명`은 툴팁 | **글자로 적는다** | 벽에는 마우스가 없어 툴팁에 닿을 수 없다 |
| `hover` 반응 | **없다** | 누를 것이 없다 |
| `차트 대체표`(그래프/표 탭) | **두지 않는다** | 탭을 누를 사람이 없다 — 대신 Recharts를 쓰지 않는다(§7.6) |

**모션은 §8을 벗어나지 않는다** `[사용자 요청 2026-09-10: 데이터가 변동 될 때에 대한 가벼운 모션은 있었으면 좋겠음]`.

`motion.tsx`가 스스로 적어 둔 명제가 이미 그 요청이다 — *"화면 진입 때 한 번 정렬되듯 올라오고, **그 뒤에는 값이 바뀔 때만 움직인다**."* 그래서 **새 예외를 등재하지 않았다**:

- 진입 1회 — `StaggerGroup` · `RiseItem` · `CountUp`
- 값이 바뀔 때 — 그 칸에 옅은 면이 한 번 깔렸다 사라진다(`useValueFlash`, 900ms)
- **티커 · 자동 순환 · 맥동 · 카운트다운 링은 만들지 않았다** — 전부 새 무한 반복이고, §8은 `SCR-AD-005`의 스크롤 카메라를 같은 이유로 지운 전례를 갖고 있다

벽시계도 같은 이유로 **초를 쓰지 않는다**. 셸의 `LiveClock`은 *"초는 남긴다 — 시계가 살아 있다는 것을 초가 보여 준다"* `[사용자 요청 2026-08-20]`가 근거인데, 아무도 보고 있지 않은 화면에서 종일 흐르는 초침은 «살아 있다»가 아니라 **눈에 남는 움직임**이다. 그 뒤집힘 때문에 `formatKstWallMinute`를 따로 두었다.

### 7.5 기준을 지어내지 않는다

막대는 문법이 하나다 — **트랙이 범위, 채움 길이가 지금 값의 위치.** 다만 그 범위가 무엇인지는 항목마다 다르고 막대 아래 글이 매번 그것을 말한다.

| | 트랙 | 색 |
|---|---|---|
| 기준이 있는 항목(pH) | **배출허용기준** | 초과 여부(등급색) |
| 기준이 없는 항목 | **오늘 관측 범위** | 중립 — 판정할 수 없다 |

`[공정자료 p.11]`이 확인해 준 것은 pH 5.8~8.6뿐이고 나머지는 규모·지역별 기준표가 있어야 고를 수 있다 `[TBD-45]`. 없는 기준으로 초록 막대를 그리면 **없는 «정상» 판정**을 만든다(`README` §3.1 «지어내지 않는 둘»).

판정 불가 항목에 **결측색을 쓰지 않는다** — 이 값들은 결측이 아니라 **온 값이고 다만 판정할 기준이 없는 것**이라 뜻으로 틀리고, 결측색은 홈 면과 명도가 가까워 2~3m에서 막대 길이가 읽히지 않는다.

**기준치는 사용자가 설정한 표에서 온다**(`useDischargeLimits`) — 정적 표를 그대로 읽으면 사업장 설정에서 고친 값이 이 화면에만 반영되지 않아 같은 항목이 화면마다 다르게 판정된다.

### 7.6 Recharts를 쓰지 않는다 — 이 화면의 유일한 그래프

벽에는 마우스가 없어 툴팁이 닿지 않고, 그러면 §8 `차트 대체표`가 요구하는 `[그래프로 보기][표로 보기]` 탭도 누를 사람이 없다.

**손 SVG(`Sparkline`)는 그 의무의 밖이다** — `sparkline.tsx`가 *"작은 표현이라 Recharts를 쓰지 않고 직접 그린다"* 로 그 경계를 이미 적어 두었다. 덤으로 `verify:docs` 검사 13(차트 포커스)도 걸리지 않는다.

**값은 글자가 갖는다.** 선은 «자정 이후 어떻게 쌓여 왔는가»만 말하고 지금 얼마인지는 위의 56px 숫자가 적는다 — 그래서 눈금·툴팁이 없어도 읽을 것이 빠지지 않는다. 결측에서는 선이 끊긴다(**E4**).

### 7.7 공용 위젯을 가져오지 않았다

`WaterQualityGrid`·`EquipmentPanel`·`AlarmList`·`DailyRibbon`은 전부 `widgets/` 아래에 있어 **위젯끼리 import 할 수 없다**(FSD §8). 가져올 수 있더라도 그쪽은 60cm 앞을 전제한 14px 라벨이라 이 화면의 단과 맞지 않는다.

**새로 만든 것은 표현이고 값은 전부 `entities`·`features`에서 온다** — `useSiteSeries` · `getAnomalySummary` · `getEquipment` · `dailyDischargeVolume` · `allAlarmsForSite` · `useDischargeLimits`. 데이터 계층은 한 줄도 건드리지 않았다.

### 7.8 부품을 하나도 가져오지 않았다

`[사용자 요청 2026-09-11: 기존의 컴포넌트를 활용하지 않고 새로 구축하며, 톤앤매너는 유지하되 최대한 레퍼런스와 유사한 UI로 구성]`.

`Panel`·`StatTile`·`MeterBar`·`Sparkline`·`CountUp`·`StaggerGroup`·`RiseItem`을 **전부 쓰지 않고** 이 위젯 안에 다시 만들었다.

| 새로 만든 것 | 공용 부품과 갈리는 곳 |
|---|---|
| `WallPanel` | **제목 왼쪽 강조 틱** — 레퍼런스 셋이 공통으로 쓰는 표식이고 `Panel`에는 없다 |
| `WallBar` | 높이를 부르는 쪽이 정한다(벽은 10px 안팎) · framer-motion 대신 **CSS 전이** |
| `WallSpark` | **가로 격자·바닥 채움·2.5px 선** — `Sparkline`은 «카드 한 칸의 작은 표현»이라 1.25px이고 2~3m에서 사라진다 |
| `ScoreArc` | 저장소에 없던 형태다(§7.9) |
| `useCountUp` | **0이 아니라 직전 값에서** 흘러간다 — 벽은 종일 켜 두는 곳이라 매번 0에서 다시 세면 바뀌지 않은 값도 움직인 것처럼 보인다 |

**토큰은 그대로다.** 색·모서리(`--radius-panel` 16 · `--radius-nested` 8)·그림자·hairline·상태색·`--actual`은 §8을 그대로 따른다 — **달라진 것은 치수와 짜임이고, 그것이 레퍼런스를 닮게 만드는 축이다.**

**framer-motion을 쓰지 않는다.** 그 라이브러리를 import 할 수 있는 파일은 저장소에서 `shared/ui/motion.tsx` 하나뿐이고(그 파일이 스스로 그렇게 적어 두었다), 이 화면의 움직임은 rAF 하나와 CSS 전이면 된다. **그래서 진입 모션이 없다** — 벽 화면은 한 번 켜고 종일 두는 곳이라 첫 페인트를 보는 사람이 없고, 값이 바뀔 때의 움직임만 뜻을 갖는다.

### 7.9 반원 계기 — 기각됐던 이유를 갚고 쓴다

`shared/ui/anomaly-gauge.tsx`가 적어 두었다 — *"원형 게이지 대신 수평 스케일을 쓰는 이유는 **4개 등급 구간의 경계를 눈으로 바로 읽을 수 있어야** 하기 때문이다."*

레퍼런스 셋 중 둘이 반원 계기를 쓰고, 그것이 «관제 현황판»을 다른 화면과 가르는 가장 뚜렷한 표식이다. 그래서 **그 기각 사유를 없애고** 쓴다:

- 호를 통째로 칠하지 않고 **네 구간으로 끊어 각각 등급 색으로** 칠한다
- **경계마다 눈금과 숫자**(50·70·80)를 적는다 — 경계가 안 보여서 기각된 것이라, 경계를 그리면 그 근거가 걸리지 않는다
- 점수와 등급 이름이 **글자로** 아래에 붙는다(**E2** — 색만으로 등급을 전달하지 않는다)

**`AnomalyGauge`는 그대로 살아 있고 다른 화면이 계속 쓴다.** 이 화면만 다른 형태를 쓰는 이유는 시청 거리뿐이다.

**두절이면 바늘을 그리지 않는다**(**E4**) — 0점 자리에 바늘을 세우면 «정상»이라는 주장이 된다.

### 7.10 기여 막대의 채움색이 중립인 이유

§8 `막대·게이지`가 못박았다 — *"**채움색은 그 화면의 범례에 있는 색만 쓴다**"*. 그 규칙이 적은 실패 사례가 **정확히 이 막대**다: XAI 기여 막대를 `--actual`로 칠했더니 그 색이 다른 차트에서는 «실측 계열»을 뜻해 같은 색이 화면마다 다른 것을 말했다.

이 화면에서 계열색(`--actual`)은 추이선이, 상태색은 계기와 수질 막대가 이미 쓰고 있다. 기여도는 **계열도 등급도 아니라** 둘 다 빌릴 수 없어 중립 잉크(`--fg-muted`)를 쓴다. **방향(상승·하강)은 색이 아니라 화살표가 나른다** — 같은 §8 규칙이다.

**퍼센트는 `weight × 100`이다.** 곱하지 않아 `0.34%`로 적힌 판본이 있었고 캡처에서 드러났다. 자릿수는 `/anomaly`가 쓰는 것과 **같은 상수**를 본다(**E1**).

### 7.11 알람 티커를 만들지 않았다

레퍼런스는 맨 위에 **긴급 상황 문구가 흐르는 티커**를 둔다. 그것은 **새 무한 반복**이라 §8 `모션`이 막는다 — 같은 규칙이 `SCR-AD-005`의 스크롤 카메라를 지운 전례를 갖고 있다.

대신 **미확인 맨 위 3건을 고정으로** 세운다. 벽에서는 흐르는 글자보다 고정된 세 줄이 오히려 읽히고, 건수는 곁의 큰 숫자가 이미 말한다.

**괄호 안의 증감(`70 (3)`)도 두지 않았다** — 레퍼런스는 전일 대비를 함께 적는데 우리는 그 값이 없다. 만들려면 어제 알람을 세는 집계를 새로 만들어야 하고, 그것은 이번 작업이 하지 않기로 한 «새 데이터»다.

### 7.12 맨 끝 표본이 아니라 «마지막으로 받은 값»을 쓴다

계측 서버가 우리 시간축보다 1분쯤 뒤에 써서 **맨 끝 칸이 늘 빈다.** 그대로 읽으면 정상 수신 중인 사업장의 수위·유량이 **종일 `수신 없음`으로 뜬다** — 실제로 첫 판본이 그랬고 캡처에서 드러났다.

그것은 두절이 아니라 시간축의 사정이다(`SCR-AD-005` §7.5가 같은 함정을 이미 적어 두었다). **채널이 아예 없는 것과는 다르다** — 그쪽은 `unreceived`가 말하고 화면이 `채널 없음 [TBD-57]`이라 따로 적는다(**E4**).

### 7.8 일부러 넣지 않은 것

| 무엇 | 왜 |
|---|---|
| **전일 대비 / 누적 대비** | 폐기된 판본의 시그니처였지만 `BUCKET_UNITS`에 일 단위가 없어 **집계를 새로 만들어야 한다.** *"새 데이터를 만들지 마라"* 와 정면으로 부딪힌다 |
| **처리 판정(0/5)** | `buildCompare`가 `widgets/inout-compare/lib`에 있어 위젯 → 위젯 import가 된다(FSD §8). 쓰려면 `entities/measurement`로 내려야 하는데 그것은 별도 작업이다 |
| **가동 ↔ 방류 두 레인** | `buildRunBands`가 같은 이유로 막힌다 |
| **알람 목록** | 건수까지만 둔다 — 목록을 넣으면 세 번째 열이 필요해 한 화면에 들어가지 않는다. 상세는 `/alarms`가 정본이다 |
| **기간 필터** | 폐기된 판본은 `?span=`으로 금일/전일/최근 24시간을 갈랐다. 대비 축을 넣지 않았으므로 고를 것이 없다 |
| **지도 · CCTV · 날씨** | 데이터가 없다. 날씨는 외부 공공 API로 확정돼 있으나 `[사용자 요청 2026-09-10: 1차 보류]` |

### 7.13 레퍼런스와 갈리는 곳

`[사용자 요청 2026-09-10]`이 준 BIX5 studio 대시보드 3종을 렌더해 확인했다.

| 가져온 것 | 버린 것 |
|---|---|
| 고정 한 화면(스크롤 없음) · 자체 머리줄 · 벽시계 · 상단 KPI 스트립 | **알람 티커**(새 무한 반복) · **다크 테마**(`[사용자 결정 2026-09-10: 라이트 유지]`) · **바늘 게이지**(§8 `막대·게이지`가 정한 형태가 아니다) · **보드 전환 탭**(조작이 없는 화면이다) |

셋 중 하나(`7_환경기술원`)가 **라이트 톤 현황판**이라, 우리 기본 테마를 바꾸지 않고도 이 장르가 성립한다는 것을 그 화면이 보여 준다.

---

## 8. 미정·제약

| 항목 | 표기 | 확정 필요 시점 |
|---|---|---|
| **무엇을 벽에 띄울 것인가** | `[설계]` — 지금은 **다른 화면의 값을 모은 것**이다. 서비스가 확정되면 이 화면의 항목이 바뀔 수 있다 `[사용자 확인 2026-09-10: 아직 어떠한 서비스를 구축할 것인지에 대한 확정이 안된 상태]` | 서비스 범위 확정 시 |
| **방류 기준치 8종 중 7종** | `[TBD-45]` — pH만 `[공정자료 p.11]`로 확정됐고 나머지는 지역구분·배출량 규모별 기준표가 필요하다. 그때까지 트랙이 «오늘 관측 범위»다 | 사업장 설정에 기준치가 입력될 때 |
| **설비 대수** | `[TBD-48]` — 원문은 종류만 정한다. 네 줄은 시연값이라 줄 수를 명세로 읽지 않는다 | 실증 사업장 확정 시 |
| **방류 의심 판정** | `[TBD-46]` — 이 화면은 «방류 중인가»만 적고 무단 여부를 판정하지 않는다. 정본은 `/anomaly` | 신고 정보 연동 시 |
| **표출 해상도** | `[설계]` 1920×1080 고정. 다른 해상도의 벽면이 나오면 칸 수를 다시 잡아야 한다 | 설치 장비 확정 시 |
