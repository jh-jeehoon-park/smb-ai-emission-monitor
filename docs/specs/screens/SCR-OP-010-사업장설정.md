# 화면 설계 — 사업장 설정 (SCR-OP-010)

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 문서명 | 화면 설계 — 사업장 설정 (SCR-OP-010) |
| 버전 | v2.0.0 |
| 작성일 | 2026-08-20 |
| 기반 문서 | /docs/specs/README.md, /docs/specs/screens.md, /docs/specs/requirements.md, /docs/applications/폐수처리 공정 및 데이터 이해.pdf |

### 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v2.0.0 | 2026-08-21 | Claude | **시스템 관리자 화면으로 옮겼다** `[사용자 결정 2026-08-21: 각 사업장을 등록하고 설정하는 것은 회원 관리나 마찬가지니 관리자의 권한]`. `SCR-AD-004` → `SCR-OP-010`, 이름 `시스템 설정` → **`사업장 설정`**. 초판 근거("허가증이 사업장 손에 있다")는 **값의 출처**에 대한 것이지 **입력 주체**에 대한 것이 아니었다 — 허가증을 제출받아 관리자가 등록하는 흐름이 일반적이고, `사업장 분류`는 값이 아니라 그 사업장을 어느 기준표에 매핑할지 정하는 **등록 정보**다. 관리자 전권 + 사업장은 `방류 기준치` 탭만(§7.1). **함께 고친 결함** — 메뉴가 권한 매트릭스와 어긋나 있었다(`navigation.ts`에 `menuRoles`가 없어 전 역할 사이드바에 뜨는데 접근은 `['site']`라 시스템 관리자가 눌러도 막혔다) |
| v1.0.0 | 2026-08-20 | Claude | 신규 작성 — 회의가 "지역별로 방류할 수 있는 기준 수치가 다를 텐데 이에 대한 설정을 위한 UI 구성이 필요해 보인다"고 정리했다 `[회의 2026-08-20]`. **법정 판정값을 우리가 정하지 않으면서 화면이 동작하는 유일한 길**이며 `[TBD-45]`의 해소가 아니라 우회다 |

---

## 2. 화면 개요

| 항목 | 값 |
|------|-----|
| 화면 ID | SCR-OP-010 |
| 메뉴(1Depth) | 사업장 설정 |
| 메뉴(2Depth) | — |
| 화면 구분 | 입력폼 |
| 경로 | `/settings` |
| 구현 | [settings-view.tsx](../../../src/widgets/settings-view/ui/settings-view.tsx) · 저장·해석 [discharge-limit-settings/](../../../src/features/discharge-limit-settings/) |
| 관련 요구사항 | REQ-AD-033(지역별 방류 기준치 설정) |

### 2.1 권한

| 사업장 | 지자체 | 시스템 관리자 |
|---|---|---|
| **R·U**(기준치 탭만) | ✕ | **R·C·U·D** |

**자기 사업장의 허가증 기준치를 넣는 화면이라 사업장만이다** `[설계]`. 남의 사업장 기준을 고칠 수 있으면 안 된다 — 지자체·시스템 관리자는 조회 화면에서 **적용된 기준**을 본다.

대상 사업장은 라우트 가드가 박아 둔 자사 1개소(`?site=`)를 그대로 쓴다 `[설계: 새 쿼리 키를 만들면 가드와 싸운다]`.

---

## 3. 화면 구성

```
┌──────────────────────────────────────────────────────────────────┐
│ 구미 염색 2공장 · 사업장 설정      [사업장 분류][방류 기준치]      │
│ 지역구분        배출량 규모        기준치 출처                     │
│ 가지역          200㎥ 미만         사용자 설정                     │
│ ─────────────────────────────────────────────────────────────── │
│ (미확정 사유 또는 "네 항목의 기준치가 모두 설정되어 초과를 판정")   │
├──────────────────────────────────────────────────────────────────┤
│ 사업장 분류 탭                                                    │
│ 지역구분         [미설정][청정지역][가지역][나지역][특례지역]      │
│ 1일 폐수배출량   [미설정][2,000㎥ 이상][700~2,000][200~700][200 미만]│
│ (허가증에서 확인한 값을 넣는다는 안내)                             │
├──────────────────────────────────────────────────────────────────┤
│ 방류 기준치 탭    [2,000㎥ 이상][700~2,000][200~700][200㎥ 미만]  │
│ 지역구분   TOC      SS        T-N      T-P      pH                │
│ 청정지역  [   ]   계측 없음  [   ]    [   ]   [  ]~[  ]           │
│ 가지역    [ 40]   계측 없음  [ 60]    [  8]   [6.0]~[8.5]  우리    │
│ 나지역    [   ]   계측 없음  [   ]    [   ]   [  ]~[  ]           │
│ 특례지역  [   ]   계측 없음  [   ]    [   ]   [  ]~[  ]           │
│ (빈 칸은 미설정이며 0이 아니다 · 값의 옳고 그름은 검사하지 않는다)  │
└──────────────────────────────────────────────────────────────────┘
```

| 구성요소 | 형태 | 구현 |
|---|---|---|
| 적용 상태 요약 | 정의 목록 3항목 + 미확정 사유 문단 | `settings-view.tsx` `Fact` |
| 탭 선택 | 세그먼트 버튼 2택 1. URL `tab` | `settings-view.tsx` |
| 사업장 분류 | 세그먼트 버튼 2조(각 `미설정` 포함) | `site-classification-form.tsx` |
| 기준치 표 | 규모 세그먼트 4택 1 + 지역 4행 × 항목 5열 숫자 입력 | `discharge-limit-editor.tsx` |
| 숫자 입력 | 단위·자릿수·범위 검증을 갖는 공용 입력 | `shared/ui/number-field.tsx` |

### 3.1 항목 목록

| 항목ID | 영역 | 구성요소 | 형태 | 근거 |
|---|---|---|---|---|
| — | 적용 상태 | 지역구분 | 현재 값 또는 `미설정` | `[공정자료 p.11]` 배출허용기준표의 첫 번째 축 · 실증 10개소 전부 미확정 `[TBD-45]` |
| — | 적용 상태 | 배출량 규모 | 현재 값 또는 `미설정` | `[공정자료 p.11]` 두 번째 축 — 종별이 배출량으로 갈린다 |
| — | 적용 상태 | 기준치 출처 | `사용자 설정` 또는 `입력 없음 · 통상 범위만` | `[설계]` 심사자가 이 기준이 어디서 왔는지 알아야 한다 — 우리가 정한 값이 아니다 |
| — | 적용 상태 | 미확정 사유 | 문단 — 분류 미설정 / 시트 없음 / 항목 없음 셋을 가른다 | `[설계]` "무엇을 해야 하는가"가 셋 다 다르다. 하나로 뭉치면 사용자가 다음 행동을 모른다 |
| — | 탭 선택 | `사업장 분류` · `방류 기준치` | 세그먼트 2택 1. URL `tab` | `[설계]` 두 일이 순서를 갖는다 — 분류가 없으면 기준치를 넣어도 적용되지 않는다 |
| `LV-*` 아님 | 사업장 분류 | 지역구분 4단계 + `미설정` | 세그먼트 5택 1 | `[공정자료 p.11]` 청정지역·가지역·나지역·특례지역 · `미설정`은 `[설계: 되돌릴 수 없으면 한 번 고른 상태가 확정으로 읽힌다]` |
| — | 사업장 분류 | 배출량 규모 4구간 + `미설정` | 세그먼트 5택 1 | `[공정자료 p.11]` 2,000㎥ 이상 / 700~2,000 / 200~700 / 200㎥ 미만 |
| — | 사업장 분류 | 허가증 안내 | 문단 | `[공정자료 p.11]` "정확한 적용 구간은 사업장 폐수배출시설 설치허가(신고)증에서 확인" |
| — | 기준치 표 | 규모 세그먼트 | 4택 1 | `[설계]` 전체 80칸을 한 화면에 그리면 읽을 수 없다. 법령 표도 규모별 시트로 인쇄된다 |
| `LGL-TOC` | 기준치 표 | TOC 상한 | 숫자 입력 1칸 · 단위 mg/L · 자릿수 1 | — |
| `LGL-SS` | 기준치 표 | SS | **입력 불가** · `계측 없음` | — |
| `LGL-TN` | 기준치 표 | T-N 상한 | 숫자 입력 1칸 · mg/L · 자릿수 1 | — |
| `LGL-TP` | 기준치 표 | T-P 상한 | 숫자 입력 1칸 · mg/L · 자릿수 2 | — |
| `LGL-pH` | 기준치 표 | pH 하한·상한 | 숫자 입력 **2칸** · 자릿수 2 | — |
| — | 기준치 표 | 우리 사업장 표시 | 해당 지역 행에 `우리 사업장` | `[설계]` 네 행 중 어느 것이 내 것인지 모르면 잘못된 행에 값을 넣는다 |
| — | 기준치 표 | 검증 사유 | 칸 아래 문구 — 뒤집힌 범위·측정 범위 밖 | `[설계]` 구조만 본다. 법정 값의 옳고 그름은 판단하지 않는다([README §3.1](../README.md)) |
| — | 기준치 표 | 미설정 안내 | 문단 — 빈 칸은 0이 아니다 | `[설계]` `Number('')`이 0이라 지운 값이 0으로 저장되면 모든 값이 초과가 된다 |

---

## 4. 요구 데이터

### 4.1 사업장 분류 (`SiteClassification`)

| 데이터 | 타입 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| 지역구분 | `RegionGrade \| null` | — | `[공정자료 p.11]` 4단계 · **사용자 입력** `[회의 2026-08-20]` | 사업장 분류 1행 |
| 배출량 규모 | `DischargeScale \| null` | — | `[공정자료 p.11]` 4구간 · 같음 | 사업장 분류 2행 |

### 4.2 기준치 시트 (`LimitSheets`)

| 데이터 | 타입 | 단위 | 출처·근거 | UI 위치 |
|---|---|---|---|---|
| 하한 | `number \| null` | 항목 단위 | **사용자 입력** `[회의 2026-08-20]` · pH만 값을 갖는다 `[공정자료 p.11]` | 기준치 표 pH 좌측 칸 |
| 상한 | `number \| null` | 항목 단위 | 같음 | 기준치 표 각 칸 |
| 갱신 시각 | ISO8601 | KST | `[설계]` 출처 문구에 날짜를 적어 언제 설정한 값인지 밝힌다(**E5**) | 기준치 출처 |

**저장은 (지역구분 × 규모 × 항목)이고 사업장별이 아니다** `[설계: 같은 지역·같은 규모면 같은 기준을 받는다. 사업장마다 넣게 하면 10번 입력해야 하고 서로 다른 값이 들어간다]`.

---

## 5. 상호작용

| 조작 | 동작 | 결과 |
|---|---|---|
| 탭 세그먼트 | 사업장 분류 / 방류 기준치 | `?tab=` 갱신 |
| 지역구분·규모 선택 | 즉시 저장 | 적용 상태 요약과 **전 화면의 기준 판정**이 함께 바뀐다 |
| 규모 세그먼트 | 시트 전환 | 표 4행이 그 규모의 값으로 교체 |
| 숫자 입력 | 즉시 저장 | 검증을 통과하면 저장 · 실패하면 칸 아래에 사유 |
| 값 지우기 | 미설정으로 되돌림 | 그 항목이 다시 `기준값 미확정 [TBD-45]`가 된다 |

| xlsx 열 | 값 |
|---|---|
| SUBMIT | N |
| CANCEL | N |
| After Action | 입력할 때마다 즉시 저장. 제출 버튼이 없다 `[설계: 저장 버튼을 두면 누르지 않고 나간 사용자가 값을 잃는다. 서버가 없어 실패할 통신도 없다]` |

---

## 6. 상태와 예외

| 상황 | 표시 |
|---|---|
| 분류 미설정 | 정적 표를 그대로 쓰고 `사업장의 지역구분·배출량 규모가 설정되지 않았습니다` |
| 분류는 됐고 시트 없음 | `이 지역구분·규모 조합의 기준치가 입력되지 않았습니다` — 분류 미설정과 **다른 문구**다 |
| 일부 항목만 입력 | 입력한 항목만 판정하고 `이 항목의 기준치가 입력되지 않았습니다` |
| 네 항목 전부 입력 | 미확정 사유가 사라진다 |
| 뒤집힌 범위·측정 범위 밖 | 저장하지 않고 칸 아래에 사유. 테두리를 세운다 |
| localStorage 접근 불가(시크릿 모드) | 이번 세션 메모리에만 남는다. 화면은 그대로 돈다 |
| 첫 렌더 | **정적 기본값**으로 그린다 — 서버가 localStorage를 모른다 |

---

## 7. 설계 근거

**이 화면은 `[TBD-45]`를 우회하는 장치다.** 배출허용기준은 법령이 원천이고 우리가 정하면 그냥 틀린 값이 된다([README §3.1](../README.md)). 회의가 "지역별로 방류할 수 있는 기준 수치가 다를 텐데 이에 대한 설정을 위한 UI 구성이 필요해 보인다"고 정리했고 `[회의 2026-08-20]`, **사용자가 자기 허가증 값을 넣게 하면** 그 원칙을 지키면서 초과 판정·기준선·초과 건수가 전 화면에서 살아난다.

**소비처를 고치지 않는다** `[설계]`. 순수 함수에 `table` 인자를 기본값과 함께 붙였다 — `hasLimit(code, table = DISCHARGE_LIMITS)`. 기존 호출과 기존 테스트가 한 곳도 바뀌지 않고, `entities`·`shared`는 localStorage도 React도 모른 채 순수하게 남는다. 표 해석은 feature가 맡고 위젯이 훅으로 주입한다.

### 7.1 왜 시스템 관리자 화면인가

`[사용자 결정 2026-08-21]` — *"각 사업장을 등록하고 설정하는 것은 회원 관리나 마찬가지니 관리자의 권한"*.

| 탭 | 성격 | 누가 |
|---|---|---|
| 사업장 분류 | 지역구분·배출량 규모 — **어느 기준표에 매핑할지 정하는 등록 정보** | 시스템 관리자 |
| 방류 기준치 | 허가증에 적힌 값. 허가증이 갱신되면 사업장이 먼저 안다 | 관리자 + **사업장** |
| 공정 구성 | 어느 단계가 있는지 — 그 사업장의 설비 구성 | 시스템 관리자 |

**초판이 사업장 전용이었던 근거가 틀렸다.** `[공정자료 p.11]`의 *"정확한 적용 구간은 사업장 폐수배출시설 설치허가(신고)증에서 확인"* 을 들었는데, 그것은 **값이 어디서 오는가**에 대한 문장이지 **누가 입력하는가**에 대한 문장이 아니다. 허가증을 제출받아 관리자가 등록하는 흐름이 실제 운영에 더 가깝다.

**회의는 설정 권한을 정하지 않았다.** 회의가 정한 것은 *"시스템 관리자는 전체를 총괄해서 볼 수 있는 권한"* 뿐이고 `볼` 권한만 말한다 `[회의 2026-08-20]`. 초판의 `사업장 R·C·U`는 우리가 `[설계]`로 채운 값이었다.

**사업장에 `방류 기준치`를 남긴 이유** `[설계]`. 계정 관리의 통상 형태다 — 관리자가 계정을 만들고 사용자는 자기 정보를 고친다. 허가증 갱신을 관리자만 반영할 수 있으면 그동안 그 사업장의 판정이 낡은 기준으로 돌아간다.

**탭 가시성은 접근 권한과 다른 축이다** `[설계]`. 화면에 들어올 수 있는지는 `SCREEN_ROLES`가, 들어온 뒤 무엇을 보는지는 `SETTINGS_TAB_ROLES`가 정한다 — `SCR-AD-002`가 접근은 전 역할이면서 메뉴 노출만 사업장인 것과 같은 구조다. 한 표에 합치면 "볼 수 있다"와 "고칠 수 있다"가 섞인다.

**기각한 대안: 우리가 법령 표를 조사해 넣는다.** `[공정자료 p.11]`이 확인해 준 것은 1종(2,000㎥ 이상) 사업장의 BOD·SS·COD뿐이고 그 셋은 우리 계측 항목이 아니다. 우리 실증 사업장은 4종이라 그 표를 적용하면 틀린다 — 조사한 값이라도 우리가 고른 것이면 지어낸 것과 같은 위험을 갖는다.

**기각한 대안: 저장 버튼을 둔다.** 누르지 않고 나간 사용자가 값을 잃는다. 서버가 없어 실패할 통신도 없으므로 즉시 저장이 더 안전하다.

**첫 렌더를 정적 기본값으로 두는 이유** `[설계]`. 테마·역할처럼 `<head>` 스크립트 + `data-*` + CSS로 가를 수 없다 — CSS는 요소를 감출 뿐 **기준선의 y좌표와 초과 건수를 고칠 수 없다.** 한 프레임 `미확정`이 스치는 것을 택했다. 틀린 기준선을 그리는 것보다 낫고, 하이드레이션이 깨지면 화면 전체가 다시 그려진다(이 저장소에서 두 번 터졌다).

---

## 8. 미정·제약

| 항목 | 표기 | 확정 필요 시점 |
|---|---|---|
| 배출허용기준 실제 값 | `[TBD-45]` — **우회했을 뿐 해소가 아니다.** 사용자가 넣지 않으면 여전히 미확정이다 | 주관기관이 실증 사업장의 지역구분·규모·방류 경로를 확인해 주면 기본값을 채울 수 있다 |
| 사업장 등록·삭제 화면 | 없다 — 이 화면은 **이미 등록된 사업장을 설정**한다 | `[TBD-09]` 계정 관리 정책이 확정되면 사업장 등록(C·D)이 이 화면에 붙거나 별 화면이 된다. 매트릭스에 `C·D`를 적어 둔 것은 **관리자가 그 권한을 갖는다는 선언**이고 화면은 아직 `R·U`만 한다 |
| 저장 위치 | localStorage. 서버가 생기면 **이 화면이 제거 대상**이 된다(`frontend.rule.md` A1) | 백엔드 착수 시 |
| 수집 주기 설정 | 미구현 `REQ-CO-001` — 저장은 되지만 **그 값을 쓰는 데이터 파이프라인이 없어** 바꿔도 아무 일도 일어나지 않는다 | 실 계측 연동 시 |
| 설비 제어 | 미구현 `REQ-CO-002` — 제어 대상·안전 인터록이 원문에 없고 **회의가 있어도 우리가 정하지 않는다** | 주관기관 |
| 공정 구성 탭 | 아직 없다. 탭 목록에 자리를 미리 두지 않았다 `[설계: 빈 탭을 누르면 고장으로 읽힌다]` | `features/process-config` 구현 시 |
| 지자체의 기준치 조회 | 이 화면은 사업장 전용이다. 지자체가 관할 지역 기준을 훑는 표는 별건 | 지자체 권한 구현 시 `[사용자 지시 2026-08-20]` |
