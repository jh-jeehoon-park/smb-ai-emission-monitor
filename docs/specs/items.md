# 항목 사전

## 1. 문서 정보

| 항목 | 내용 |
|------|------|
| 문서명 | 항목 사전 |
| 버전 | v1.2.0 |
| 작성일 | 2026-08-20 |
| 기반 문서 | /.claude/rules/deliverable-xlsx.rule.md, /docs/specs/README.md, /docs/specs/data-definition.md, /docs/applications/HSKorea_AI_Application_Proposal.pdf, /docs/applications/AIoT_Emission_Control_System.pdf |

### 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.2.0 | 2026-08-20 | Claude | **공정 단계 6 → 5** `[회의 2026-08-20]` `[INC-110]` — 표준 공정이 5단계이고 방류는 마지막 단계의 끝이다. `PS-discharge`를 `PS-advanced`에 합쳤다. 합계 76 → 75 |
| v1.0.0 | 2026-08-20 | Claude | 신규 작성 — 화면에 보이는 항목을 집합별로 등재. 근거를 **여기 한 번만** 쓰고 화면 문서 §3.1은 항목ID로 참조한다(`deliverable-xlsx.rule.md` §6.2). **14집합 75항목**. 작성 중 EC 표시 단위 모순을 발견해 `INC-106`으로 등록했다 |
| v1.1.0 | 2026-08-20 | Claude | **진동 등재** `[회의 2026-08-20]` — 설비 이상 탐지의 주 입력이다. 계측 항목 13 → 14, 합계 75 → 76. 사양(단위·범위·정확도)은 계측 사양표에 없어 `[TBD-49]`로 비웠고 **값을 표시하지 않고 이상 여부만 낸다**고 적었다. `EQUIPMENT_SERIES_CODES`에는 넣지 않는다 — y축을 세울 수 없어 시계열 화면이 빈 계열을 그린다 |

---

## 2. 이 문서가 있는 이유

화면설계서 xlsx는 **영역이 아니라 영역 안의 항목**을 행으로 쓴다(`deliverable-xlsx.rule.md` X4). 그런데 같은 항목이 여러 화면에 나온다 — 설비 4대는 8군데, 계측 11항목은 3군데, XAI 기여 5항목은 3군데다. 화면마다 근거를 다시 쓰면 320곳에서 갈린다.

그래서 **근거는 여기 한 번만 쓴다.** 화면 문서 §3.1은 `항목ID`로 참조하고, 그 화면에서의 표시 형태만 적는다. E1(단위·자릿수를 한 곳에 모은다)이 이미 쓰는 원리와 같다.

> **xlsx에서는 생성기가 이 참조를 글로 펼친다**(X8). 심사자는 `항목ID`만 적힌 칸을 보고 뜻을 알 수 없다.

### 2.1 표 읽는 법

| 열 | 뜻 |
|---|---|
| `항목ID` | `<집합코드>-<코드>`. 화면 문서 §3.1이 이 값으로 참조한다 |
| `라벨` · `기호` | 화면에 그대로 나오는 문자열 |
| `단위` | 없으면 `—`. **화면마다 다르게 쓰지 않는다**(E1) |
| `자릿수` | 표시 소수 자릿수. 실체는 `src/shared/config/provisional.ts` |
| `근거` | `README` §3 표기. **없는 행은 이 문서에 쓸 수 없다** |
| `단일 출처` | 그 항목 집합을 정의한 코드. 상수가 바뀌면 이 표도 바뀐다 |

### 2.2 집합 요약

| 집합코드 | 집합 | 개수 | 단일 출처 |
|---|---|---|---|
| `MEAS` | 계측 항목 | 14 | `src/shared/config/measurement.ts` `MEASUREMENT_ITEMS` |
| `EQ` | 설비 | 4 — **시연값** `[TBD-48]` | `src/entities/equipment/api/fixtures.ts` `EQUIPMENT_TEMPLATE` |
| `EQM` | 설비 지표 | 5 | `src/entities/equipment/model/types.ts` |
| `FCST` | 예측·추정 대상 | 4 | `src/entities/prediction/config/constants.ts` |
| `LV` | 상태 등급 | 4 | `src/shared/config/provisional.ts` `PROVISIONAL_STATUS_LEVELS` |
| `BAND` | 이상 점수 구간 | 4 | 같은 파일 `PROVISIONAL_ANOMALY_BANDS` |
| `ALP` · `ALC` · `ALS` | 알람 우선순위·조건·상태 | 3 · 4 · 3 | `src/entities/alarm/model/types.ts` |
| `PS` | 공정 단계 | 5 | `src/entities/process/config/constants.ts` `PROCESS_STAGES` |
| `ANA` | 수분석 항목 | 5 | `src/entities/water-analysis/config/constants.ts` `ANALYSIS_ITEMS` |
| `LGL` | 법정 점검 항목 | 5 | `src/shared/config/discharge-limits.ts` `LEGAL_CHECK_ITEMS` |
| `XAI` | XAI 기여 변수 | 5 | `src/entities/anomaly/api/fixtures.ts` |
| `SITE` | 실증 사업장 | 10 | `src/shared/config/demo-scenario.ts` `SITE_SCENARIOS` |
| | **합계** | **75** (집합 14종) | |

---

## 3. 계측 항목 (`MEAS`)

**단위·측정범위·정확도는 전부 `[원문 p.55]`, 표시 자릿수는 `[PROVISIONAL]`** — 원문이 자릿수를 정하지 않았다.

| 항목ID | 라벨 | 기호 | 단위 | 범위 | 정확도 | 자릿수 | 근거 |
|---|---|---|---|---|---|---|---|
| `MEAS-pH` | 수소이온농도 | pH | — | 0~14 | ±0.1 | 2 | `[원문 p.55]` · 자릿수 `[PROVISIONAL]` |
| `MEAS-EC` | 전기전도도 | EC | μS/cm | 0~20,000 | ±2% | 0 | `[원문 p.55]` · 단위 모순 `[INC-106]` — H/W 성능지표 `[원문 p.35]`는 mS/cm이나 **계측 사양을 따랐다** |
| `MEAS-turbidity` | 탁도 | Turb | NTU | 0~4,000 | ±5% | 1 | `[원문 p.55]` |
| `MEAS-DO` | 용존산소 | DO | mg/L | 0~20 | ±0.2 mg/L | 2 | `[원문 p.55]` |
| `MEAS-temperature` | 수온 | Temp | ℃ | 0~50 | ±0.5℃ | 1 | `[원문 p.55]` |
| `MEAS-chromaticity` | 색도 | Color | Pt-Co | 0~500 | ±10 Pt-Co | 0 | `[원문 p.55]` · 수질 8종의 8번째가 색도임을 `[사용자 확인 2026-08-19]` `[INC-07 해소]` |
| `MEAS-NO3N` | 질산성질소 | NO₃-N | mg/L | 0~100 | ±5% | 2 | `[원문 p.55]` |
| `MEAS-TOC` | 총유기탄소 | TOC | mg/L | 0~500 | ±10% | 1 | `[원문 p.55]` · **계측이자 AI 추정 대상** `[INC-90]` — 원문이 두 지위의 관계를 설명하지 않는다 |
| `MEAS-current` | 전류 | I | A | 0~500 | ±1% | 1 | `[원문 p.55]` · 설비 운전 센서 `[사용자 확인 2026-08-19]` |
| `MEAS-power` | 전력 | P | kW | 0~100 | ±1% | 1 | `[원문 p.55]` |
| `MEAS-flow` | 유량 | Q | m³/day | 0~1,000 | ±2% | 0 | `[원문 p.55]` |
| `MEAS-vibration` | 진동 | Vib | **원문 미규정** | **원문 미규정** | 원문 미규정 | — | `[회의 2026-08-20]` 설비 이상 탐지의 주 입력 · **사양이 없다** `[TBD-49]` — 계측 사양표 `[원문 p.55]`에 없고 목표시스템 그림·출력 화면 예시에만 나온다 `[원문 발표 p.11·18 그림]` `[INC-96]`. **값을 표시하지 않고 이상 여부만 낸다** |
| `MEAS-TN` | 총질소 | TN | mg/L | 0~100 | AI 추정 | 1 | `[원문 발표 p.17]` Soft Sensing · **계측기 없이 AI로만 추정** `[사용자 확인 2026-08-19]` |
| `MEAS-TP` | 총인 | TP | mg/L | 0~20 | AI 추정 | 2 | 같음 |

**수질 8종** = `pH · EC · turbidity · DO · temperature · chromaticity · NO3N · TOC` (`WATER_SERIES_CODES`)
**설비 3종** = `current · power · flow` (`EQUIPMENT_SERIES_CODES`)
**AI 추정 2종** = `TN · TP` — 계측 8종에 들어가지 않는다
**이상 탐지 입력 1종** = `vibration` — **시계열 계열이 아니다.** 단위·범위가 없어 y축을 세울 수 없으므로 `EQUIPMENT_SERIES_CODES`에 넣지 않았다. 넣으면 시계열 화면이 빈 계열을 그린다

> **`NH₄-N`은 등재하지 않았다.** 발표 p.15 AutoEncoder 입력층 그림에는 있으나 계측 사양 8종(`[원문 p.55]`)에 없다 `[INC-98]`. 재지 않는 항목을 화면에 두면 없는 계측을 주장한다.

---

## 4. 설비 (`EQ`) 와 설비 지표 (`EQM`)

대상 설비의 **종류**는 펌프·폭기장치·약품주입펌프 `[원문 p.30·62·63]`, 산출 모델은 **RandomForest** `[원문 p.66]`.

> **개수는 원문에 없다** `[TBD-48]`. 아래 4대는 **시연값이며 명세가 아니다.** 산출물에는 `설비 1대당 한 행` 반복 규칙을 쓰고 개수를 명세로 적지 않는다(`deliverable-xlsx.rule.md` §6.3). 시연 fixture는 전 사업장에 같은 4대를 돌려주며, **사업장별로 다른지 같은지도 원문이 말하지 않는다.**

| 항목ID | 라벨 | 근거 |
|---|---|---|
| `EQ-01` | 폭기 블로워 #1 | `[원문 p.30]` 폭기장치 · **개체명·개수 모두 시연값** `[PROVISIONAL]` `[TBD-48]` |
| `EQ-02` | 유입 펌프 #2 | `[원문 p.30]` 펌프 · 같음 |
| `EQ-03` | 약품주입 펌프 A | `[원문 p.30]` 펌프 · 같음 |
| `EQ-04` | 방류 펌프 #1 | `[원문 p.30]` 펌프 · 같음 |

> 원문 예시(`[원문 발표 p.18 그림]`)의 설비는 `펌프 · 송풍기 · 모터 · 약품펌프`다. **`모터`가 원문에만 있다** — 실증 사업장별 구성 확정 시 맞춘다 `[PROVISIONAL]`.

| 항목ID | 라벨 | 단위 | 자릿수 | 근거 |
|---|---|---|---|---|
| `EQM-failure` | 고장 확률 | % | 0 | `[원문 p.66]` 0~100% |
| `EQM-rul` | 잔여 수명 | 일 | 0 | `[원문 p.66]` · **단위 `[PROVISIONAL]`** (원문에 단위 없음) |
| `EQM-mpi` | 유지관리 우선순위(MPI) | 상대 지수 | 0 | `[원문 p.32]` · **산정식·범위·단위 없음** `[TBD-22]` |
| `EQM-runtime` | 누적 운전시간 | 시간 | 0 | `[설계: 예지보전 입력이 전류·전력·운전시간이다 `[원문 p.30·31]`]` |
| `EQM-status` | 상태 등급 | — | — | `[파생: toStatusLevel(고장 확률)]` · 등급 체계는 `LV` |

---

## 5. 예측·추정 대상 (`FCST`)

예측 구간은 **향후 6시간** `[원문 p.65]`. 모델은 **LSTM + Attention** `[원문 p.65, 발표 p.16]`.

| 항목ID | 라벨 | 단위 | R² 목표 | 근거 |
|---|---|---|---|---|
| `FCST-TOC` | 총유기탄소 | mg/L | **없음** | `[원문 발표 p.17]` 추정 대상 · R²는 원문에 없어 `null` |
| `FCST-TN` | 총질소 | mg/L | 0.886 | `[원문 p.27]` |
| `FCST-TP` | 총인 | mg/L | 0.782 | `[원문 p.27]` |
| `FCST-flow` | 유량 | m³/day | **없음** | `[INC-95]` 유량 예측 포함 여부가 원문에서 갈린다 — 포함으로 판정. 정확도 목표는 원문에 없어 `null` |

---

## 6. 상태 등급 (`LV`) 과 이상 점수 구간 (`BAND`)

**원문이 5가지 등급 체계를 혼용한다** `[INC-01·03·04]`. 발표자료 p.15의 4단계를 채택했다.

| 항목ID | 라벨 | 글리프 | 근거 |
|---|---|---|---|
| `LV-normal` | 정상 | ● | `[원문 발표 p.15·18]` |
| `LV-caution` | 주의 | ◆ | 같음 |
| `LV-warning` | 경고 | ▲ | 같음 |
| `LV-critical` | 위험 | ■ | 같음 |

> 글리프는 색각 이상·흑백 인쇄에서 등급이 사라지지 않게 두는 형태 부호다 `[설계]`. 색값과 대비 검증은 [design-system §2](../../design-system/smb-ai-emission-monitor/pages/dashboard.md).

| 항목ID | 구간 | 등급 | 근거 |
|---|---|---|---|
| `BAND-normal` | 0~49 | 정상 | `[PROVISIONAL]` `[TBD-02]` |
| `BAND-caution` | 50~69 | 주의 | 같음 |
| `BAND-warning` | 70~79 | 경고 | 같음 |
| `BAND-critical` | 80~100 | 위험 | 하한 80은 저장소 내 유일한 수치 앵커(`unclear.rule.md` §5 예시)에서 가져왔고 나머지는 균등 배분 `[PROVISIONAL]` `[TBD-02]` |

---

## 7. 알람 — 우선순위 (`ALP`) · 조건 (`ALC`) · 상태 (`ALS`)

| 항목ID | 라벨 | 근거 |
|---|---|---|
| `ALP-urgent` | 긴급 | `[원문 p.32]` |
| `ALP-caution` | 주의 | `[원문 p.32]` |
| `ALP-info` | 정보 | `[원문 p.32]` |

> 등급(`LV` 4단계) ↔ 우선순위(3단계) 대응은 **원문에 없어 추정이다** `[INC-02]` → [`assumptions.md` §3.1](assumptions.md).

| 항목ID | 라벨 | 근거 |
|---|---|---|
| `ALC-anomaly` | 이상 탐지 | `[원문 p.32]` |
| `ALC-pollutionSurge` | 오염도 급변 | `[원문 p.32]` |
| `ALC-qualityShift` | 수질 변화 이상 | `[원문 p.32]` |
| `ALC-equipment` | 설비 이상 | `[원문 p.32]` |

| 항목ID | 라벨 | 근거 |
|---|---|---|
| `ALS-open` | 미확인 | `[원문 p.32]` 확인·조치 상태 |
| `ALS-acknowledged` | 확인 | 같음 |
| `ALS-resolved` | 조치 완료 | 같음 |

> 상태 전이 규칙(누가 언제 바꿀 수 있는가)은 원문에 없다 `[TBD-05]`. 화면은 전이만 표시하고 **저장하지 않는다**.

---

## 8. 공정 단계 (`PS`)

표준 6단계 `[공정자료 p.4·6]` — **원문이 아니다**(주관사 작성 공정 설명 자료, 매일유업 TMS 사업장 기준).

| 항목ID | 라벨 | 계측 등급 | 근거 |
|---|---|---|---|
| `PS-intake` | 유입 및 전처리 | 실측 | `[공정자료 p.4·6]` · 등급 `[PROVISIONAL]` |
| `PS-primary` | 1차 침전 | AI 추정 | 같음 · 응집제 투입 지점 `[공정자료 p.4·6]` |
| `PS-biological` | 생물학적 처리 | 계측 없음 | 같음 |
| `PS-secondary` | 2차 침전 | 계측 없음 | 같음 |
| `PS-advanced` | 고도 처리 및 소독 · 방류 | 실측 | 같음 · 방류구 계측 지점 · **방류를 이 단계에 합쳤다** `[회의 2026-08-20]` `[INC-110]` |

---

## 9. 수분석 항목 (`ANA`) 과 법정 점검 항목 (`LGL`)

검증 원천 둘 — 위탁 실험실 `[원문 p.38]` · TN/TP 분석기 `[원문 발표 p.17]`(**1개소뿐**).

| 항목ID | 라벨 | 단위 | 자릿수 | AI 추정 | 근거 |
|---|---|---|---|---|---|
| `ANA-TOC` | 총유기탄소 | mg/L | 1 | ✓ | `[원문 p.38]` |
| `ANA-TN` | 총질소 | mg/L | 1 | ✓ | 같음 |
| `ANA-TP` | 총인 | mg/L | 2 | ✓ | 같음 |
| `ANA-SS` | 부유물질 | mg/L | 1 | **✕** | `[공정자료 p.5·19]` 법정 점검 항목 · **우리 계측에 없어 추정 대상이 아니다** |
| `ANA-COD` | 화학적 산소요구량 | mg/L | 1 | **✕** | 같음 |

| 항목ID | 라벨 | 배출허용기준 | 근거 |
|---|---|---|---|
| `LGL-TOC` | 총유기탄소 | **미확정** | `[공정자료 p.5·19]` 점검 항목 · 기준값은 지역구분·배출량 규모가 있어야 표를 고른다 `[TBD-45]` |
| `LGL-SS` | 부유물질 | **미확정** | 같음 · **우리 계측 항목이 아니다** |
| `LGL-TN` | 총질소 | **미확정** | 같음 |
| `LGL-TP` | 총인 | **미확정** | 같음 |
| `LGL-pH` | 수소이온농도 | **5.8~8.6** | `[공정자료 p.11]` "통상 널리 적용" — **원문이 아니다.** 정확한 적용 구간은 사업장 허가증에서 확인 |

> **법정 판정값을 지어내지 않는다**(README §3.1). pH 외에는 판정하지 않는다. 현재 fixture는 10개소 전부 pH 기준 안이라(최저 5.93 · 최고 8.27) **기준 초과 판정이 시연에서 발동하지 않는다** — [`SCR-OP-003`](screens/SCR-OP-003-시계열변화.md) §8에 보류로 등재.

---

## 10. XAI 기여 변수 (`XAI`) 와 실증 사업장 (`SITE`)

이상 점수의 근거로 함께 노출한다(**E3**). 상위 5개를 기여도 순으로 낸다.

| 항목ID | 라벨 | 근거 |
|---|---|---|
| `XAI-TOC` | TOC 총유기탄소 | `[원문 발표 p.15 그림]` XAI 기여 변수 · 기여도 값은 `[PROVISIONAL]` |
| `XAI-DO` | DO 용존산소 | 같음 |
| `XAI-turbidity` | 탁도 | 같음 |
| `XAI-EC` | EC 전기전도도 | 같음 |
| `XAI-current` | 전류(폭기 블로워) | 같음 |

> **기여도 수치는 원문에 없다** `[PROVISIONAL]` `[TBD-32]`. 상위 몇 개를 어떻게 고르는지도 원문에 없어 5개 고정으로 뒀다 `[설계: 발표 p.15 그림이 5개를 보여준다]`.

| 항목ID | 사업장 | 지역 | 근거 |
|---|---|---|---|
| `SITE-01`~`SITE-10` | 실증 10개소 | 경기·경북·광주 등 | 실증 사업장 수 `[원문 p.29]` · **개별 사업장명·업종·지역은 `[PROVISIONAL]`**(원문이 개소 수만 정하고 명단을 주지 않는다) |

> `SITE-04`(안동 식품 B동)만 **통신 두절** 상태다 `[설계: 결측·두절 처리(E4)를 화면에서 보여야 하는데, 전 사업장이 정상이면 그 경로가 한 번도 그려지지 않는다]`.

---

## 11. 등재하지 않은 것

혼동을 막기 위해 함께 적는다. **아래는 항목이 아니다.**

| 항목 | 왜 아닌가 |
|---|---|
| `NH₄-N` | 발표 p.15 그림에는 있으나 계측 사양 8종에 없다 `[INC-98]` |
| 진동 · 압력 | `[원문 발표 p.18 그림]`의 이상 유형에 나오나 **설비 운전 센서는 전류·전력·유량뿐**이다 `[사용자 확인 2026-08-19]` `[INC-96]` |
| BOD · COD(계측) | 배출허용기준 표는 있으나 **2,000㎥ 이상(1종) 대상**이고 우리 계측 항목이 아니다 `[공정자료 p.11]` |
| 여백 · 구분선 · 장식 아이콘 | 사용자가 읽을 내용이 없다(`deliverable-xlsx.rule.md` §6.1) |

---

## 12. 갱신 규칙

- 코드 상수가 바뀌면 **같은 작업에서** 이 표도 바꾼다. `npm run verify:docs`가 개수를 대조한다
- 새 항목은 근거 없이 등재할 수 없다(X1). 근거가 없으면 `[TBD-nn]`을 먼저 등록한다
- 화면 문서 §3.1은 **항목ID만** 참조한다. 근거를 거기 다시 쓰면 두 곳이 갈린다
